# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

app-example
.expo/
.expo/
.vercel
.expo/
supabase/.temp/

# Локальная веб-сборка для снимков экранов (scripts/shoot-screens.mjs)
.figma-export/

# Сырые ответы первого партнёра могут содержать токены и ПДн. Они сначала
# попадают только в локальный staging; в git разрешены лишь обезличенные fixtures.
.partner-intake/

# Питон складывает разобранные модули рядом с исходником — появляется, как
# только что-нибудь из infra/ загружают модулем
__pycache__/
*.pyc

# Секреты — живут только на хостинге, в репозиторий не попадают
php-proxy/sb_service_key.php
php-proxy/admin_credentials.php
php-proxy/app_secrets.php
php-proxy/sb_url.php
php-proxy/deploy_token.php
php-proxy/studio_credentials.php
php-proxy/gh_token.php
php-proxy/backup_s3.php

# Ключ шифрования push-подписок. Создаётся сам при первом запуске php-proxy
# (например, прогоном tests/push_privacy_test.php) и у каждой установки свой.
# В репозитории ему делать нечего по двум причинам: это секрет, и выкладка
# копирует php-proxy/*.php на сервер, не удаляя локальные секреты, — файл из
# git затёр бы производственный ключ, и уже сохранённые подписки перестали бы
# расшифровываться.
php-proxy/push_token_key.php
