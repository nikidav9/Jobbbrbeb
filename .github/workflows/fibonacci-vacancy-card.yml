name: Temporary Fibonacci vacancy card patch

on:
  push:
    branches: ["ui/fibonacci-vacancy-card"]
    paths: [".github/workflows/fibonacci-vacancy-card.yml"]

permissions:
  contents: write

jobs:
  patch-card:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo "workflow parsed"
