name: Apply truthful address suggestion states

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-address-suggest-truth.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm

      - name: Apply guarded patch
        run: python3 scripts/apply_address_suggest_truth.py

      - name: Validate
        run: |
          git diff --check
          php tests/offline_states_test.php
          npm ci
          npm test
          npm run typecheck

      - name: Commit source fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add services/db.ts components/feature/AddressSuggestField.tsx tests/offline_states_test.php
          git commit -m "fix: distinguish address lookup failures from empty results"
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
