name: Apply truthful network states

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-network-truth.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm

      - name: Apply guarded patch
        run: python3 scripts/apply_network_truth.py

      - name: Validate
        run: |
          git diff --check
          php tests/offline_states_test.php
          npm ci
          npm run typecheck

      - name: Commit source fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/register-worker.tsx app/register-employer.tsx app/support.tsx tests/offline_states_test.php
          git commit -m "fix: keep registration and support truthful offline"
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
