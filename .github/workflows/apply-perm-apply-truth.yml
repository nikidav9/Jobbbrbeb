name: Apply truthful permanent application state

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply_perm_apply_truth.py'
      - '.github/workflows/apply-perm-apply-truth.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply guarded patch
        run: python3 scripts/apply_perm_apply_truth.py
      - name: Validate focused contracts
        run: |
          git diff --check
          python3 tests/network_action_truth_test.py
          npm ci
          npm run typecheck
      - name: Commit source fix
        run: |
          if git diff --quiet; then
            echo 'No source changes'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/perm-vacancy-detail.tsx tests/network_action_truth_test.py
          git commit -m 'fix: keep successful permanent applications truthful'
          git push origin HEAD:main
