-- ВТБ убран из ленты карьерных вакансий (решение владельца, 25.09.2026).
--
-- Раздел rabota-vtb.ru/career отдаёт вакансии, ссылки которых ведут на
-- api.hh.ru: источник по сути — hh, а с hh мы не работаем. Сам адрес уходит в
-- scripts/career-runtime-quarantine.json, и обход его больше не трогает. Но
-- уже загруженные строки карантин не гасит: ingest.php гасит пропавшее только
-- на полном круге, а на неполном — через неделю. Поэтому гасим сразу здесь.
--
-- Строки не удаляются: на них могут ссылаться отклики и свайпы.
update jm_ext_vacancies
   set active = false
 where active
   and (company = 'ВТБ' or url like '%api.hh.ru%');
