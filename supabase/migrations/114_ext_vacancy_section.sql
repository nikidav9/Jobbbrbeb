-- Раздел вакансии с карьерного сайта: «IT», «Склад», «Курьеры» и т.д.
--
-- Решение владельца 25.09.2026: лента как у Sorce — человек в шестерёнке
-- выбирает раздел, колода показывает только его; не выбрал ничего — подбор
-- решает сам. Карьерные сайты рубрик не присылают, поэтому раздел выводится
-- из названия правилом php-proxy/job_sections.php при приёме (ingest.php).
--
-- Пересчёта здесь нет, и он не нужен: приёмник на каждом заходе (раз в
-- шесть часов) перезаписывает все вакансии, которые видит, — у живых раздел
-- появится на ближайшем круге. Дублировать правило на SQL значило бы завести
-- вторую копию, которая разойдётся с первой.
--
-- Колонка до кода: infra/local-web-deploy.sh копирует php-proxy только после
-- удачных миграций, так что ingest.php не начнёт писать в несуществующее поле.
alter table public.jm_ext_vacancies add column if not exists section text;

create index if not exists jm_ext_vac_section_idx
  on public.jm_ext_vacancies (section) where active;
