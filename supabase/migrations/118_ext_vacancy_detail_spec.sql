-- Где лежит JSON-карточка вакансии, из которой describe.php берёт описание.
--
-- У части источников страница вакансии — пустая оболочка SPA: разбор HTML
-- ничего не находит (Магнит — все ~2700 вакансий 25.09.2026 остались без
-- описания). Зато у них есть открытый JSON одной вакансии с обязанностями,
-- требованиями и условиями по полям. ingest.php кладёт сюда
-- {url, list, sections} из map.detail_* записи в career-endpoints.json,
-- describe.php идёт по url вместо страницы.
--
-- Колонку пишет только ingest и только у строк таких источников: у остальных
-- ключа в upsert нет, значение не трогается.
begin;

alter table public.jm_ext_vacancies add column if not exists detail_spec jsonb;

commit;
