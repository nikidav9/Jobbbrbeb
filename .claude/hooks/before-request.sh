#!/usr/bin/env bash
# Правила JobToo перед КАЖДЫМ запросом.
#
# Прежний хук срабатывал один раз за сессию и только перед первой правкой
# файла. Дальше правила держались на памяти — а память в длинном разговоре
# вытесняется ровно тем, ради чего правила и написаны.
#
# Текст намеренно короткий: он подмешивается к каждому запросу, и лишние
# полсотни слов здесь стоят дороже, чем кажется.
set -u
cat <<'JSON'
{
  "hookSpecificOutput": {
    "hookEventName": "UserPromptSubmit",
    "additionalContext": "Правила JobToo (CLAUDE.md), применить к этому запросу:\n1. Grill-me: не бросаться делать. Сначала прожать запрос вопросами — строго по одному, к каждому свой рекомендованный ответ. Факты, которые можно узнать из кода, базы или окружения, выяснять САМОМУ, не спрашивать. Спрашивать только решения, которые за владельцем. Исключение: запрос однозначен и проверить всё можно самому.\n2. Скиллы: перед работой подобрать подходящий из .claude/skills/ и вызвать через Skill. Экраны — senior-frontend/frontend-design; публичные страницы и разметка — seo-optimizer; токены и ПДн — security-review; база — supabase-postgres-best-practices; чужой код — code-review/simplify; размытая задача — prompt-master.\n3. Экономия токенов: сначала docs/MAP.md, потом поиск. Большие файлы (feed.tsx 4800 строк, db.php 5200) целиком не читать — grep, затем sed нужным куском.\n4. Проверять свою работу, а не докладывать об успехе: сверить diff, прогнать проверку, посмотреть сборки."
  }
}
JSON
