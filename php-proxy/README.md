# php-proxy

Файлы для `jobtoo.ru/api/`. FTP-деплоя (Reg.ru) больше нет: московский сервер
раз в минуту забирает их прямо из репозитория (`git pull` → копирование в
`/opt/jobtoo-proxy`, см. `infra/bootstrap.sh` и `infra/README.md`) — правка
коммитом доезжает без ручной заливки.

| Файл | Зачем |
|---|---|
| `db.php` | Всё общение приложения с базой |
| `api.php` | Внешнее партнёрское API (`/api/v1`) — отдельно от `db.php`, у которого другой формат ответов и один общий пропуск |
| `admin.php` | Дашборд: вход и доступ к таблицам `jm_*` |
| `deploy.php` | Приём секретов, доставленных из GitHub Actions (замена FTP-доставки `app_secrets.php` и др.) |
| `expo-updates-proxy.php` | Обновления приложения |
| `sb_lite.php` | Короткий доступ к базе для файлов, которым не нужен весь `db.php` как обработчик запроса |
| `metro.php`, `metro_stations.php` | Справочник станций метро — собираются автоматически из `constants/metro.ts`, руками не править |
| `openapi.json`, `sample-feed.json` | OpenAPI-схема и пример фида для внешнего API |

### Телеграм-бот

| Файл | Зачем |
|---|---|
| `tg.php` | Вебхук телеграм-бота |
| `tgtool.php` | Служебные действия с ботом: пост в общую группу, настройка вебхука |
| `bot_brain.php` | Что бот умеет отвечать сам |

### Партнёрский шлюз и источники вакансий

| Файл | Зачем |
|---|---|
| `ingest.php` | Забор вакансий из чужих источников — вторая половина агрегатора (первая — `api.php`, которым нашу выдачу читают снаружи) |
| `arbihunter.php` | Адаптер каталога arbihunter под формат фида JobToo (`docs/feed-format.md`) |
| `headhunter.php` | Адаптер публичного поиска hh.ru |
| `superjob.php` | Адаптер каталога SuperJob |
| `superjob_oauth.php`, `superjob_oauth_lib.php` | OAuth SuperJob: redirect URI зарегистрированного приложения и общие примитивы — токены серверу клиента не отдаются |
| `partner_core.php` | Независимое ядро партнёрского шлюза — конечный автомат статусов, без привязки к конкретному партнёру |
| `partner_billing.php` | Чистые функции партнёрского биллинга (без побочных эффектов — безопасно гонять в sandbox) |
| `partner_outbox.php` | Доставляет поставленные в очередь отклики подключённым партнёрам (по cron) |
| `partner_webhook.php` | Принимает подписанные изменения статуса отклика от партнёра |

## Три файла, которых нет в репозитории

Все три лежат только на хостинге и в git не попадают — иначе ключи оказались
бы в открытом доступе. Расширение `.php` тут не для красоты: если кто-то
запросит такой файл по прямой ссылке, сервер выполнит его и отдаст пустоту,
а не содержимое.

Собирает их выкладка (`.github/workflows/deploy-regru.yml`) из GitHub Secrets;
руками ничего заливать не нужно. Если секрет не задан, файл не создаётся —
тот, что уже лежит на хостинге, остаётся нетронутым.

### `sb_service_key.php`

Секретный ключ из Supabase → Settings → API Keys (`sb_secret_…`).
Образец: `sb_service_key.example.php`.

Файл **обязателен**: запасного ключа в коде больше нет. Раньше здесь стоял
анонимный ключ «чтобы ничего не легло» — но он же пережил бы смену ключей и
работал бы дальше втихую. Нет файла — запросы честно падают, и это видно
сразу.

### `admin_credentials.php`

Логин и пароль для входа в дашборд. Образец: `admin_credentials.example.php`.

Это единственная преграда между интернетом и всей базой, поэтому пароль
нужен длинный и нигде больше не используемый. Перебор ограничен: десять
неудачных попыток с одного адреса — и вход закрыт на 15 минут.

Смена пароля мгновенно обесценивает все выданные токены: они подписаны
ключом, который выводится из пароля.

### `app_secrets.php`

Пропуск к `db.php` (`APP_SECRET`) и токен бота (`TG_BOT_TOKEN`).
Образец: `app_secrets.example.php`. GitHub Secrets, из которых он собирается:
`EXPO_PUBLIC_APP_SECRET`, `APP_SECRET_PREV`, `TG_BOT_TOKEN`.

Адреса геокодирует Яндекс.Геокодер (HTTP) прямо с сервера, ключ
`YANDEX_GEOCODER_KEY` в `app_secrets.php`. Иностранных геокодеров нет: без ключа
координаты по адресу просто не определяются.

Оба **обязательны**: запасных значений в коде больше нет ни у одного.
Токен бота уже утекал вместе с открытым репозиторием — посторонний переписывал
боту описание на рекламу чужого VPN — и был отозван. Если секрет не задан,
соответствующая часть просто молчит: это лучше, чем работа на ключе, который
знает чужой.

Про `APP_SECRET_PREV`. Сменить пропуск разом нельзя: у части людей приложение
уже установлено, и старый пропуск зашит в него до следующего обновления по
воздуху. Поэтому новый кладём в `EXPO_PUBLIC_APP_SECRET`, старый — в
`APP_SECRET_PREV`, и `db.php` какое-то время принимает оба. Когда все
обновятся, `APP_SECRET_PREV` очищается, и старый перестаёт работать.

## Как это устроено

Раньше и приложение, и дашборд ходили в Supabase с публичным ключом. Ключ
этот по своей природе виден всем — он лежит в бандле сайта. Пока RLS
выключены, с ним читались телефоны, хеши паролей и вся переписка.

Теперь наружу не уходит ни один ключ. Приложение зовёт `db.php` по имени
операции — их 88, всё остальное отклоняется. Дашборд ходит через `admin.php`,
и тот пропускает запрос только с действующим токеном и только к таблицам
`jm_*`. Сервисный ключ подставляется уже на сервере.

## Пароли

Пароль не покидает сервер. Вход — операция `dbLogin`: телефон и пароль уходят
в `db.php`, там `password_verify`, обратно едет профиль **без поля
`password`**. Регистрация и смена пароля хешируют на сервере же.

Так было не всегда. До 1 августа сверка шла на клиенте: приложение просило
`dbGetUserByPhone`, получало строку целиком вместе с паролем и сравнивало у
себя. Пропуск к `db.php` публичен — он в бандле сайта, — поэтому пароль
любого человека доставался по одному номеру телефона. А с конца июня
регистрация вдобавок перестала хешировать, и 176 паролей из 381 лежали
открытым текстом.

От той поры не осталось ничего: `dbGetUserByPhone` закрыт — операция отвечает
410 и хеш пароля по номеру телефона больше не достать. Вход у не обновившихся
работает через `dbLogin`.

Что осталось — совместимость внутри самого `dbLogin`: пароль, лежащий открытым
текстом, там ещё сверяется строкой (`hash_equals`) и при удачном входе тут же
переводится в bcrypt. Эту ветку можно убрать, когда в базе не останется ни
одного пароля без хеша: `select count(*) from jm_users where password <> ''
and password not like '$2y$%'`.
