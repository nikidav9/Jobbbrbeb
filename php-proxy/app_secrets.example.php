<?php
/**
 * Секреты приложения. В репозитории лежит только этот пример.
 *
 * На хостинге файл называется app_secrets.php и рядом с db.php / tg.php его
 * кладёт выкладка (.github/workflows/deploy-regru.yml) из GitHub Secrets.
 * Руками создавать не нужно — только если поднимаете копию у себя.
 *
 * Почему .php, а не .json или .env: если кто-то откроет
 * https://jobtoo.ru/api/app_secrets.php, сервер выполнит файл и отдаст пустую
 * страницу. Текстовый файл он отдал бы как есть.
 */

return [
    // Пропуск в db.php: заголовок X-App-Secret. Тот же самый должен попасть в
    // приложение через EXPO_PUBLIC_APP_SECRET на сборке.
    'APP_SECRET'      => '',

    // Предыдущий пропуск — на время смены секрета. Установленные приложения
    // ещё какое-то время ходят со старым, пока не получат обновление.
    // Когда обновятся все — очистить.
    'APP_SECRET_PREV' => '',

    // Токен бота из @BotFather.
    'TG_BOT_TOKEN'    => '',

    // Ключ «HTTP Геокодер» Яндекса (developer.tech.yandex.ru → Геокодер).
    // Сервер в РФ. Даёт точные координаты по адресу; если его нет — работает
    // Геосаджест (ниже). Иностранных геокодеров в проекте нет.
    'YANDEX_GEOCODER_KEY' => '',

    // Ключ «API Геосаджеста» Яндекса — подсказки адреса при вводе (сервер в РФ).
    // Используется для поиска адреса вакансии, если нет ключа Геокодера.
    'YANDEX_SUGGEST_KEY' => '',
];
