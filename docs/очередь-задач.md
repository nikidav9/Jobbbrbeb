# Очередь задач

**16.09: все интеграции с внешними источниками вакансий удалены** (решение
владельца — «чистый лист», код и данные). Всё, что ниже говорит про
Arbihunter, SuperJob, hh.ru, карьерные страницы работодателей, партнёрский
шлюз заявок и разведку cofinder/career-discover, — история того, что было
построено и потом снесено, а не актуальное устройство. Подробности —
`docs/MAP.md` (разделы «Партнёрские источники» и «Источники вакансий»).

**17.09: раздел подработки закрыт** (решение владельца — «только постоянная
работа»). Из приложения убраны лента смен, экран публикации смены, вкладки
«Карьера» и «Чаты» (переписки остались отдельной страницей и вторым разделом
экрана «Отклики»); осталось три вкладки — Вакансии, Отклики, Профиль.
`dbUpsertVacancy` и `dbUpsertVacancyBatch` на сервере отвечают 410: около 500
человек сидят на прежней сборке, где экран публикации смены ещё есть, и
запретить его правкой клиента нельзя. Прежние смены из базы не удалялись —
старые мэтчи, оценки и переписки по ним продолжают работать. Всё, что ниже
написано про смены как про действующий раздел, — история.

Telegram выведен из контура. Пока Claude недоступен, Codex продолжает задачи в
ветке `claude/gifted-hamilton-q470gy`. Строки с именем исполнителя оставлены как
история, а не как ограничение текущего распределения.

**День Codex 15.09: 279 коммитов, 116 файлов.** Считать их по одному
бессмысленно — около 160 из них служебные: он работал через одноразовые
воркфлоу по схеме «завёл патчер → применил → удалил патчер». Содержательных
линий шесть, и они стоят того, чтобы быть записанными здесь, а не только в
истории git.

1. **Правда о сети, около двадцати пяти правок.** Самая большая линия и прямое
   продолжение находки от 14.09 про `backendOffline`. Экраны врали при обрыве
   связи в обе стороны: сбой показывался то как «ничего нет», то как «успешно
   сохранено». Теперь подтверждения ждут сервера — профиль, отклики, решения по
   кандидатам, оценки, удаление вакансий, согласие, регистрация, поддержка,
   адресные подсказки, привязка Telegram, отметки о прочтении.
2. **Атомарность, миграции 067–070.** Мэтч смены, одобрение постоянной заявки,
   вставка сообщения вместе со счётчиком непрочитанного, партнёрские очереди
   через CAS. Раньше это были составные операции, рвущиеся посередине.
3. **Права объектов.** Мутации привязаны к аутентифицированным объектам, чаты и
   аналитика — к серверной личности; `tests/db_object_authz_test.php`.
4. **RLS в выкладке.** `infra/verify-rls.sh`: деплой падает, если защита строк
   на проде сломана. Проверка живая, а не по коду.
5. **SEO на проде.** Аудит индексируемости после каждого деплоя и карта сайта с
   явной инвалидацией кэша — раньше она могла рекламировать адрес, который уже
   честно отвечает 404.
6. **Когорта активации** (`dashboard/lib/workerCohort.ts`) и воронки ленты и
   смен на сервере.

**Смена модели: два раздела, один партнёр (15.09, решение владельца).**
JobToo становится платформой из двух разделов. «Подработка» — смены линейного
персонала, наши четыре вида работ. «Работа» — постоянные вакансии компаний, в
том числе IT и крупный бизнес. Из агрегаторов остаётся **один партнёр,
Arbihunter**; SuperJob и hh.ru выключены миграцией 072, «Работа в России»
удалена целиком миграцией 073.
Остальное — свои вакансии и официальные карьерные страницы.

Правило приёма стало зависеть от раздела, и это не мелочь. Отсев по виду работ,
введённый часом раньше, требовал `work_type` от всех подряд — при новой модели
он выбросил бы **весь** раздел «Работа»: ни Arbihunter, ни карьерные страницы в
складские четыре вида не укладываются и не должны. Теперь профессия обязательна
только для `kind = shift`; для постоянных вакансий она может быть пустой.

**Чем это грозит и о чём предупреждено.** На выключенных трёх держался основной
объём: hh тянул всю Москву, trudvsem — государственный каталог. Поток чужих
вакансий падает до каталога Arbihunter (на 15.09 — 303 вакансии) плюс свои.
Карьерные страницы объём пока не заменят: `career.php` читает только разметку
JobPosting, а её нет ни у одного проверенного работодателя — ни у складских, ни
у крупных IT (Сбер, МТС, Яндекс, Ozon, ВТБ, VK, Т-Банк — проверено живьём).
Источники выключены, а не удалены: вернуть любой — одна правка `enabled`.

**Разобран cofinder, и копировать у него нечего (15.09).** Владелец просил
посмотреть, откуда конкурент берёт вакансии, и повторить. Разбор —
`docs/источники-вакансий-разбор.md`, здесь только то, что меняет решения.

У cofinder открытый API без ключа: `/api/v1/companies/` отдаёт 160 компаний
вместе с адресами их карьерных сайтов, 111 из них российские. Список у нас
есть целиком. Но искать в его базе наши профессии бесполезно: комплектовщик —
0, упаковщик — 0, «повар» находит «Руководителя проекта по автоматизации».
Его собственная категория «Logistics / Supply Chain» пуста. Оба конкурента
(второй — swipejobs) работают на офисном и IT-рынке, а мы про смены.

Проверили и свою нишу напрямую: Самокат, X5, Магнит, ВкусВилл, Детский мир,
Яндекс Еда — **ни на одном нет разметки JobPosting**. `career_feed.php` умеет
только её, так что по нашей нише он сегодня не прочитает ничего. Дело не в
коде: читать нечего.

Гипотеза про системы найма не подтвердилась — все 111 держат карьерный сайт на
своём домене. Хотя у Хантфлоу публичный API карьерных сайтов есть и работает
(`<компания>.huntflow.io/api/vacancy`, проверено на пяти клиентах); канал
дешёвый, но вакансии там офисные.

Цена их подхода видна в их же ответе: `parse_status` показывает **15 сломанных
парсеров из 111**, причины — перевёрстанный сайт, просроченный сертификат, а у
Альфа-Банка в сообщении лежит Selenium stacktrace, то есть ради части сайтов
они гоняют браузер.

**Главное, что нашлось по дороге, и это важнее источников.**
`headhunter.php:26` тянет `api.hh.ru/vacancies?area=1` — всю Москву без фильтра
по профессии. `ing_work_type` возвращает `null` для всего, что не склад, повар
или бригадир, но строка с `null` всё равно записывается (`ingest.php:238`), а
при выдаче в ленту фильтра по профессии нет. То есть человеку, ищущему смену,
мы показываем бухгалтеров и Java-разработчиков. Это и есть ответ на «2000+
вакансий — ноль откликов»: не мало вакансий, а не те.

Порядок работ записан в разборе. Коротко: сначала почистить hh-фильтром то, что
уже есть; потом разведка по своей нише; коннектор — под то, что найдётся;
агрегаторы отключать последними, иначе лента опустеет.

**Ревью этого дня нашло тихую поломку на проде, миграция 071 (15.09).** Правило
«проверять работу целиком, а не по отчёту» отработало ровно так, как задумано:
279 коммитов уехали в прод без единого ревью, и в самой аккуратной из линий —
атомарности — потерялись две строки.

Сводя мэтч в одну транзакцию, миграция 069 не перенесла в функцию инкремент
`jm_vacancies.workers_found` и перевод смены в `closed` при наборе. Писать
`workers_found` стало НЕКОМУ: единственный оставшийся путь, `dbUpdateVacancy`,
зовётся с одним лишь `status` при автозакрытии по времени. Читают это поле
четверо — `api.php` в `spots_left`, дашборд в доле закрытия, `services/db.ts`,
лента.

Что это значило для людей: смена, набрав работников, не закрывалась и висела в
ленте, собирая отклики сверх нужного. Партнёру в `spots_left` мы всегда обещали
полный набор мест. Доля закрытия смен в дашборде читала нули — то есть метрика,
по которой судят о работе сервиса, показывала «ничего не закрывается», и это
было правдой.

Проверено не чтением, а живой базой: поднял Postgres 16, применил 069 и 071,
прогнал три сценария. На 069 мэтч состоялся (`matched = t`), а `workers_found`
остался `0`. На 071 первый мэтч даёт 1, второй закрывает смену, повтор вызова
счётчик не накручивает.

**Заодно нашлась слепая проверка.** `tests/shift_match_atomicity_test.py` читал
жёстко файл `069`. Переопредели функцию новой миграцией — и тест продолжал бы
зеленеть, глядя на файл, который больше ничего не решает. Теперь он берёт
последнюю миграцию, определяющую функцию, — как их применяет `migrate.sh`.
Проверок стало 21, и мутация (убрать 071) роняет ровно три новых.

**Забор сообщений бота больше не встаёт навсегда (15.09).** Вторая находка того
же ревью, и она была тяжелее первой: не метрика врала, а сервис молчал.

Не сумев отдать update обработчику, забор не двигал `offset` и делал `break` —
Телеграм отдавал тот же update снова каждые три секунды. Для временного сбоя это
верно, но предела не было: один update, который обработчик принять не может,
останавливал бота НАВСЕГДА, и за ним вставала вся очередь сообщений и нажатий.
Снаружи — «бот молчит». Сторож при этом зелёный: отметку живости обновляет
успешный `getUpdates`, а он как раз продолжает успешно приходить.

Починка различает два случая, которые прежде лечились одинаково. Обработчик
ответил 400, 404, 413 или 422 — он разобрал запрос и отказался, повтор не
поможет никогда. Обработчик недоступен или ответил 5xx — повтор осмыслен, но не
дольше пяти попыток. 401 и 403 намеренно НЕ считаются окончательными: их даёт
разъехавшийся `APP_SECRET`, который подхватывается со следующей попытки.

Сдавшись, забор не выбрасывает сообщение молча — кладёт его целиком в
`/var/lib/jt-tg-dead-letters.jsonl` и считает в тех же замерах, что читает
суточный отчёт (`dropped`). Иначе починка превратила бы «бот стоит» в «бот тихо
теряет сообщения», а это не лучше.

Проверка — `tests/tg_poll_stuck_test.py`, и она нарочно функциональная: гоняет
настоящий `main()` с подменёнными сетью и обработчиком. Поломка была не в
отсутствии строк, а в поведении во времени, и проверка по тексту зеленела бы на
сломанном коде. Мутация (вернуть вечный повтор) роняет шесть проверок из девяти.

**Осталось из того же ревью, отдельными задачами не расписано, потому что чинить
это надо вместе с людьми, которые знают продукт:** повтор в `useSignedMedia`
мёртв, потому что `dbSignMedia` не бросает, а возвращает `null`;
`statsLoadFailed` в `user-profile` недостижим по той же причине;
`sm_cache_invalidate` не зовётся для постоянных вакансий; в миграции 068 нет
блокировки пары, которая есть в 069.

**Задача 23 закрыта (14.09).** При создании мэтча сервер записывал две
системные строки, а экран переписки одновременно дорисовывал собственные
редакции тех же сообщений. До следующего опроса человек видел один текст,
после — другой. Клиент больше не создаёт системные сообщения: после успешного
`dbCheckAndCreateMatch` он сразу перечитывает чат через `pollRef`. Источник
текста теперь один — сервер. Проверка: `tests/chat_match_refresh_test.py`.

**Задача 22 закрыта (13.09), но наполовину решением не делать.**

Счётчики сужены. `dbGetVacancyStatsMap` и `dbGetPermVacancyViewsMap` читали две
таблицы ЦЕЛИКОМ и отдавали каждому вошедшему отклики, отказы и просмотры по
всему рынку. Персональных данных там нет, но числа чужие, а нагрузка — на
каждое обновление экрана. Теперь выбираются только свои вакансии, порциями по
сто (идентификаторы уходят в адрес запроса, и работодатель с сотней вакансий
упёрся бы в его длину). Экран «мои вакансии» и так фильтровал список по себе,
так что не потеряно ничего.

**А вот `dbAutoClosePastVacancies` я оставил как есть, и это решение, а не
недоделка.** Она закрывает смены, время которых прошло, и зовётся при каждом
запуске приложения любым вошедшим. Выглядит как уборка не на своём месте —
но перенести её на расписание GitHub значило бы сделать ХУЖЕ. В этом же файле
записано, почему: заявленное `*/5` на деле давало запуск раз в два-четыре часа.
Смена, время которой прошло, висела бы открытой до часа, и на неё откликались
бы. Сейчас её закрывает первый же зашедший человек.

Запрос при этом дешёвый — не полная выборка, а фильтр по статусу и дате, и
обычно он не находит ничего: первый зашедший закрыл, остальные приходят на
пустое место. Троттлинг через `jm_settings` добавил бы чтение и запись на каждый
запуск, то есть работы БОЛЬШЕ, чем экономит.

Правильное место для неё появится вместе с настоящим расписанием вне GitHub
Actions — это уже записано отдельной находкой ниже.

**Задача 20 закрыта целиком (13.09).** Приложение больше не шлёт уведомлений
вообще. Мэтч извещает `dbCheckAndCreateMatch`, итог смены — `dbSetShiftOutcome`,
и оба — только ту сторону, которая событие не нажимала. Тексты, включая разные
слова на каждый исход смены, переехали на сервер целиком.

Семь вызовов с экранов убраны, восемь помощников удалены, а вместе с ними вся
цепочка отправки с телефона — `pushTo`, `sendExpoPush`, `sendTelegramTo`,
`sendWebPushTo` — и три обёртки в `services/db.ts`.

Раз звать стало некому, пять опасных операций закрыты админским токеном:
`tgNotifyUser`, `sendPushNotification`, `tgNotifyNewApplication`,
`dbGetPushToken`, `dbSaveNotification`. Это и есть то, ради чего задача
заводилась: пока они были открыты вошедшему, через нашего бота можно было
послать что угодно тому, с кем есть переписка.

Правило `jt_may_notify`, написанное часом раньше, удалено вместе с нуждой в
нём. Оставлять сторожа, которого никто не зовёт, — ровно та видимость проверки,
на которую я сам жаловался в прошлой записи.

**Нашлось по дороге.** В `components/feature/PermApplicationsSheet.tsx`
оставались вызовы уведомлений о решении по заявке — экран, который я пропустил,
когда переносил это на сервер. Дубля человек не видел только потому, что
заголовки совпадали и срабатывал глушитель повторов в `notify_user`.

**Что сохранено дословно.** На экране блокировки по-прежнему ни имени, ни
текста сообщения; в пуше работодателю о мэтче нет имени кандидата, но есть
название смены; подтверждённая смена уходит в канал `default`, а не `matches` —
для этого `notify_user` получил ещё один необязательный довод.

**Задача 20, первая часть: уведомления о сообщениях (13.09).** Право писать
человеку я закрыл раньше, но содержание оставалось за клиентом: телефон
отправителя собирал заголовок из своего имени и слал. Значит, тому, с кем есть
переписка, через нашего бота можно было послать что угодно. Плюс обычная для
этой породы потеря при обрыве связи.

Теперь уведомление собирает сервер — в `dbInsertMessage` и в `dbCreateChat`.
Имя отправителя из базы, текст из того, что сервер только что записал. Три
вызова в переписке и два в ленте убраны, сами помощники удалены.

Заодно убралось ДВОЙНОЕ уведомление: при отклике на постоянную вакансию
работодатель получал и серверное «Новая заявка», и клиентское «💬 Имя» — с
разными заголовками, поэтому глушитель повторов в `notify_user` их не гасил.

Уведомление ставится не внутри `chat_ensure`, а в обработчике `dbCreateChat`, и
это не мелочь: `chat_ensure` зовёт ещё и `dbApplyPermVacancy`, которая извещает
сама. Поставь уведомление внутрь — и два пуша вернулись бы.

**Что осталось по задаче 20.** Клиент по-прежнему собирает текст для
уведомлений о мэтче и об итоге смены (`notifyWorkerGotMatch`,
`notifyEmployerGotMatch`, `notifyWorkerShiftConfirmedByEmployer`,
`notifyWorkerShiftCancelled`). Все эти события сервер и так обрабатывает сам —
`dbCheckAndCreateMatch` и `dbSetShiftOutcome`, — так что переносить есть куда.

**Ревизия прав — вторая половина, задача 21 (13.09).** Разобраны все
одиннадцать операций, отдававших чужие данные. Пять оказались дырами.

`dbGetLikes` отдавала ВСЮ таблицу откликов сервиса: кто куда откликался, кому
отказали, чем кончилась смена. Любому вошедшему, одним запросом. Приложение и
само звало её на каждом обновлении списка — то есть чужое ездило по сети
постоянно.

`dbSignMedia` подписывала ссылку на ЛЮБОЙ файл переписки — чужие фотографии и
голосовые читались любым вошедшим. `dbUploadChatMedia` пускала заливку с чужим
именем, и с заголовком `x-upsert`, то есть файл можно было подменить. Спасло то,
что id чата стоит в самом имени файла: по нему и проверяем. Схему имён сверил по
истории — она не менялась ни разу, так что старые файлы правка не задевает.

`dbFileComplaint` брала заявителя из тела запроса: жалобу можно было подать от
чужого имени. `dbGetPermVacancyViewers` отдавала список посмотревших чужую
вакансию — при том что её сменный близнец `dbGetVacancyViewers` проверку имел.

**Нашлось по дороге и стоит запомнить.** `dbGetLikeByVacancyWorker` была в
`$selfArgFns`, то есть работодателю на его же смену отвечала отказом. Экран
переписки обошёл это мягким путём — стал выкачивать все отклики сервиса, чтобы
найти в них один. Проверка, которую обходят так, хуже отсутствующей: она создаёт
видимость, а дыру открывает шире. Теперь операция отвечает обеим сторонам смены,
и обходить нечего.

**Что осмотрено и оставлено как есть.** `dbGetUserById` отдаёт
`USER_PUBLIC_COLS` — имя, метро, рейтинг; ни телефона, ни пароля там нет, и
собеседнику по делу это видно по существу. `dbGetRatingsForUser` — отзывы на то
и отзывы. `dbUserStats` считает отзывчивость по метаданным переписок, без
текстов. `dbGetVacancyStatsMap` и `dbGetPermVacancyViewsMap` отдают счётчики по
всем вакансиям разом: персональных данных нет, но это чужие цифры рынка и
полная выборка двух таблиц на каждый вызов — записано задачей 22.
`dbAutoClosePastVacancies` закрывает смены с прошедшей датой — работа уборщика,
запускать её может кто угодно без вреда, но месту в приложении она не
соответствует, туда же.

**Сплошная ревизия прав в `db.php` — первая половина (13.09).** Построена
таблица: 153 операции против всех механизмов проверки (`$adminFns`,
`$publicFns`, `$selfArgFns`, `$chatArgFns`, `$ownedVacancyFns`, точечные
проверки до `switch`). Без механизма и без собственной проверки оказались 23.

**Сначала — поправка к моему же отчёту.** Я писал, что у
`dbSetPermApplicationStatus` «чей это отклик, не проверялось вовсе». Это
неверно: проверка `Application access denied` стояла в блоке до `switch` ещё до
моей правки, я её не заметил, потому что смотрел только тело `case`. Настоящей
находкой там была потерянная строка в чат и потерянное уведомление, а не права.
Про `dbUpsertLike` и `dbNotifyAllWorkersNewVacancy` сказанное верно — там не
было ничего.

**Закрыто восемь дыр, все одного вида: вход требовался, а чьё это — нет.**

| Операция | Что можно было |
|---|---|
| `tgNotifyUser` | отправить через нашего бота любой текст любому человеку по id |
| `sendPushNotification` | послать пуш на любой токен устройства |
| `dbGetPushToken` | взять чужой пуш-токен (а он сам по себе никого не спрашивает) |
| `dbSaveNotification` | положить в чужой колокольчик любой текст |
| `tgNotifyNewApplication` | отправить карточку отклика любому работодателю |
| `dbReleasePushToken` | лишить человека уведомлений, зная его токен |
| `dbMarkNotifRead`, `dbDeleteNotif` | пометить или стереть чужое уведомление |
| `dbDeleteMatch` | стереть чужой отклик по id, а с ним итог смены и основание рейтинга |

Плюс две операции переведены в админские: `dbMergeDuplicateChats` (разовая
уборка по ВСЕМ чатам сервиса, приложение её не зовёт) и `extPartnerCostSave`
(запись расходов, зовёт только дашборд и уже с админским токеном).

**Что ещё нашлось по дороге, но не дыра. ЗАКРЫТО 14.09.** `sendWebPushTo` брал
подписку получателя через `dbGetWebPushSubscription`, а та в `$selfArgFns` —
чужую подписку сервер отдавать отказывался, и веб-пуш второй стороне не уходил
НИКОГДА. Клиентскую отправку снесла задача 20, но дыра осталась с другой
стороны: серверный `web_push_to` звали ровно из одного места — рассылки о новой
вакансии. То есть человек из браузера узнавал о чужой смене и не узнавал о
своём мэтче, сообщении и итоге собственной смены. Теперь `notify_user` доходит
и до браузера — запасным путём, только когда нет ни телеграма, ни приложения.
Проверка — `tests/web_push_test.php`.

**Вторая половина ревизии — СДЕЛАНА (14.09).** Разобраны все одиннадцать
операций, отдающих чужие данные на чтение. Семь закрыты проверками
(`dbGetLikes`, `dbGetVacancyStatsMap`, `dbGetPermVacancyViewers`,
`dbGetPermVacancyViewsMap`, `dbSignMedia`, `dbUploadChatMedia`,
`dbFileComplaint` — в `read_authz_test.php` и `stats_scope_test.php`).
По остальным четырём решения приняты и записаны:

- `dbGetRatingsForUser` — **была настоящая дыра, закрыта.** Отдавала `'*'` по
  любому id, то есть `from_user_id`. Экран чужого профиля рисует отзывы
  анонимно, а один запрос снимал анонимность: видишь «⭐2, Работник, 03.09» —
  и узнаёшь автора. `like_id` и `vacancy_id` выдавали его не хуже. Теперь
  опознавательные колонки уходят только тому, кто спрашивает про себя;
  `tests/ratings_privacy_test.php`.
- `dbGetUserById` — не тайна: отдаёт только `USER_PUBLIC_COLS`, и это ровно те
  поля, которые собеседник обязан видеть по делу.
- `dbUserStats` — не тайна: считает отзывчивость и текстов сообщений не
  отдаёт (`chat_id,sender_id,created_at`). На этих же числах стоит публичная
  плашка «как отвечает этот работодатель».
- `dbAutoClosePastVacancies` — оставлена на месте осознанно, разбор в коммите
  `e0ae2c8`: перенос на расписание GitHub сделал бы хуже.

**Задача 20: перенести тексты уведомлений на сервер.** Право писать закрыто, но
текст по-прежнему приходит с клиента: тому, с кем есть переписка, можно послать
через нашего бота что угодно. Сервер уже умеет складывать такие тексты сам
(`jt_group_html`, `jt_perm_app_announce`, `jt_shift_reject_announce`) — надо
довести до конца и для остальных.

**Вычитка работы Codex (13.09).** Девять коммитов прочитаны, все проверки
прогнаны с честными кодами возврата. Кода к исправлению не нашлось — нашлись две
слепые проверки, обе моего любимого вида «утверждение проходит на сломанном
коде», и обе исправлены:

- `seo_infra_test.py` требовал, чтобы в файле страницы вакансии встречалось
  `http_response_code(503)`. Ответ 503 там вынесен в функцию `vp_503()`, поэтому
  строка оставалась на месте, даже когда функцию никто не зовёт: страница с 503
  есть, а при сбое базы до неё не доходит. Теперь проверяется, что 503 достижим
  ИЗ обработчика `catch (Throwable)`. Ловит обе поломки — и вынесенную функцию,
  и выпотрошенный catch в моих собственных файлах.
- `career_infra_test.py` стерёг только ЧТЕНИЕ: что `extSourcesList` отдаёт
  `career_pages` лишь карьерному источнику. Сторожа ЗАПИСИ — того самого, ради
  которого задача 18 и делалась, — не стерёг никто: его можно было удалить, и
  проверка оставалась зелёной. Добавлены две: на сторожа записи и на запасной
  ход, которым вид источника берётся из прежней строки (панель при переключении
  `enabled` не пересылает `connector_kind`).

Что проверено и оказалось верным: `CURLOPT_RESOLVE` закрепляет проверенный
адрес и закрывает DNS rebinding по-настоящему; `jt_new_user_id_is_valid`
пропускает всё, что делает клиентский `uid()`, а `tgAuth` пользователей не
заводит, так что вход через телеграм правка не задевает; вилка оплаты
действительно разделена — поле `per` заполняется в обеих ветках `lp_collect`;
`extSourceSave` и `extSourcesList` — админские операции.

**Мелочь на будущее, не заводил задачей.** `bootstrap.sh` зовёт `migrate.sh`
дважды за заход (рано — чтобы не выложить код раньше миграций, и в конце —
для первой установки), а сам себя не сериализует. Миграции идемпотентны, так
что вреда нет, но при заходах длиннее минуты два bootstrap могут пересечься.
Это было и раньше.

**Codex.** Что делать дальше — `docs/план-для-codex.md`. Первым там не задача, а
предупреждение: коммит `374f147` вырезал из `db.php` 5859 строк, `6100c60` их
вернул. Файл цел, всё зелёное — пересчитано. После любой правки `db.php`:
`wc -l`, `php -l`, полный прогон.

**Правило сопровождения:** взял задачу — отметь её здесь; закончил — тоже.
Очередь опустела — не выдумывай мелочь ради занятости: разберись, что мешает
продукту, и внеси сюда обоснованную задачу. Пустая очередь честнее выдуманной.

**Как выкладываем.** PR №73 слит 14.09.2026. Новые изменения идут отдельными PR
от актуального `main`; после проверки их сливает владелец.

## Сделано

| № | Задача | Проверено |
|---|---|---|
| 1 | `-L` вызовам GitHub API в `infra/report.sh` | да, плюс имя репозитория приведено к нынешнему |
| 2 | Ежедневный продуктовый отчёт: `cronDailyReport`, тема форума, пороги тревоги | да |
| 3 | Проверка доступности: `--compressed` и честное расписание `*/30` | да |
| 4 | Внятная ошибка при восстановлении вебхука: три вида отказа, повторы | да, после доделки 5.1 |
| 5 | SEO, первая половина: `robots.txt`, `sitemap.xml`, честные 404 | да, проверено по живому сайту |

| 5.1 | Общая обёртка повторов на оба вызова к Telegram | да, обёртка одна на всё |
| 6 | Метрика в ежедневный отчёт | да |
| 6.1 | Считать поиск по источнику визита, а не по «последнему значимому» | да, обе точки `lastSign` убраны, проверено в `main` |

| 7 | SEO-страницы, часть 1: серверная страница своей вакансии, разметка JobPosting, генерируемая карта сайта | слито в `main` |
| 12 | Смену закрывает её сторона: проверка владельца в `dbSetShiftOutcome`, `dbConfirmShift`, `dbCancelShift` | слито в `main` |
| 17 | Проверка формата `id` пользователя | новые регистрации ограничены безопасным форматом, старые `id` продолжают работать; слито в `main` |
| 18 | Панель источников не должна отправлять `career_pages` для hh.ru и остальных коннекторов | поле отдаётся только карьерному источнику; слито в `main` |
| 19 | Не выкладывать код раньше миграций базы | обе серверные выкладки оставляют прежний релиз при ошибке миграции; слито в `main` |
| 20 | Главная воронка: отклик → выход → повторная смена в суточном отчёте | слито в `main` |
| 21 | Разобрать «2000+ вакансий — ноль откликов» по показам и шагам воронки | слито в `main` |
| 11 | Пункт 6 плана: реферальная программа | закрыта целиком 15.09, см. ниже |
| 40 | Удалить все интеграции с внешними источниками вакансий | слито в `main`, PR №133 |
| 41 | Три вкладки вместо пяти: Вакансии / Отклики / Профиль | да, `app/(tabs)/_layout.tsx`; `chats` осталась маршрутом без вкладки |
| 42 | Убрать ленту смен у работника | да, `WorkerFeed` и фильтры смен удалены из `feed.tsx` (4215 → 2600 строк) |
| 43 | Работодатель не может создать смену | да, экран `create-vacancy.tsx` удалён, `$shiftWriteFns` в `db.php` отвечает 410 |
| 44 | Редизайн ленты вакансий по макету | да, шапка с поиском и счётчиком, карточка перерисована, размеры через `rs()`/`rf()` |
| 45 | Редизайн экрана «Отклики» по макету | да, экран работника переведён с `likes` на `permApplications` |
| 47 | Убрать полосу над колодой, вернуть возврат вакансии | да, возврат в шапке ленты; карта метро и признак фильтров переехали, не потерялись |
| 48 | «Отклики» по второму макету, избранное туда же | да, шапка с закладкой/конвертом/лупой, группировка по дням, «Ждут вашего ответа», шторка фильтров с разделом «Избранное» |
| 49 | Карточка вакансии листается вниз | да, `flexGrow` вместо `flex` + тот же ScrollView, что держал «потяните вниз»; подсказка «Листайте вниз» на растворении текста |
| 50 | Дневной запас свайпов, 40 в сутки, не копится | да, `services/energy.ts` + `hooks/useEnergy.ts`, локально; плашка «На сегодня всё» вместо свайпа при нуле |
| 51 | Избранное отдельным экраном по макету | да, `app/saved.tsx`, группировка по дню сохранения; ради даты заведена `dbGetPermSavedDetailed` |
| 52 | Экран сообщений по макету | да, шапка с «назад», поиск, чипы «Все/Непрочитанные», строки карточкой |
| 53 | Вся вакансия в карточке, переход на страницу убран | да, «Расположение» и «Описание вакансии» переехали в карточку; тело карточки перестало быть кнопкой — она и уводила при прокрутке |

## В работе

| № | Задача | Состояние |
|---|---|---|
| 10 | Пункт 5 плана: диверсификация источников вакансий (уроки Jobr) | код слит; источник заведён выключенным до проверки страниц |

**Что осталось по задаче 10 — руками, из сессии я этого не сделаю.**

1. ~~Убедиться, что `infra/migrate.sh` применил `063_career_pages_source.sql`.~~
   **Проверено 15.09:** прод отдаёт `security-status.json` с
   `latest_migration: 070_atomic_message_insert.sql` — значит 063 применена
   давно. Проверять руками больше нечего, эндпоинт отвечает на это сам.
2. Вписать адреса карьерных страниц в `connector_config.pages` источника
   `career` в панели. Пока список пуст, источник вернёт «нет годных адресов» —
   это ожидаемо, а не поломка.
3. Посмотреть глазами, что именно отдают страницы, и только потом включить
   источник. Так же заводили hh.ru миграцией `058`.

**Решение, которое за владельцем.** Тянем ли мы карьерные страницы сами или
только тех работодателей, кто сам дал ссылку. Код одинаковый, разница в том,
чьё содержимое мы перепечатываем.

**Задача 11 закрыта целиком (15.09).** Оба условия, без которых включать было
нельзя, проверены — и ни одно не потребовало правки кода.

1. ~~Проверить миграции `064_referral_programme.sql` и
   `065_referral_vouching.sql`.~~ Прод отдаёт `security-status.json` с
   `latest_migration: 070_atomic_message_insert.sql` и `rls_guard: true`. Обе
   миграции применены, и выяснилось это не по журналу выкладки, а по самому
   серверу — эндпоинт Codex завёл 15.09 ровно для таких вопросов. Заглядывать
   в журналы GitHub за этим больше не нужно.
2. ~~Описать программу в `dataPolicy`.~~ Уже описана редакцией 2026-09-14:
   состав данных (код приглашения, связь пригласившего с приглашённым, дата,
   факт и результат первой смены), цель обработки (учёт добровольных
   приглашений и поручительство после первой отработанной смены) и срок
   хранения связи с её удалением при удалении любого из аккаунтов.
   `tests/referral_data_policy_test.py` зелёный.

Программу можно включать: код на проде, миграции применены, правовое основание
описано. Ниже оставлены две записи задачи — они объясняют, ПОЧЕМУ программа
устроена так, и в этом их ценность.

3. ~~Назначить вознаграждение~~ — **отпало.** Решение владельца: платить пока
   нечем, и обещание без суммы читается как обман. Вместо денег —
   поручительство: приведённые, вышедшие на смену, видны работодателю на
   карточке поручителя. `jm_settings.referral_reward_rub` больше не нужен и не
   читается, денежные колонки из журнала убраны миграцией `065`.
4. **Личные данные (152-ФЗ) — почему их вообще пришлось описывать.** Программа
   связывает двух людей: кто кого привёл, и показывает счётчик приведённых в
   карточке. Обычная кнопка «поделиться» остаётся обезличенной, приглашение —
   отдельное осознанное действие, но это всё равно новая цель обработки.
   Денег в программе нет, поэтому ИНН и налоговая сторона не понадобились, и
   текст вышел короткий.

**Найдено по дороге и закрыто задачей 12.** `dbSetShiftOutcome`,
`dbConfirmShift` и `dbCancelShift` требовали входа, но не проверяли, чья смена.
Любой вошедший мог закрыть чужую смену любым итогом и испортить человеку
рейтинг невыходом.

**Найдено по дороге и закрыто сразу: согласие на обработку данных терялось.**
Разбирая жалобу на молчащие объявления, я нашёл там не одну ошибку, а породу:
клиент делает вызов «выстрелил и забыл», сервер о событии не знает и доделать
за него не может. Запись согласия была устроена ровно так же —
`void dbRecordConsent(...)`, без ожидания и без повтора. Ставка здесь выше, чем
у объявления: согласие — не аналитика, а доказательство, его предъявляют, когда
спрашивают, на каком основании мы обрабатываем данные человека. Регистрация
идёт с телефона на плохой связи, и потеря была тихой.

Теперь согласие пишется ТЕМ ЖЕ запросом, что создаёт человека
(`jt_consent_attach`). Сбой записи регистрацию не рушит — отказать человеку
из-за этого было бы хуже, — но и молчания больше нет: суточный отчёт считает
зарегистрировавшихся без согласия и поднимает тревогу.

## Очередь

**Единый шаг отступов на карточке вакансии — не нужен. Решение владельца
23.09.** Просьба «расположить по сетке Фибоначчи» была, я её сделал, но пока
правка лежала на ревью, владелец переделал карточку сам (коммит `2ef7620`) с
другим набором отступов — 20, 14, 18, 16. На прямой вопрос, приводить ли новый
дизайн к единому шагу, ответ: нет. `tests/card_grid_test.php` удалён вместе с
правкой: сторож, описывающий отменённое решение, хуже отсутствия сторожа.


**Урок 23.09: снималка экранов молча снимала не приложение.**
`scripts/shoot-screens.mjs` рисует настоящие экраны с подменёнными ответами
сервера — ею проверяют раскладку, потому что typecheck к вёрстке слеп. Она
входила в систему, записывая профиль в `localStorage`. После усиления сессий
этого мало: `AppContext` на старте зовёт `dbRestoreSession` → `dbSession`, а
заглушка про такую функцию не знала и возвращала пустоту, то есть сессия
считалась недействительной. В результате ВСЕ 35 снимков выходили байт в байт
одинаковыми — экраном выбора роли. Ни одной ошибки при этом не печаталось:
скрипт отрабатывал успешно.

Поймано только потому, что снимки сравнили между собой, а не посмотрели один.
Заглушка научена отвечать на `dbSession` и класть токен `jm_session_token`
(на вебе он в обычном `localStorage`, SecureStore включается только на
телефоне). Снимки в репозитории уцелели — они от 13.09, до поломки, и прогон
их едва не затёр.

Отсюда правило: **после прогона снималки сверять снимки между собой**, а не
глазами по одному. Одинаковый размер у всех файлов — признак, что вход отвалился.

**Сами макеты в `docs/screens/` устарели.** Они от 13.09 — то есть до редизайна
ленты, оранжевого фона, скруглений и этой правки сетки. Перерисовать их теперь
есть чем (снималка починена), но класть 35 картинок и 35 векторов в PR про
карточку значит утопить ревью. Отдельным срезом, и лучше после того, как
владелец посмотрит карточку: перерисовывать дважды незачем.


**Долг, записанный 17.09.** Свайп-удаление строки в `app/(tabs)/chats.tsx`
сделано на `PanResponder` — против правила фронтенда «жесты только через
gesture-handler». Жест рабочий, поэтому при перекраске экрана я его не трогал:
переписывать работающее заодно с косметикой — лишний риск. Но правило он
нарушает, и когда до этого экрана дойдут руки по делу, перевести на
gesture-handler.

**Урок 17.09: жест проверяется касанием, а не колесом мыши.** Прокрутку карточки
в ленте я объявил рабочей дважды, и дважды на телефоне она не работала. Оба раза
проверка шла снимками экрана в браузере, а колесо мыши прокручивает и то, что
пальцем не прокручивается: `failOffsetY` отменял жест, но gesture-handler держал
на области `touch-action: none`, и браузер отказывался листать. Плюс список был
обычный, из `react-native`, — то есть вне разбора жестов, где отпускать касание
некому. Нашлось только настоящим `touchmove` через CDP. Правило записано в скилл
`senior-frontend`, условия — в `docs/MAP.md`, охрана —
`tests/card_scroll_test.php`.

Второе, что стоит помнить из этого: **прокрутку пальцем на настоящем телефоне я
проверить не могу.** Браузер и телефон здесь идут разными путями, и веб-половина
(`touch-action`) на телефоне вообще не при делах. Нативная половина — список из
gesture-handler — проверяема только на устройстве.


| № | Задача | Что мешает начать |
|---|---|---|
| 8 | SEO-страницы, часть 2: сводные страницы «профессия + город» и по станции | сделано Claude, ждёт слияния |
| 14 | Переснять `docs/screens`: снимки показывают прежнее устройство приложения | — |
| 15 | Мост Телеграм ↔ Codex: сверка повторов читает только первые 100 комментариев, а результаты не возвращаются в тему | контур выведен из работы, чинить только если вернём |

**Задача 16 закрыта, и по дороге закрылась дыра.** Формат объявления в группу
жил в двух местах: приложение собирало разметку и слало её готовой, сервер
собирал свою для догоняющего задания. Теперь текст собирает только сервер
(`jt_group_html`) из строки вакансии, а клиентский довод оставлен пустым, чтобы
не сдвинуть номера остальных.

Разбираясь с этим, я увидел, ПОЧЕМУ разметка вообще приходила с клиента — и что
из этого следовало. Вход в операцию требовался, а чья вакансия, не проверялось.
То есть любой зарегистрированный человек мог отправить в открытую группу
«ПОДРАБОТКИ» и пушем каждому работнику произвольный текст со своей ссылкой. Для
сервиса, где люди ищут работу, это готовая площадка для обмана. Теперь сервер
берёт строку вакансии и сверяет `employer_id` с сессией, и делает это ДО отметки
о рассылке: иначе посторонний пометил бы чужую вакансию разосланной, и настоящее
объявление не ушло бы уже никогда.

Чем за это платим. Вызов без номера вакансии подтвердить нечем, и он теперь
отклоняется. Пост в группу от этого не теряется — раз в час его догоняет
`jt_announce_missed`. А вот личные сообщения по такой вакансии не уйдут. Это
задевает только сборки приложения старше того дня, когда номер вакансии начали
передавать; все нынешние экраны публикации его передают.

**Сплошной разбор породы «выстрелил и забыл»: отклики на постоянные вакансии.**
Две находки подряд (объявление в группу и согласие) были одной породы, поэтому
я прошёл по всем вызовам с клиента, которые сервер не может доделать. В решении
по отклику эта порода дала сразу три беды.

Уведомление соискателю слал телефон директора отдельным вызовом с пустым
`.catch()` — обрыв связи, и человек о решении не узнал. Системную строку в чат
(«кандидат одобрен», «вы не подошли») тот же телефон писал от имени `system`, а
писать от имени `system` приложению ЗАПРЕЩЕНО нашей же проверкой «Invalid
sender»: сервер отвечал 403 ВСЕГДА, и этот отказ гасился тем же пустым
`.catch()`. Директор видел строку у себя — её дорисовывали на месте, — а
соискатель не видел ничего и никогда. И третье: чей это отклик, не проверялось
вовсе, то есть любой вошедший мог одобрить или отклонить чужого кандидата.

Всё это теперь делает сервер в том же запросе, что меняет статус.

**Та же порода на сменах — закрыта следом.** Отказ по сменной вакансии был
устроен так же и получал тот же 403, а счётчик непрочитанного при этом рос:
значок был, а за ним пусто. Теперь строку пишет сервер. В `dbUpsertLike`
нашлось и остальное: проверок не было ни одной (любой вошедший мог одобрить или
отклонить чужого кандидата на чужую смену и отозвать чужой отклик), владелец
смены брался с клиента доводом, и с клиента принимались ВСЕ поля отклика —
включая мэтч, отметку о выходе на смену и об оценках, то есть их можно было
себе подписать.

**Решение принято владельцем: отказ включён везде.** С «Кандидатов» и «Мэтчей»
он раньше молчал совсем. Теперь отказ объявляет сервер со всех трёх экранов, и
объявляет он в первую очередь уведомлением: чат заводится только на мэтче, так
что у отклонённого с «Кандидатов» переписки обычно нет вовсе — одной строкой до
него не достучаться. Строка в переписку добавляется, если переписка есть.

**Цена, которую стоит знать.** По сменам это НОВЫЙ повод написать человеку: и
пуш, и сообщение в телеграм-бота. Слепых рассылок мы боимся не зря — в файле
записано, что 27% подключивших бота заблокировали его после веерных касаний. Это
касание не слепое, оно про собственное действие человека, но если решите, что
отказ должен приходить тихо (только колокольчик в приложении, без пуша), —
скажите, снимается одним доводом.

**Про текст — моя ошибка, поправляю.** Я написал, что «Чат закрыт» врёт. Это
неверно: экран действительно блокирует ввод по отклонённому отклику
(`isChatBlocked` в `app/chat-room.tsx`), и фраза объясняет серый ввод. Плоха она
была другим — не говорила, ПО КАКОЙ вакансии отказ, хотя в одной переписке речь
идёт о нескольких сменах подряд. Вакансию теперь называем, фразу про переписку
оставили.

**Откуда взялась задача 17.** В суточном отчёте счётчик согласий собирает
собирал фильтр `in.(…)` из идентификаторов людей — обычной склейкой через
запятую. Пошёл проверять, откуда эти идентификаторы берутся, и выяснил, что
формата у них нет: `dbUpsertUser` принимает `id` от клиента и отвергает только
пустую строку. Настоящее приложение делает `uid()` — время плюс четыре знака,
всегда `[a-z0-9]`, — но прокси открыт, и прислать туда можно что угодно.
Запятая внутри такого `id` разорвала бы список на два значения, а скобка
сломала бы запрос целиком: `sb_select_all` в `db.php` бросает исключение на
ответ 4xx, и суточный отчёт не пришёл бы вовсе. Свой счётчик я починил —
значения теперь берутся в кавычки через `sb_in_list()`. Сама дыра закрыта без
риска для старых строк: безопасный формат проверяется только при создании
нового пользователя. Существующий пользователь с прежним `id` по-прежнему
может войти и сохранить профиль. Загрузку аватара это не задевает: имя файла
сверяется отдельным правилом на сервере.

**Задача 13 закрыта как надуманная — и на её месте нашлась настоящая.**
Я записал, что заголовок «в Москве» врёт, потому что увидел вакансию с адресом
в городском округе Красногорск. Присмотревшись: станция метро у неё
«Пятницкое шоссе» — московская, а страницы сгруппированы именно по станциям
московского метро. Склад за МКАД у московской станции — нормальный результат
для человека, ищущего работу в Москве, а не обман. Заголовок оставлен.

Зато на том же месте обнаружилось, что карточка могла не показывать место
ВООБЩЕ: выводилась только станция метро, а у партнёрских вакансий она
заполнена далеко не всегда. Свой адрес при этом забирался из базы и
выбрасывался, партнёрский не запрашивался вовсе. Исправлено: станция, а если
её нет — адрес.

## План развития: что закрыто

Порядок из `План_развития_JobToo.pdf`, раздел 7:

| Пункт | Состояние |
|---|---|
| 1. Свести ежедневный отчёт | сделано (задача 2) |
| 2. Пороги и тревоги | сделано (задача 2) |
| 3. Подключить Метрику | сделано (задачи 6 и 6.1) |
| 4. Страницы вакансий для поисковиков | слито и живёт на проде (задачи 7 и 8); задача 9 — после замера отдачи |
| 5. Диверсификация источников | слито, миграция применена; адреса карьерных страниц и включение источника — руками |
| 6. Реферальная программа | **закрыт 15.09**: слито, миграции применены, политика данных описана. Денег в программе нет — решение владельца от 13.09, вместо выплаты поручительство |

**Проверено на живом сайте 15.09** (пункт 4, чтобы «сделано» не держалось на
одном лишь слиянии): карта сайта отдаёт 88 адресов — 54 страницы вакансий и 29
сводных, включая станционные вроде `/rabota/brigadir/medvedkovo`. Сводные
страницы отвечают 200 по всем четырём видам работ; на комплектовщике 668
вакансий, то есть порог `LP_MIN` пройден с запасом, а не впритык.

**Решение по способу для пункта 4 принято:** тонкий серверный слой на PHP рядом с
существующим прокси, а не серверный рендеринг приложения. Приложение не трогаем.
Причина: рендеринг Expo-приложения на сервере — недели работы и перестройка клиента,
тогда как прокси уже ходит в базу и умеет отдавать HTTP.

**Почему часть 1 — на своих вакансиях, а не на партнёрских.** Текст партнёрской
вакансии у нас тот же, что на hh.ru: поисковик покажет первоисточник, а не копию.
Свои вакансии уникальны и ни с кем не конкурируют — на них и отлаживаем механику.
Объём придёт из части 2, где страницы собираются из данных, а не копируются.

Разбор «почему при 2000+ новых вакансий за сутки ноль откликов» теперь опирается
на суточные показы: свои и партнёрские карточки разделены, для партнёрских
результатом считается переход, гостевой путь виден до завершения регистрации.

## Найдено, но пока не разложено на задачи

- **Московский сервер не всегда дотягивается до Telegram.** Новая диагностика
  Codex это показала прямым текстом: `curl_errno 28, Connection timed out after
  20002 milliseconds`. Повторялось дважды — в 20:12 и 21:03 UTC. Повторы лечат
  симптом; причину (исходящая связь сервера с `api.telegram.org`) надо смотреть
  на самом сервере.
- ~~Плашка «как отвечает работодатель» не доходит до свайпа.~~ **СДЕЛАНО
  14.09**, решение владелец делегировал. `ReplyBadge` теперь и на карточке
  свайпа, до разделителя: решение принимается свайпом, а до «Читать полностью»
  доходят единицы. Карта отзывчивости уже грузилась одним запросом на всех
  именно ради ленты (`contexts/AppContext.tsx`) — проводка была проложена,
  оставался последний шаг. Плашка молчит при менее чем двух переписках и на
  партнёрских карточках. **После выкладки посмотреть живьём**: отсюда снимок
  экрана не сделать, лента за входом и без боевых данных пустая.
- ~~`supabase-js` лежит в бандле, а ходить через него некуда.~~ **ЗАПИСЬ БЫЛА
  НЕВЕРНОЙ, снята 14.09.** Ветки с прямым обращением к базе действительно
  мёртвые (`IS_NATIVE = true`), но у клиента осталась вторая работа: **живые
  обновления через realtime**. Три канала, и на обоих концах: `rt_broadcast` в
  `db.php` шлёт, `AppContext`, `chat-room` и профили слушают. На broadcast
  переходили осознанно, когда закрыли базу и замолчал `postgres_changes`.
  Убрать `supabase-js` — убить живой чат. `ws` при этом закрыт обновлением
  внутри мажора (8.20.0 → 8.21.3), без смены SDK. Разбор —
  `docs/аудит-зависимостей.md`.
- ~~Веб-пуш ходит по всем подпискам таблицы.~~ **СДЕЛАНО 14.09, и по дороге
  сломано и починено.** Сужая выборку, я обернул `sb_in_list($chunk)` в
  `'in.(' . … . ')'`, а функция сама возвращает готовый фильтр. Вышло
  `in.(in.("a","b"))` — не совпадает ни с кем, и веб-пуш умер ЦЕЛИКОМ, включая
  ту рассылку, что работала до правки. Поломка тихая: вся отправка под `catch`.
  Нашло собственное ревью следующего дня, починено в `05d4d13`. Проверка в
  `tests/web_push_test.php` переписана: пиналась к точному тексту — со
  скобками, — и потому зеленела на сломанном коде.
- ~~Признак обрыва связи на экранах откликов и переписок.~~ **СДЕЛАНО 14.09.**
  Экраны читали общий `backendOffline`, который поднимают только загрузки
  вакансий: плашка «нет связи» появлялась, когда список экрана пришёл, и
  молчала, когда не пришёл. Теперь признак на каждый список отдельно
  (`offline.likes`, `offline.chats`), и в переписках он меряется ДО поиска —
  ненайденный запрос при живой сети больше не объявляется обрывом.
- ~~Ошибочную отметку о смене не исправить в поручительстве.~~ **СДЕЛАНО
  14.09.** `dbSetShiftOutcome` зовётся повторно и рейтинг пересчитывает, а
  строка поручительства была заморожена: «не вышел» оставалось на карточке
  навсегда. Правка ТОЙ ЖЕ смены теперь доходит до журнала и двигает счётчик в
  обе стороны. Пересчёт счётчика в миграции 065 заодно исправлен: он шёл
  внутренним соединением и пропускал ровно тот случай, ради которого написан.
- **Расписания GitHub не для мониторинга.** Заявленное `*/5` на деле давало запуск
  раз в два-четыре часа. Сейчас стоит `*/30`, но и ему нельзя верить как часам;
  настоящий контроль доступности должен жить вне GitHub Actions.

**Убрано отсюда как закрытое (13.09).** Два пункта — «сайта для поиска не
существует» и «карта сайта почти бесполезна» — описывали состояние до задач 5 и
7. `robots.txt` и `sitemap.xml` отдаёт сервер, несуществующие адреса отвечают
404, страницы вакансий и сводные собираются на сервере с разметкой. Там же
висела строка «задача 7 ждёт решения владельца о способе»: решение принято и
записано выше, работа сделана.
