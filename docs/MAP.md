# Карта репозитория JobToo

Оглавление проекта: где что лежит и куда идти за чем. Нужна, чтобы не перечитывать
одни и те же файлы в каждой сессии — поиск начинается отсюда, а не с обхода репозитория.

**Правило сопровождения:** изменил устройство части проекта — поправь здесь строку
тем же коммитом. Карта, отставшая от кода, хуже её отсутствия: она уводит не туда
с полной уверенностью.

## Что это за проект

Приложение поиска постоянной работы в Москве. Работник листает карточки свайпом,
откликается и общается в чате. Все вакансии свои: работодатели публикуют их сами.

Раздел подработки (смены) закрыт 17.09.2026 решением владельца. Прежние смены
живут в базе — старые мэтчи, оценки и переписки по ним работают, — но новых не
создать: экран публикации удалён, а `dbUpsertVacancy` отвечает 410. Тому, что
осталось в коде про смены, место только в обслуживании этой истории.

Клиент — React Native на Expo SDK 53, маршрутизация expo-router по файлам.
Работает как приложение, как сайт и как мини-приложение в телеграме.
Сервер — PHP-прокси перед базой. Панель управления — отдельное приложение на Next.js.

## Верхний уровень

| Каталог | Что внутри |
|---|---|
| `app/` | Экраны приложения. Имя файла = адрес (expo-router). `(tabs)/` — три вкладки: Вакансии, Отклики, Профиль |
| `components/` | Общие компоненты. `ui/` — мелкие, `feature/` — крупные с логикой |
| `services/` | Работа с данными и внешним миром. Точка входа к серверу — `db.ts` |
| `contexts/` | Общее состояние. Главный — `AppContext.tsx` |
| `hooks/` | Хуки. `useApp`, `useSwipeDeck` (свайп карточек) |
| `constants/` | Типы, тема, юридические тексты, метро, справочники |
| `php-proxy/` | Серверная часть: обработчик запросов, страницы для поисковиков, бот |
| `supabase/migrations/` | Миграции базы, 75 штук, применяются по порядку номеров |
| `dashboard/` | Панель управления на Next.js, 29 разделов аналитики |
| `infra/` | Развёртывание: `bootstrap.sh` (сервер тянет обновления), nginx, docker |
| `docs/` | Документация, включая эту карту |
| `.claude/skills/` | Библиотека скиллов, 872 |
| `.claude/agents/` | Команда моделей `opus-team` (saint4ai/claude-setups@38ead88, не обновлять): `coder` — Sonnet, `checker` — Haiku, `architect` — Opus; модель по умолчанию `opusplan` в `.claude/settings.json` |
| `tests/` | Тесты на node:test, запуск `npm test` |

## Куда идти за чем

### Лента и свайп
- `app/(tabs)/feed.tsx` — **2300 строк, целиком не читать.** Колода работника —
  `WorkerPermMode`, экран работодателя — `EmployerHome`. Ищи `grep`-ом по имени.
  Шапка ленты (марка, поиск, счётчик, возврат) — `FeedSearchHeader`, объявлена
  на уровне модуля: вложенный компонент пересоздавался бы на каждый ввод и терял
  фокус.
- **Лента — всегда колода.** Вкладок «Отклики»/«Избранное» и списочного режима
  в ней больше нет: отклики и избранное живут на своём экране. Осталась одна
  выборка — открытые вакансии, на которые человек ещё не откликался.
- Возврат последней пролистанной — `swUndo`, кнопка в шапке и только когда
  `swHistory` не пуст. Раньше он отдавался наружу пропом `onUndoChange` и не
  доходил ни до какой кнопки.
- **Фон ленты тёплый** — `Colors.bgWarm` (`#FFE9D9`), отдельный токен. Подменять
  им `Colors.bg` нельзя: тем же токеном покрашена карточка, и колода слилась бы
  с фоном. Заодно поиск в шапке стал белым (серая таблетка на тёплом выглядит
  грязной), и счётчик запаса — тоже: на прежнем `#FFF4EC` его держал
  `primaryBorder`, а на потемневшей подложке тот стал теряться. Дальше темнить
  фон нельзя по той же причине. У работодателя фон прежний, белый.
- Скругление карточки — свой токен `Radius.card` (28), больше общего `xl` (20):
  карточка занимает почти весь экран, и на такой площади 20 читается как прямой
  угол. Тем же радиусом скруглены «призраки» колоды и подсказка «Листайте вниз»,
  иначе её углы торчали бы из-под карточки.
- Фильтры и карта метро — в шторке `PermFilterSheet` (шестерёнка в нижнем ряду).
  Шестерёнка красится в основной цвет, когда фильтры включены: иначе короткая
  колода читается как «вакансий нет».
- `hooks/useSwipeDeck.ts` — физика свайпа, общая для обеих колод.
  Жест на gesture-handler, значения на Reanimated, всё на потоке интерфейса.
- **Карточка листается вниз.** Длинная вакансия не обрезается: карточка растёт
  по содержимому (`cardAnimated` — `flexGrow`, не `flex`), а прокручивает её тот
  же список, что держит «потяните вниз».
  Это отменяет прежнее правило «внутри карточки прокрутки нет». Правило было
  верным для PanResponder, который не умел отдавать уже взятый жест; у
  gesture-handler есть `failOffsetY([-20, 20])` — палец, ушедший вниз на
  двадцать пикселей, отменяет свайп и отдаёт касание списку.
  Одного `failOffsetY` мало, и это стоило двух заходов:
  1. Список берётся из `react-native-gesture-handler` (`GHScrollView`,
     `GHRefreshControl`), а не из `react-native`. Обычный живёт вне разбора
     жестов: Pan забирает касание первым, отпустить его некому.
  2. На вебе gesture-handler ставит области жеста `touch-action: none`, и
     браузер перестаёт прокручивать её пальцем вообще — даже после отмены
     жеста. Поэтому `pan.config.touchAction = 'pan-y'` в `useSwipeDeck`
     (настройщика в цепочке нет, свойство пишется в `config` напрямую).
  Колесо мыши прокручивает и то, что пальцем не прокручивается, — поэтому
  проверять только снимками экрана бесполезно, нужен настоящий `touchmove`.
  Охрана — `tests/card_scroll_test.php` (проверен мутациями) и
  `tests/reply_badge_test.php`.
- Пока ниже есть текст, у нижнего края карточки он растворяется в её цвете и
  поверх стоит подсказка «Листайте вниз» (`pS.scrollHintWrap`). Без неё ровный
  край не отличить от конца текста.
- **Карточка никуда не ведёт.** Вся вакансия внутри неё: чипы, «Расположение»
  (линия метро, адрес, «Смотреть на карте») и «Описание вакансии» целиком.
  Тело карточки — обычный `View`, не `Pressable`: кнопка на всей площади
  срабатывала на отпускании после прокрутки и открывала страницу вакансии.
  Нажатия ловят ровно две кнопки — закладка и «поделиться». Охрана —
  `tests/reply_badge_test.php` (проверена мутациями).
- `app/perm-vacancy-detail.tsx` при этом НЕ удалён и не должен быть: это цель
  ссылок из телеграма (`app/_layout.tsx`), публичного адреса
  `jobtoo.ru/perm-vacancy-detail?id=…` (`php-proxy/api.php`) и строк в
  «Откликах» и «Избранном». Из ленты на неё переходов больше нет.
- Контакты работодателя в карточку НЕ переехали и остались на странице
  вакансии: показывать их всем, кто листает ленту, — это раздать телефоны
  работодателей любому, кто открыл приложение.

### Дневной запас свайпов
- Каждый свайп — влево или вправо, жестом или кнопкой — стоит единицу.
  `DAILY_ENERGY` = 40 в сутки, запас **не копится**: вчерашний остаток сгорает.
  Мера временная, до монетизации: ограничивает не деньгами, а вниманием.
- Чистая логика — `services/energy.ts`, хранение — `hooks/useEnergy.ts`
  (AsyncStorage, ключ `jt_energy_v1`). Разделено ради проверки: тест
  `tests/energy.test.ts` запускается в обычном node, куда нативные модули не
  тянутся.
- День считается по МЕСТНОМУ времени (`energyDay`). По UTC запас у московского
  пользователя обновлялся бы в три часа ночи.
- Пересчёт на новый день не только при запуске: приложение живёт в фоне сутками,
  поэтому ещё и на возврате из фона (`AppState`) и на фокусе экрана.
- Возврат вакансии возвращает и списанный свайп, но не выше дневного запаса —
  иначе «свайпнул — вернул» по кругу печатало бы энергию из воздуха.
- Хранение локальное, и цена этого известна: переустановка приложения обнуляет
  счётчик. Переносить на сервер сейчас — заводить миграцию ради того, что
  переделают вместе с монетизацией.
- Проводка до экрана — `tests/swipe_energy_test.php` (проверен мутациями).

### Профиль, резюме и личные данные (`app/(tabs)/profile.tsx`)
- У работника четыре сегмента: резюме, личные данные, файлы резюме и отзывы.
- PDF импортируется через `services/resumeImport.ts`; `lib/resumeParser.ts`
  извлекает ФИО/отчество и возраст без отдельного сохранения точной даты
  рождения, желаемую должность и зарплату, занятость/формат, город, email,
  гражданство/право на работу, опыт,
  образование, проекты, экзамены, языки, навыки, интересы, сертификаты,
  награды и курсы. Скан без текстового слоя не распознаётся.
- Исходный PDF (до 10 МБ и 12 страниц) сохраняется в закрытом Storage-бакете
  `resume-files`; версии и распознанная структура — в `jm_resume_files`.
  Пользователь может хранить несколько файлов и выбирать активный.
- Активная структура дублируется в `jm_users.resume_data`. Email из резюме
  хранится отдельно в `resume_email`; имя файла/дата импорта — в
  `resume_file_name` / `resume_imported_at`. Сырой извлечённый текст
  отдельно не хранится.
- `resume_data` входит в `USER_PUBLIC_COLS`: работодателю в профиле кандидата
  показываются структурированные разделы резюме. Исходный PDF, `resume_email`
  и `personal_data` в публичную проекцию не входят.
- Импорт обновляет обычные поля пользователя (имя, фамилия, возраст, bio,
  workTypes) и приватные `personalDetails`: отчество, контактный email,
  гражданство/право на работу, местоположение, доступность и переезд.
- Вкладка «Личные» хранится в `jm_users.personal_data` и доступна только
  владельцу. Оставлены только данные с прямой пользой для поиска работы:
  предпочтительное имя, email, ссылки, гражданство/право на работу,
  местоположение, доступность, переезд, водительские права и ограничения по
  трудоустройству.
- Миграция 102 удаляет `disabilityStatus`. Миграция 103 удаляет ранее
  сохранённые `birthday`, `emergencyContact`, `gender`, `pronouns`,
  `race`, `sexualOrientation`, `veteranStatus`, `professionalReferences`,
  `militaryService` и `securityClearance`.
- Проверки доступа: `tests/read_authz_test.php`; разбор резюме —
  `tests/resumeParser.test.ts`; соответствие юридической документации —
  `tests/resume_data_policy_test.py`.

### Отклики работника (`app/(tabs)/matches.tsx`)
- `WorkerMatches` — раскладка с макета владельца, понятия наши. Шапка: закладка
  (избранное), конверт с числом непрочитанных (ведёт в переписки), лупа.
  Заголовок — сколько откликов за сегодня. Чипы статусов и та же пятёрка в
  шторке фильтров. Список сгруппирован по дням (`dayKey`/`dayLabel`).
- **«Ждут вашего ответа» — наша замена «Needs you» с макета.** У прототипа там
  анкеты, которые агент не дозаполнил; у нас действие, которого правда ждут от
  человека, ровно одно — ответить работодателю, и берётся оно из непрочитанных
  переписок. Строка с непрочитанным красится и получает «ОТВЕТИТЬ» вместо
  плашки статуса.
- Из раздела VIEWS макета (Starred, Archived, Saved jobs, Jobs you passed on) у
  нас есть данные ровно под один — избранное. Остальные три не хранятся вовсе,
  поэтому не нарисованы: пустой раздел обещал бы то, чего нет.
- **«Ждут вас» — ручной путь отклика Jupiter.** Заявки, которые сервер сам
  отправить не может (капча, SPA, сайт вне списка проверенных), выводятся
  первым блоком внутри раздела Юпитера (`jupiterManualEligible` в
  `services/jupiterFill.ts`, состояния
  `action_required`/`failed`/`retryable_failed`/`ready_to_submit`, кроме
  особых путей Сбера и повторной авторизации). Строка ведёт на
  `app/jupiter-fill.tsx` — встроенный браузер, где анкета заполняется данными
  профиля (`services/jupiterFill.ts` — скрипт заполнения строкой, потому что
  Hermes не отдаёт исходник функции через `toString`), а отправляет и проходит
  капчу человек сам, подтверждая кнопкой «Я отправил отклик»
  (`jupiterMarkManualSubmitted`, состояние `submitted` с причиной
  `MANUAL_WEBVIEW`). Это очередь: после отправки или «Пропустить» экран сам
  открывает следующую анкету (`nextManualApplication`, пропущенные едут в
  параметре `skip`), кнопка «По очереди ›» в заголовке блока начинает с первой.

### Избранное (`app/saved.tsx`)
- Отдельный экран со своей кнопкой «назад», а не раздел внутри «Откликов».
  Входов два: закладка в шапке «Откликов» и строка в шторке фильтров.
- Группировка по дню **сохранения**, не публикации. Ради этого заведена
  `dbGetPermSavedDetailed`: обычная `dbGetPermSaved` отдаёт голые id, и менять
  её форму значило бы переписать все места, где по `permSavedIds` рисуется
  отметка «сохранено». Аргумент 0 в `$selfArgFns` — чужое избранное не прочитать.
- Даты приходят отдельным запросом, и его сбой — не пустой список: список
  показывается без группировки, а про связь говорит отдельная плашка.
- Снятие из избранного ждёт сервера до правки UI (`dbRemovePermSaved`, потом
  `optimisticRemovePermSaved`) — иначе обрыв связи выглядит как удаление.

### Переписки (`app/(tabs)/chats.tsx`)
- Маршрут внутри вкладок, но без своей вкладки (`href: null`): открывается
  конвертом из шапки «Откликов», поэтому у экрана своя кнопка «назад».
- Чипов на макете три и все про категории писем; у нас писем нет, есть
  переписки с работодателями, и единственное деление с данными — прочитано или
  нет. Пустой фильтр «непрочитанные» — это «Всё прочитано», а не обрыв связи.
- Непрочитанное отмечается конвертом в углу аватарки и плашкой «НОВОЕ».
  Прежний счётчик справа отнимал место у текста, а число сообщений в переписке
  с одним работодателем ничего не добавляет к «тебе написали».
- Свайп-удаление строки — на gesture-handler (`Gesture.Pan` + Reanimated).
  Переведено 23.09 с `PanResponder`, и не только ради правила: `PanResponder`,
  захватив касание, не умеет его отдать, и палец, ушедший вниз, оставался у
  строки — список не прокручивался. Вход вбок `activeOffsetX([-10, 10])`,
  отмена вниз `failOffsetY([-12, 12])`, на вебе `touchAction = 'pan-y'` (иначе
  gesture-handler ставит `touch-action: none` и страница не листается пальцем
  вообще). Нажатие в первые 400 мс после перетаскивания пропускается: браузер
  присылает `click` вслед за жестом. Список и потягивание — из gesture-handler,
  обычный `FlatList` лежит вне разбора жестов. Сторож —
  `tests/chats_swipe_test.php`.

### Группировка по дням (`services/dayGroups.ts`)
- Общая для «Откликов» и «Избранного»: `dayKey`, `dayLabel` («СЕГОДНЯ»,
  «ВЧЕРА», «8 СЕНТЯБРЯ»), `dayShort` («8 сен») и `groupByDay`.
- Всё по МЕСТНОМУ времени: с `toISOString()` вечерние события уезжали бы в
  завтрашнюю группу. Порядок списка не трогается — сортировка за вызывающим.
- Проверка — `tests/dayGroups.test.ts`, включая границу месяца.

### Подробности вакансии
Один макет на три вида, собран из общих кусков:
- `components/feature/VacancyDetailHead.tsx` — шапка: знак компании, «40 минут назад», чипы
- `components/feature/VacancyContacts.tsx` — раздел «Контакты», скрыт до входа
- `app/perm-vacancy-detail.tsx` — экран вакансии (есть и как ссылка из телеграма)

### Страницы для поисковиков

Приложение рисует вакансии в браузере, роботу показывать нечего. Рядом с прокси
живёт тонкий серверный слой — приложение он не трогает:

- `php-proxy/vacancy_page.php` — страница одной своей вакансии: настоящий HTML,
  разметка `JobPosting`, канонический адрес. Сбой базы отвечает `503`, а не
  ложным `404`. Подключается тестом как библиотека через константу
  `VACANCY_PAGE_LIB_ONLY`.
- `php-proxy/sitemap.php` — карта сайта, собираемая из базы. Статического
  `public/sitemap.xml` больше нет.
- `php-proxy/vacancy_url.php` — одно правило годности идентификатора для адреса,
  общее для страницы и карты: иначе карта опубликует то, что страница не откроет.
- Маршруты `/v/<id>` и `/s/<id>` в `infra/nginx-site.conf`. Стоят **выше** правила
  о путях с расширением: идентификатор может содержать точку.
- `php-proxy/landing_page.php` — сводные страницы «работа комплектовщиком в Москве»
  и она же по станции. Маршруты `/rabota/<профессия>` и `/rabota/<профессия>/<станция>`.
  Порог `LP_MIN`: меньше пяти вакансий — страницы нет, тонкая страница понижает
  весь сайт. Оплата за смену и за месяц считается раздельно (смены закрыты, но
  прежние в базе остались). Перечень существующих страниц отдаёт `lp_index()`,
  им же пользуется карта сайта.

### Связь с сервером
- `services/db.ts` (~2000 строк) — все вызовы. Обращение по схеме `fn` + `args`.
- `php-proxy/db.php` (**5950 строк, целиком не читать**) — обработчик. Функции ищи
  `grep -n "case 'имяФункции'"`.
- Адрес сервера на вебе — origin текущей страницы, на телефоне — из окружения.

### Внешние источники: партнёрские удалены, карьерные вернулись
Миграция `096_remove_external_sources.sql` снесла всю ветку целиком — и код, и
данные. Потом решение владельца изменилось в одной части: карьерные страницы
работодателей нужны снова, потому что на них подаёт заявки Jupiter.

**Вернулось** (миграция `105_restore_career_sources.sql`): `jm_ext_sources`,
`jm_ext_vacancies`, `jm_ext_ingest_runs`; `php-proxy/career.php`,
`career_feed.php`, `ingest.php`, `safe_url.php`. Таблицы созданы не в том виде,
в каком их застала 096, а в карьерном подмножестве: без песочницы
(`environment`), без двустороннего коннектора (`integration_mode`,
`webhook_secret`) и без публичной политики RLS — доступ только сервисным ключом
через `db.php`, как у всего остального.

**Осталось снесённым и не воскрешается:** hh.ru, SuperJob, Arbihunter,
партнёрский шлюз заявок с биллингом и тарифами (`jm_partner_*`,
`jm_superjob_*`), счётчики переходов `jm_ext_clicks`/`jm_ext_events`. Переходов
больше нет по замыслу: человек не уходит на чужой сайт, заявку подаёт Jupiter.

**Как это устроено, и из одного файла это не видно.** `ingest.php` — общий
приёмник: он ходит по адресу из `jm_ext_sources.url` и ждёт JSON вида
`{items: [...], next_url}`. Для карьерных источников этот адрес указывает на
нашу же точку входа `/api/career.php?source=career_owner`: она берёт чужую
страницу и превращает её в тот самый формат. То есть `ingest.php` не знает, что
имеет дело с карьерным сайтом, и знать не должен — именно поэтому раньше рядом
жили адаптеры hh и SuperJob, а теперь остался один. Разбор страницы —
`career_feed.php`: разметка JobPosting, JSON по карте полей, список обычных
ссылок и встроенное состояние страницы. Проверка адреса у всех одна,
`safe_url.php`: только публичный HTTPS, без служебных сетей и метаданных
облака, без переходов по редиректу и с закреплённым DNS-адресом.

`ingest.php` закрыт админским токеном (`X-Admin-Token`), `career.php` открыт —
он ничего не пишет и ходит только по адресам, заранее записанным в
`connector_config`. Произвольный адрес из запроса туда не попадает.

**Расписание забора.** `.github/workflows/career-ingest.yml` зовёт `ingest.php`
каждые 6 часов (02:30, 08:30, 14:30, 20:30 UTC). Без него таблица
`jm_ext_vacancies` остаётся пустой. Можно запустить руками через
`workflow_dispatch` с параметрами `source` и `force`.

### Приглашения (реферальная программа)
- **Денег в программе нет** — решение владельца. Вознаграждение —
  поручительство: позвал знакомого, тот вышел на первую смену, и это видно
  работодателю на карточке поручителя. Миграция `065`.
- `php-proxy/referral.php` — чистые правила: код приглашения и «что
  записываем». **Только итог первой смены** (`worked` или `no_show`), никогда
  отклик — урок Jobr из плана развития. Невыход записывается тоже: без него
  число выходов набивалось бы рассылкой кода кому попало.
- Связь пишется в `jm_users.invited_by` **сервером** при регистрации по коду из
  второго довода `dbUpsertUser`; клиент это поле не правит.
- Журнал — `jm_referral_rewards` (миграции `064` + `065`; имя историческое,
  денежные колонки `amount_rub`/`paid_at`/`status` убраны). Строки заводит
  `jt_referral_on_outcome` в `db.php`, он же поднимает счётчик
  `jm_users.referral_worked`.
- `referral_worked` — денормализация ради одного экрана: карточку кандидата
  работодатель видит списком. Входит в `USER_PUBLIC_COLS`, показывается в
  `app/candidates.tsx` и `app/user-profile.tsx`. Источник правды — журнал,
  миграция `065` пересчитывает счётчик из него.
- Ссылка приглашения — `startapp=ref_<КОД>`, разбирается в `app/_layout.tsx`,
  переживает путь до регистрации в `services/storage.ts` и применяется в
  `registerUser` (`contexts/AppContext.tsx`).
- Свой код человек узнаёт операцией `dbGetMyReferral` (только про себя). В
  `USER_PUBLIC_COLS` кода НЕТ и быть не должно: эти колонки уходят при запросе
  любого человека.
- Экран — `app/invite.tsx`, вход из `app/(tabs)/profile.tsx` отдельной строкой.
  Показывает код, ссылку и три числа: позвали, вышли на смену, не вышли.
  Разрыв между ними и есть весь смысл. Экран прямо говорит, что денег за
  приглашение нет: обещание без суммы читается как обман.
- Проверки — `tests/referral_test.php` (правила) и
  `tests/referral_chain_test.py` (непрерывность цепочки от ссылки до сервера).

### Магазины приложений
- `docs/магазины-приложений.md` — готовые тексты для RuStore и Google Play:
  название, описания, ключевые слова, план скриншотов. Загружает владелец.
- `docs/план-для-codex.md` — что Codex делает дальше. Правила самого Codex — в
  `AGENTS.md`. Работаем вдвоём в одной ветке, поэтому там про согласование, а не
  про разделение файлов.

### Каталог карьерных сайтов
Два файла, и оба — источник правды, а не справка:

- `scripts/career-sites.tsv` — master-list целей: 163 компании / 170 разделов.
- `scripts/career-endpoints.json` — 83 проверенных адреса, найденных
  разведкой, с картами полей и режимом разбора (`json`, `html_links`,
  `embedded`). Из них 66 рабочих: остальные 17 лежат в
  `scripts/career-runtime-quarantine.json` с причиной и датой.

Заливает их в `connector_config` источника `career_owner` сценарий
`infra/sync-career-catalog.sh`, который зовёт `infra/migrate.sh` после каждого
накатывания миграций.

**Почему endpoints лежат файлом.** Раньше их накапливали миграции `074`–`095`
прямо в базе, и в репозитории списка не было вовсе. Когда `096` снесла таблицу,
единственный экземпляр исчез: восстановить удалось только проигрыванием тех
миграций на пустой базе. Данные, живущие в одной базе и больше нигде, теряются
вместе с ней.

**Чего пока нет.** Разведка (`scripts/career-discover*.mjs`,
`scripts/cofinder-*`, `infra/career-discover-local.sh` и их workflow) не
возвращена — каталог заморожен на найденном, новые компании сами не появятся.
Это отдельный срез. Старые разборы (`docs/источники-вакансий-разбор.md`,
`docs/cofinder-прямые-ссылки.md`, `docs/карьерные-источники-сентябрь.md`)
остаются историческими заметками.

### Безопасность и сессии
- **Смену закрывает её сторона.** `jt_shift_party` в `db.php`: итог смены ставит
  работодатель, старые `dbConfirmShift`/`dbCancelShift` — любая из двух сторон.
  Владелец тут не довод запроса, а поле в строке, поэтому в `$selfArgFns` такие
  операции не попадают и нуждаются в своей проверке. Тест —
  `tests/shift_authz_test.php`.
- `services/db.ts`: `IS_NATIVE = true` **всегда**, Supabase из браузеров в РФ
  недоступен. Ветки с прямым обращением к Supabase — мёртвый код, живой путь
  один: `db.php`. Искать защиту в политиках RLS для них бессмысленно.
- Токен сессии: выдаётся `jt_session_issue`, проверяется `jt_session_claims` в `db.php`.
  Отзыв — по полю `sessions_valid_from` (миграция `062`).
- Заголовки безопасности — `infra/nginx-site.conf`.
- Юридические тексты — `constants/legal.ts`. Правка `dataPolicy` **не** вызывает
  повторного согласия: ключа нет в `LEGAL_KEYS`, это сделано намеренно.
- **Общее согласие на обработку данных** пишется тем же запросом, что создаёт
  человека: `jt_consent_attach` в `db.php`, третий довод `dbUpsertUser`.
  Отдельное пользовательское согласие на трансграничную передачу для push/web-push
  больше не используется. Нативный Expo push централизованно сворачивается до
  нейтрального служебного сигнала без user_id, chat_id, vacancy_id, типа или
  содержания события; web-push также отправляет только нейтральный сигнал.
  Legacy-функции `crossborder:*` сохранены только для исторических записей и
  отдельных каналов вроде Telegram и не должны гейтить Expo/Web Push.
- **`id` пользователя приходит с клиента.** Для новой регистрации
  `dbUpsertUser` принимает только `[a-z0-9]` длиной 8–32 знака. Старые строки
  не блокируются: существующий пользователь проходит по своему сохранённому
  `id`. Списки для PostgREST всё равно строить через `sb_in_list()`.

### Уведомления о сообщениях
- **Собирает сервер**, не телефон: `jt_notify_new_message` в `db.php`, зовётся
  из `dbInsertMessage` (обычное сообщение) и из `dbCreateChat` (первое
  сообщение при заведении чата). Имя отправителя берётся из базы, текст — из
  того, что сервер только что записал.
- Внутри `chat_ensure` уведомления НЕТ намеренно: её зовёт ещё и
  `dbApplyPermVacancy`, а та извещает своим «Новая заявка» — иначе на одно
  событие приходило бы два пуша.
- **Нативный push на экране блокировки всегда нейтральный** —
  «JobToo / У вас новое уведомление». Даже если вызывающий код собрал более
  подробный title/body/data, `push_privacy.php` заменяет их перед Expo.
  Полные детали остаются в российском `jm_notifications` и видны после
  открытия приложения.
- Предпросмотр (`jt_message_preview`) повторяет `services/messagePreview.ts`:
  фото и голосовые лежат в текстовой колонке меткой `[img]`/`[voice]` плюс
  ссылка, показывать её человеку нельзя.
- Проверка — `tests/message_notify_test.php`.

### Счётчики по вакансиям
- `dbGetVacancyStatsMap` и `dbGetPermVacancyViewsMap` отдают числа **только по
  своим** вакансиям: `jt_own_vacancy_ids` берёт их по сессии,
  `jt_rows_for_vacancies` выбирает строки порциями по 100 (идентификаторы уходят
  в адрес запроса). Прежде читались две таблицы целиком, и каждый вошедший
  получал отклики и просмотры всего рынка.
- Карту читает один экран — «мои вакансии», и он и так фильтрует список по себе.
- Проверка — `tests/stats_scope_test.php`.
- **`dbAutoClosePastVacancies` оставлена на клиенте намеренно** — см. очередь,
  задача 22: перенос на расписание GitHub сделал бы её МЕНЕЕ надёжной.

### Публикация смен закрыта на сервере
- `$shiftWriteFns` в `db.php` (`dbUpsertVacancy`, `dbUpsertVacancyBatch`) отвечает
  410 до всякой работы. Запрет серверный, а не только экранный: около 500 человек
  сидят на прежней сборке приложения, где экран публикации смены ещё есть, и
  запретить его правкой клиента нельзя.
- `dbUpdateVacancy` НЕ закрыта намеренно: ею закрываются уже созданные смены —
  и руками, и авто-закрытием прошедших.

### Чужие данные на чтение
- **`dbGetLikes` отдаёт только свои отклики** (`or=(worker_id.eq,employer_id.eq)`).
  Прежде отдавалась вся таблица сервиса, и приложение звало её на каждом
  обновлении списка.
- **`dbGetLikeByVacancyWorker` отвечает обеим сторонам смены** — работнику и
  работодателю этой вакансии. Была в `$selfArgFns`, то есть работодателю
  отказывала, и экран переписки обходил это, выкачивая всё.
- **Файлы переписки.** Id чата стоит в самом имени файла
  (`chat/<id чата>_<время>`), `jt_chat_id_from_media_path` его достаёт,
  `jt_require_chat_party` сверяет. Применено к `dbSignMedia` и
  `dbUploadChatMedia` (там ещё и `x-upsert`, то есть можно было подменить).
- `dbFileComplaint` берёт заявителя из сессии, `dbGetPermVacancyViewers` — в
  `$ownedVacancyFns`, как и её сменный близнец.
- Проверка — `tests/read_authz_test.php`.

### Обрыв связи против пустоты
- Признак — на КАЖДЫЙ список отдельно: `offline` в `contexts/AppContext.tsx`
  (`vacancies`, `permVacancies`, `likes`, `permApplications`, `chats`). Загрузка
  при неудаче НЕ трогает список (остаётся кэш) и поднимает свой ключ, при
  успехе снимает. `backendOffline` там же — старое имя для пары вакансий.
- Кто что читает: лента — `backendOffline`, отклики — `offline.likes`,
  переписки — `offline.chats`. Общий признак на всех врал в обе стороны:
  говорил «нет связи», когда список экрана пришёл, и молчал, когда не пришёл.
- Тексты разные, смысл один — «дело в связи, а не в том, что здесь пусто», и в
  ленте есть кнопка «Попробовать снова». Пустой поиск обрывом не объявляется:
  признак меряется до фильтра.
- Зачем: «вакансий нет» человек читает как «здесь искать нечего» и уходит молча —
  в суточном отчёте это выглядит обычным оттоком. «Пока нет откликов» только
  что откликнувшийся читает как «мой отклик пропал».
- Проверка — `tests/offline_states_test.php`.

### Плашка «как отвечает этот работодатель»
- `components/feature/ReplyBadge.tsx`. Показывается в ДВУХ местах: на карточке
  свайпа в ленте (`app/(tabs)/feed.tsx`, до разделителя) и на странице вакансии
  (`app/perm-vacancy-detail.tsx`).
- Карта отзывчивости — `responsivenessMap` из `contexts/AppContext.tsx`, один
  запрос на всех. За ней на каждую карточку ходить нельзя: лента превратится в
  слайд-шоу.
- Молчит при менее чем двух переписках: по одной вывод делать нельзя.
- Почему до разделителя: `cardSummary` стоит `flexShrink`, поэтому лишняя
  высота ужимает описание, а не выталкивает «Читать полностью» за край —
  прокрутки внутри карточки нет намеренно.
- Проверка — `tests/reply_badge_test.php`.

### Отзывы: кто их видит
- `dbGetRatingsForUser` отдаёт РАЗНЫЕ колонки. О себе — всё: на своём профиле
  приложение показывает имя и аватар оценившего. О чужом — только
  `id,rating,role,review_text,created_at`: экран чужого профиля рисует отзывы
  анонимно.
- Почему это важно: `from_user_id`, `like_id` и `vacancy_id` вычисляют автора
  отзыва. Работодатель, узнавший, кто поставил ему двойку, — это конец честных
  отзывов в среде, куда возвращаются за следующей сменой.
- Проверка — `tests/ratings_privacy_test.php`. Она же следит за экранами: если
  чужой профиль однажды захочет показать автора, тест покраснеет и решение
  придётся принять заново.

### Суточный отчёт: разрыв воронки
- `php-proxy/funnel.php` — чистые правила: окно, доля ответивших, строка и
  тревога. Ни базы, ни сети: метрику нельзя проверить на живых данных из
  сессии, а молча нулевая доля соврала бы владельцу, что никто не отвечает,
  когда на деле сломан фильтр.
- **Окно вчерашнее, не последние сутки.** Метрика называется «ответ в течение
  суток» — значит у отклика эти сутки должны быть.
- Ответ по смене — `employer_liked` **не null** (он `boolean|null`, null это
  «ещё не решил»); отказ — тоже ответ. По постоянной вакансии — статус ушёл с
  `pending`.
- Известный перекос: работодатель лайкнул первым с «Кандидатов» — отклик
  засчитается отвеченным сразу. Порядок сторон в строке не записан.
- Тревога — меньше половины и не меньше пяти откликов. Проверка —
  `tests/funnel_test.php`.
- `php-proxy/shift_funnel.php` — исходы смен за последние сутки, конверсия
  отклик → подтверждённый выход по дозревшей 30-дневной когорте и доля
  работников, дошедших до второй смены. Проверка —
  `tests/shift_funnel_test.php`; индексы — миграция 066.
- `php-proxy/feed_funnel.php` — показывает, где обрывается путь от показа
  карточки до отклика: показы своих вакансий, гостевой путь заканчивается
  завершением регистрации. Проверка — `tests/feed_funnel_test.php`.

### Уведомления человеку: шлёт только сервер
- **Приложение уведомлений не шлёт вообще.** Каждое событие извещает та
  серверная операция, которая его записала: `jt_notify_new_message`
  (`dbInsertMessage`, `dbCreateChat`), `jt_notify_match`
  (`dbCheckAndCreateMatch`), `jt_notify_shift_outcome` (`dbSetShiftOutcome`),
  `jt_perm_app_announce` (`dbSetPermApplicationStatus`),
  `jt_shift_reject_announce` (`dbUpsertLike`).
- Поэтому `tgNotifyUser`, `sendPushNotification`, `tgNotifyNewApplication`,
  `dbGetPushToken` и `dbSaveNotification` переведены в `$adminFns`. Пока они
  были открыты вошедшему, текст уведомления приходил с клиента — через нашего
  бота можно было послать что угодно.
- Правило `jt_may_notify` удалено вместе с нуждой в нём: сторож, которого никто
  не зовёт, создаёт видимость проверки.
- **Исключение одно** — `notifyWorkersNewVacancy`: это рассылка, а не событие
  одного человека, и у неё свой серверный обработчик
  `dbNotifyAllWorkersNewVacancy`.
- **Громко и тихо.** `notify_user` — колокольчик + телеграм + пуш, а у кого нет
  ни телеграма, ни приложения — веб-пуш в браузер (`web_push_to`, запасной путь,
  не добавочный). До 14.09 веб-пуш звали только из рассылки о новой вакансии:
  человек из браузера узнавал о чужой смене, но не о своём мэтче. Ключи подписи
  (VAPID) живут в дашборде, php-proxy ходит к нему по HTTP — второй экземпляр
  ключей в публичном репозитории не нужен. Проверка — `tests/web_push_test.php`.
  `notify_bell` — только колокольчик, для новостей, за которые нельзя дёргать
  телефон. Гашение повтора по заголовку в пределах минуты живёт в `notify_bell`,
  то есть общее для обоих. Тихо приходит отказ по смене: их десятки за вечер,
  и разбуженный третьим выключает уведомления целиком — вместе с мэтчами и
  завтрашней сменой, ради которых он их и включал. Отказ по заявке на
  постоянную вакансию остаётся громким: его ждут, и приходит он редко.
- Проверки — `tests/notify_authz_test.php`, `tests/message_notify_test.php`,
  `tests/match_outcome_notify_test.php`, `tests/like_authz_test.php` (тихий
  отказ).

### Отклики на смены (`dbUpsertLike`)
- **Кто что вправе менять.** Владелец смены берётся из `jm_vacancies`, а не из
  довода запроса: `employerLiked` ставит только он, `workerLiked`/`workerSkipped`
  — только сам работник. Проверок здесь не было ни одной. Если смену удалили, а
  отклик остался (связи в базе нет), владелец берётся из самого отклика.
- **С клиента принимаются только три поля** — эти же. Мэтч, отметки о выходе на
  смену и об оценках ставит сервер в своих обработчиках; раньше их можно было
  прислать сюда и подписать себе.
- **Отказ объявляет сервер** — `jt_shift_reject_announce`, со ВСЕХ экранов, где
  отказывают. Сначала колокольчик через `notify_bell` — тихо, без пуша и
  телеграма (и только он доходит до тех, у кого переписки ещё нет, — а чат
  заводится лишь на мэтче, так что это самый частый случай), затем строка в
  переписку и счётчик непрочитанного, если переписка есть. Приложение писало строку от имени `system` и получало 403, а счётчик
  при этом рос: значок был, а за ним пусто.
- **Тексты решений — только на сервере**, в одном месте. Приложение своих не
  держит и после решения просто перечитывает переписку (`pollRef.current?.()`).
  В текстах названа вакансия: чат один на пару людей, и «по данной вакансии» не
  говорило, по какой именно. Фраза про закрытую переписку оставлена — ввод
  действительно блокируется (`isChatBlocked`), и без неё серый ввод нечем
  объяснить.
- Проверка — `tests/like_authz_test.php`.

### Отклики на постоянные вакансии
- **Решение принимает владелец вакансии.** `dbSetPermApplicationStatus` в
  `db.php` берёт отклик из `jm_perm_applications` и сверяет `employer_id` с
  сессией (чужой — 403, ненайденный — 404). Проверки не было вовсе: любой
  вошедший мог одобрить или отклонить чужого кандидата.
- **О решении говорит сервер тем же запросом** — `jt_perm_app_announce`:
  уведомление соискателю плюс системная строка в уже существующий чат и
  счётчик непрочитанного. Приложение это больше не делает. Строка от имени
  `system` приложению запрещена проверкой «Invalid sender», так что её вызов
  ВСЕГДА возвращал 403 и гасился пустым `.catch()` — соискатель не видел
  ничего. Заголовки уведомлений оставлены те же, что слал клиент: `notify_user`
  гасит повтор с тем же заголовком в течение минуты, поэтому старая сборка
  второго уведомления не даст.
- Проверка — `tests/perm_app_test.php`.

### Объявления в группу «ПОДРАБОТКИ»
- `dbNotifyAllWorkersNewVacancy` в `db.php`. Зовёт КЛИЕНТ после публикации
  (`services/notifications.ts` → `notifyWorkersNewVacancy`), сервер сам вакансии
  не сторожит.
- Отметок о доставке **две**: `bcast:<id>` — личные сообщения (ставится
  заранее, повторная рассылка всем хуже неотправленной), `gpost:<id>` — пост в
  группу (ставится только ПОСЛЕ успеха, чтобы повтор доделал несделанное).
- Итог последней публикации — `jm_settings.last_group_post`, виден строкой в
  суточном отчёте; недоставленный поднимает тревогу.
- **Догоняющее объявление** — `jt_announce_missed` в `db.php`, операция
  `cronAnnounceMissed`, расписание `.github/workflows/announce-missed.yml` (раз
  в час). Нужно потому, что объявление посылает КЛИЕНТ уже после закрытия
  экрана: свернул человек приложение — запрос не ушёл, и сервер о вакансии не
  узнал. Окно 6 часов и не больше 5 за заход, иначе первый запуск вывалил бы в
  группу всю историю.
- **Текст объявления собирает СЕРВЕР**, один на оба пути: `jt_group_html` в
  `db.php`. Приложение его больше не строит — довод `args[4]` оставлен пустым,
  чтобы не сдвинуть номера остальных. Раньше формат жил в двух местах и
  разъехался бы (была задача 16, закрыта).
- **Объявить вакансию может только тот, кто её разместил.** Обработчик берёт
  строку вакансии по `args[5]` и сверяет `employer_id` с сессией; чужая — 403,
  ненайденная — 404. Без этого любой вошедший слал в открытую группу и пушем
  каждому работнику произвольный текст. Проверка стоит ДО отметки `bcast:` —
  иначе посторонний пометил бы чужую вакансию разосланной, и настоящее
  объявление не ушло бы уже никогда.
- Проверка — `tests/group_post_test.php`.

### Телеграм
- `php-proxy/tg.php` — вебхук бота приложения `@JobToo_bot`.
- `php-proxy/bot_brain.php` — ответы бота.
- `php-proxy/tgtool.php` — служебная отправка в общую группу. Адрес группы зашит
  числом намеренно: пропуск лежит в открытом коде.
- Мост к Codex (PR #62) и рабочая беседа в телеграме **выведены из контура 13.09**
  решением владельца. Механика описана в истории PR, но не используется: работу
  ведёт Claude один, общение — в терминале сессии. Продуктового бота это не
  касается: уведомления людям и автоматические отчёты идут как шли.
- `lib/telegram.ts` — мини-приложение: ссылки `startapp`, настройка окна.

### Jupiter — агент подачи откликов (`jupiter/`)

Свой движок, не Playwright и не Selenium: HTTP-клиент, cookie jar, разбор
HTML и форм, политика переходов, отправка и проверка успеха написаны здесь.
Только stdlib Python, браузер не ставится.

| Файл | Что внутри |
|---|---|
| `jupiter/engine.py` | HTTP, cookie, разбор HTML в семантическую модель (`PageState`/`FormState`/`ControlState`), сборка и отправка формы |
| `jupiter/agent.py` | Планировщик: поиск вакансии и анкеты, заполнение полей, коды причин остановки (`Reason`); `is_application_form` отсеивает фильтры, подписки, формы для клиентов и «порекомендуй знакомого» до выбора формы |
| `jupiter/validation.py` | Проверка формы по правилам HTML5 до отправки |
| `jupiter/script_runtime.py` | Свой ограниченный DOM-рантайм, не JS VM |
| `jupiter/network_runtime.py` | Контролируемые fetch/XHR со страницы |
| `jupiter/policy.py` | Сетевая политика: резолв, отсечение внутренних сетей, закрепление адреса |
| `jupiter/tasks.py` | Очередь задач подачи: аренда, сердцебиение, чекпоинты, повторы |
| `jupiter/remote_tasks.py` | HTTP-мост к серверной очереди + `fetch_profile` для загрузки профиля кандидата из базы |
| `jupiter/run_worker.py` | Точка входа: цикл `run_once`, профиль кандидата из базы по `user_id` задачи |
| `jupiter/worker.py` | Воркер: берёт задачу и доводит её агентом; боевую задачу на сайт без `live_ready` не исполняет, а паркует с `SITE_NOT_VERIFIED` («Сайт ещё подключаем» в «Откликах») |
| `jupiter/handoff.py` | Просьба к человеку и состояние возврата: токен, куки, адрес шага |
| `jupiter/candidate.py` | Классы знаний о кандидате, разбор согласий, provenance |
| `jupiter/submission.py` | Доказательства отправки, отпечаток отклика и журнал поданных |
| `jupiter/spa_payload.py` | Чтение встроенного JSON SPA (`__NEXT_DATA__`, ld+json, Nuxt) ради адреса анкеты |
| `jupiter/site_compat.py` | Реестр работодателей: 62 источника владельца + сайты каталога; доверенные хосты подачи, переопределения полей и `live_ready` — куда разрешена боевая подача (29 сайтов: dry-run пройден разведкой без капчи и поля сверены вручную) |
| `jupiter/recon.py` | Разведка форм отклика по всем 170 разделам `scripts/career-sites.tsv`: Jupiter в dry-run с синтетическим кандидатом, класс раздела и снимок полей. Итог — `docs/разведка-форм.md`; `--via-proxy` только для облачного контейнера |
| `infra/recon-run.sh` | Та же разведка на московском сервере раз в сутки (таймер `jt-recon`, в 04:40; первый прогон сразу, пока журнал пуст; состояние — `recon_state`/`recon_last` в `security-status.json`): честная подпись Jupiter, 4 потока, только чтение; итог — открытый `https://147.45.184.99.sslip.io/jupiter-recon.json` (адреса работодателей и устройство анкет, без людей) |
| `jupiter/test_recon.py` | Классы разведки на синтетическом сайте; ни одного POST за весь обход |
| `jupiter/test_e2e.py` | E2E на синтетическом сервере работодателя |
| `jupiter/test_form_semantics.py` | Семантика формы и валидация, без сети |

- **dry-run — не про вежливость, а про запрет.** У движка есть
  `read_only=True`: POST и отправка формы падают на уровне движка, даже если
  планировщик ошибётся. Инвариант, ломать нельзя.
- **CAPTCHA не обходится.** В обычном режиме — остановка, в dry-run Jupiter
  сначала заполняет всё, что может, и только потом говорит, что остался
  человеческий шаг.
- **Неизвестные факты о кандидате не выдумываются.** Нет достоверного
  значения для обязательного поля — `action_required`, а не догадка.
- **Список хостов сам по себе не защищает.** Куда указывает домен, решает
  DNS, то есть чужая сторона. Перед каждым запросом адрес резолвится,
  внутренние сети, петля, link-local и метаданные облака отсекаются (хватает
  одного плохого адреса в ответе), а соединение идёт именно на проверенный
  адрес — это закрывает окно DNS rebinding. Имя остаётся в `Host` и в SNI.
  Внутренние адреса доступны, только если движку это разрешили ПРИ СОЗДАНИИ:
  список хостов пополняется адресом запуска, и выводить разрешение из него
  нельзя — стартовый `http://127.0.0.1/` разрешил бы себя сам.
- **Заявки Jupiter лежат в своей таблице** — `jm_jupiter_applications`
  (миграция 104). Не в `jm_perm_applications`: та про отклик ВНУТРИ JobToo,
  где решает работодатель, а эта про чужой сайт и фоновый прогон. Защита от
  второго отклика — уникальный индекс `(user_id, canonical_url)` в базе, а не
  проверка в коде: код проигрывает гонке двух запросов, индекс — нет. Задачу
  воркер берёт функцией `jupiter_lease_task` с `for update skip locked`, иначе
  два воркера возьмут одну и работодатель получит два отклика.
- **Функции `jupiterEnqueue`/`jupiterMyApplications`** — по `$selfArgFns`,
  человек видит только свои. `jupiterLease`/`jupiterHeartbeat`/`jupiterFinish`
  — админские: воркер берёт ЧУЖИЕ задачи по очереди, и пользовательская
  сессия здесь означала бы доступ к чужим заявкам. `jupiterFinish` пишет по
  белому списку полей — подменить владельца или адрес вакансии нельзя.
- **Свайпа во внешнюю вакансию пока НЕТ, и это не забывчивость.** Решение,
  откуда берутся внешние вакансии, принято: агрегируем карьерные страницы
  сервером (миграция 105, раздел «Каталог карьерных сайтов»). Но у `PermVacancy`
  по-прежнему нет ни адреса анкеты, ни источника, а вакансии из
  `jm_ext_vacancies` в ленту пока не отдаются — это следующий срез, и он
  трогает `app/(tabs)/feed.tsx`.
- **Свайп не ждёт чужой сайт.** Задача кладётся в очередь (`tasks.py`), её
  берёт воркер. Аренда с сердцебиением, чекпоинты после дорогих шагов, предел
  на домен (двадцать свайпов по одному работодателю — не двадцать
  одновременных запросов к нему), пауза перед повтором 1/4/9 минут. Ожидание
  человека и неизвестный исход отправки — конечные состояния: воркеру их не
  отдают. Боевой таблицы и связи со свайпом ЕЩЁ НЕТ — это отдельная правка,
  она трогает живую базу.
- **Остановка «нужен человек» — возобновляемая.** Капча, код из письма, вход,
  неизвестный обязательный вопрос: Jupiter отдаёт `HumanActionRequest` с
  токеном и сохраняет состояние (адрес шага, куки, список хостов).
  `agent.resume(token, profile)` продолжает с того же места, и это работает из
  другого процесса. Ничего не обходится: ни капча, ни MFA; пароли работодателя
  не хранятся, состояние живёт сутки и стирается по завершении.
- **Галочка согласия — не обычное поле.** «Обработка персональных данных» не
  равно «кадровый резерв» и не равно «реклама». Обязательное согласие
  ставится, если человек его дал; реклама и кадровый резерв — никогда по
  умолчанию; смешанная галочка (обязательное и необязательное вместе) всегда
  уходит человеку, даже если оба согласия даны по отдельности. Разделять то,
  что работодатель связал в один чекбокс, — не наше решение.
- **Юридические вопросы не угадываются.** Гражданство, право на работу, виза,
  права, инвалидность: нет в профиле — `UNKNOWN_REQUIRED_QUESTION`, а не
  догадка. Ошибка здесь — ложное заявление от имени человека.
- **У каждого отправленного значения есть происхождение.** В траектории —
  `provenance`: класс поля и источник. Без этого нельзя объяснить человеку,
  что именно за него отправили.
- **Нажатие — не успех, и двести — не успех.** Статус `submitted` ставится
  по доказательствам с весами (`submission.py`), порог 0.8. Текст «спасибо за
  отклик» засчитывается, только если его не было на странице ДО отправки:
  иначе сайт с этой фразой в подвале подтверждал бы что угодно. HTTP-ответ
  пишется в доказательства с весом ноль — намеренно, чтобы правило было видно
  в данных.
- **Один отклик — один раз.** Отпечаток (кандидат, работодатель, адрес
  вакансии без рекламных меток, адрес анкеты, состав формы) проверяется ДО
  отправки. Уже поданное не подаётся снова: статус `duplicate`. Журнал —
  `ReceiptStore`, файл через `--receipts`.
- **Оборванный POST — отдельное состояние.** `EngineTransportError` отличает
  «связь пропала» от «сервер ответил отказом». Заявка могла дойти, поэтому
  повтор POST запрещён: пишется `submission_unknown`, делается одна проверка
  GET-ом, и до подтверждения этот отпечаток остаётся неизвестным.
- **SPA читаем, а не рисуем.** Формы и ссылки на анкету в первичном HTML нет —
  их дорисовывает браузер. Jupiter берёт адрес из уже готового JSON страницы
  (`__NEXT_DATA__`, ld+json, `window.__NUXT__`) и добавляет его к обычным
  ссылкам, ранжируя тем же счётом. React он не исполняет: нет адреса в
  состоянии — честный тупик, но в траектории видно, какие блоки прочитали.
  Ключ `page` в `__NEXT_DATA__` — шаблон маршрута (`/vacancy/[id]`), а не
  адрес: по очкам он обыгрывал настоящую ссылку и вёл в 404.
- **«Далее» — не «Отправить».** Кнопки классифицируются по намерению
  (`apply`/`next`/`back`/`save`), шаг анкеты узнаётся по адресу отправки и
  набору полей, а не по адресу страницы: его меняет сам POST. Вернулся тот же
  шаг — остановка с `STEP_DID_NOT_ADVANCE`, а не повторная отправка. В dry-run
  многошаговая анкета даёт статус `step_ready`, а не `ready_to_submit`: пройти
  дальше можно только настоящим POST, который движок запрещает.
- **Причина остановки — код, а не текст.** `AgentResult.reason_code`
  (`VALIDATION_FAILED`, `CAPTCHA_REQUIRED`, `MISSING_PROFILE_FIELD` и
  прочие). Статистику совместимости считать по коду: текст меняется.
- **Форма разбирается по стандарту, а не «как обычно бывает»**: `<base href>`,
  `<fieldset disabled>`, `formaction`/`formmethod`/`formenctype`,
  `<select multiple>`, `readonly`, `type=image`. Расхождение с браузером
  здесь всегда тихое — анкета уходит не та или не туда.
- Приватная лаборатория — `https://jobtoo.ru/jupiter/`, только админ, живой
  dry-run по реальным URL без отправки.

### Панель управления
`dashboard/` — Next.js, TailAdmin. 29 разделов: воронка, когорты, активность, гео,
вовлечённость, качество, спящие, источники, матчинг. Запросы — `dashboard/lib/queries.ts`.
Выкладка — отдельная, через релизы GitHub; сервер тянет их сам.

### Выкладка
- Пуш в `main` запускает: `ci.yml`, `deploy-regru.yml` (сайт на московский сервер),
  `eas-update.yml` (обновление приложения по воздуху).
- `public/robots.txt`, `public/sitemap.xml` — поисковые правила и карта только
  существующих публичных страниц; файловые URL без файла получают 404 в nginx.
- `infra/bootstrap.sh` — сервер раз в минуту проверяет обновления и тянет их сам.
- `infra/migrate.sh` накатывает новые SQL до замены PHP и сайта. Ошибка оставляет
  прежний релиз на месте; следующий запуск таймера повторяет попытку.
- Регулярные задачи: `evening-digest.yml` (16:00 UTC), `daily-nudges.yml` (07:00),
  `partner-monthly-report.yml`, `site-availability.yml` (каждые 5 минут).

## Грабли, на которые уже наступали

- **`app/(tabs)/feed.tsx` и `php-proxy/db.php` слишком велики для чтения целиком.**
  Всегда `grep` по имени, потом `sed -n 'N,Mp'`.
- **На вебе после перетаскивания приходит `click`** — нажатие срабатывает вслед за
  свайпом. Лечится признаком «только что был свайп».
- **`curl` в GitHub без `-L`** врёт после переименования репозитория: ответ `301`
  выглядит как отсутствие данных.
- **Заголовки nginx внутри `location` заменяют серверные целиком**, а не дополняют.
- **`.expo/types/router.d.ts`** — временный артефакт. Если после запуска dev-сервера
  `tsc` ругается на маршруты, удали каталог `.expo/types`.
- **Метрика стоит, но её данные никуда не забираются** — счётчик `109805381`.
