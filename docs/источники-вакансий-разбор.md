# Откуда брать вакансии: разбор cofinder и план ухода от агрегаторов

Разобрано 15.09.2026. Задача владельца: посмотреть, откуда берёт вакансии
конкурент cofinder.ru, повторить это у себя и уйти от агрегаторов — Работы
России и SuperJob, — оставив свои вакансии и карьерные сайты.

Всё ниже проверено живыми запросами, а не прочитано в описаниях. Где вывод
держится на одном наблюдении, это сказано прямо.

## 1. Как устроен cofinder

У него открытый JSON API без ключа, на FastAPI. Документации нет (`/docs` и
`/openapi.json` отдают страницу приложения), поэтому разбирали перебором.

| Адрес | Что отдаёт |
|---|---|
| `/api/v1/vacancies/?limit=100&offset=N` | вакансии, `total` = 18 001 |
| `/api/v1/companies/?limit=500` | **160 компаний с их карьерными сайтами** |
| `/api/v1/categories/` | 24 категории-классификатора |

Поля вакансии: `title`, `company_name`, `location`, **`url`** (прямая ссылка на
карьерный сайт работодателя), `description`, `salary_min`/`salary_max`,
`work_format`, `is_remote`, `responsibilities`, `requirements`, `conditions`,
`found_at`, `days_since_update`, `has_hr`, `match_score`.

Важное про честность: **cofinder отдаёт первоисточник открыто** — в каждой
вакансии лежит ссылка на карьерный сайт компании. Второй конкурент, swipejobs,
делает наоборот: у него на странице вакансии ни одной внешней ссылки,
в разметке `directApply: true`, а `identifier.name` — «Swipe Jobs». Чужая
вакансия подана как своя. Наше правило в `cf_normalize` (нет ссылки на
оригинал — вакансию не берём) ближе к cofinder, и это правильная сторона.

### Фильтры, которые у него работают

Проверено подстановкой: меняют выдачу только четыре.

| Фильтр | Работает | Пример |
|---|---|---|
| `category_id` | да | `28` → 250 вакансий |
| `company_id` | да | `21` → 99 |
| `work_format` | да | `remote` → 2 585 |
| `salary_min` | да | `100000` → 17 321 |
| `is_remote`, `experience_level`, `has_hr`, `sort`, `country`, `location` | **нет** | молча игнорируются, отдают все 18 007 |

То есть фильтрация у него беднее, чем кажется по полям: половина параметров
принимается и не применяется.

## 2. Их 160 компаний

- **111 российских** (`country_codes = ["RU"]`), остальные — Кипр, Армения и международные.
- Отрасли российских: Технологии — 80, Финтех — 7, E-commerce — 6, Телеком — 5,
  Ритейл — 2, по одной — энергетика, производство, EdTech, геймдев.
- Сумма вакансий у российских: 11 934.
- Крупнейшие: Сбер (1 756), МТС (1 563), Росатом (1 123), Яндекс (1 095),
  Ozon (866), Альфа-Банк (829), ВТБ (722), Т-Банк (548).

Полный список — в приложении в конце файла.

### Ни одна не сидит на системе найма

Проверяли гипотезу, что карьерные сайты сделаны на ATS (Хантфлоу, Talantix,
Поток) и потому имеют общую структуру — тогда один разбор покрыл бы сотни
работодателей. **Гипотеза не подтвердилась: все 111 российских компаний держат
карьерный сайт на своём домене.**

Оговорка, которая стоит того, чтобы её записать: у Хантфлоу публичный JSON API
карьерного сайта действительно есть и работает одинаково у разных клиентов —
`https://<компания>.huntflow.io/api/vacancy` отдаёт список, а
`/api/vacancy/<slug>` полное описание. Проверено на шести клиентах, ответили
пять. Канал рабочий и дешёвый, но крупные работодатели им не пользуются, а у
тех, кто пользуется, вакансии офисные: у Кофемании там CRM-маркетолог и два
разработчика C#, ни одного повара.

### Чего стоит их подход

У cofinder в ответе есть собственный `parse_status`, и он показывает цену
поддержки 160 отдельных парсеров: **15 из 111 российских сломаны** (13,5 %).

| Причина | Примеры |
|---|---|
| `Parser returned 0 vacancies` — сайт перевёрстан | СИБУР, Cloud.ru, Arenadata, Юзтех, Спортс |
| `[SSL: CERTIFICATE_VERIFY_FAILED]` | Точка Банк, Positive Technologies, НСПК |
| Selenium stacktrace | Альфа-Банк |

Последняя строка говорит больше остальных: ради части сайтов им приходится
гонять настоящий браузер. Это не «скачал страницу и разобрал», это отдельная
инфраструктура, которая ломается и стоит денег.

## 3. Главный вывод: копировать этот список нам нечего

Искали в их базе из 18 001 вакансии наши профессии. Результат:

| Запрос | Найдено | Что это на самом деле |
|---|---|---|
| комплектовщик | **0** | — |
| упаковщик | **0** | — |
| кладовщик | 3 | «Product manager WMS», «Тимлид команды в Лавку» |
| повар | 3 | «Руководитель проекта по автоматизации» |
| официант | 2 | «Кофе-леди/кофе-мен в офис» |
| грузчик | 8 | «Менеджер склада», «Оператор механизированных» |
| склад | 754 | «Заведующий складом», «Менеджер по адаптации (склад)» |

Совпадения — это слова из описаний и названия управленческих позиций. Линейных
смен нет ни одной. Подтверждает и их собственный классификатор: из 24 категорий
все офисные, а единственная близкая, «Logistics / Supply Chain», содержит
**ноль** вакансий.

Второй конкурент такой же: у swipejobs в выдаче «Ведущий разработчик Java»,
«Юрист», «Бизнес-ассистент операционного директора», компании — Яндекс, Сбер,
ВТБ, VK.

**Оба конкурента работают на рынке офисного и IT-найма. JobToo — про смены.
Это разные рынки, и их список компаний для нас пустой.**

Отсюда же ответ, почему они вообще смогли построиться на карьерных сайтах:
IT-компании выкладывают вакансии структурированно и борются за выдачу. Склады и
общепит так не делают — проверено отдельно (см. ниже).

## 4. Что показала проверка нашей ниши

Прошли по карьерным сайтам работодателей, которые реально нанимают на смены:
Самокат, X5, Магнит, ВкусВилл, Лента, Детский мир, Яндекс Еда, Ozon.

**Ни на одном нет разметки `schema.org/JobPosting`.** У Самоката страница
«Работа кладовщиком» — 63 КБ HTML и ноль структурированных данных, обычный
лендинг с формой. У Детского мира WordPress, но REST API закрыт (401). У X5
REST API открыт, но типов записей для вакансий там нет вовсе — это
корпоративный сайт.

Наш `career_feed.php` умеет читать только `JobPosting`. Значит по нашей нише он
сегодня не прочитает ничего — и дело не в коде, а в том, что читать нечего.

Важная оговорка о качестве этой проверки: четыре адреса из первого захода
(`dodo.jobs`, `rabota.5ka.ru` и ещё два) не существуют — они были предположены,
а не проверены. Вывод построен только на тех, что ответили.

## 5. Отдельная находка: лента засорена не из-за нехватки источников

Пока искали источники, нашли то, что мешает сильнее их отсутствия.

`php-proxy/headhunter.php:26` запрашивает `api.hh.ru/vacancies?area=1` — всю
Москву подряд, **без фильтра по профессии**. Дальше `ing_work_type`
(`ingest.php:147`) пытается угадать профессию по названию и возвращает `null`,
если это не склад, повар или бригадир — но строка с `null` **всё равно
записывается** (`ingest.php:238`). При выдаче в ленту фильтр только по `active`
и `environment`; по профессии — никакого.

То есть в ленту человеку, который ищет смену, приезжают бухгалтеры и
Java-разработчики. Это прямой ответ на записанное в очереди «2000+ новых
вакансий за сутки — ноль откликов». **Не мало вакансий. Не те вакансии.**

Проверить живьём отсюда не вышло: hh.ru отвечает 403 на запросы из контейнера,
на московском сервере адаптер работает. Вывод сделан по коду, с обеих сторон —
и на приёме, и на выдаче.

## 6. План действий

Порядок важен. Главная опасность — отключить агрегаторы раньше, чем карьерные
сайты дадут объём: лента опустеет, и это будет хуже, чем засорённая лента.

### Шаг 1. Почистить то, что уже есть — СДЕЛАНО 15.09

Ничего нового подключать не понадобилось.

1. **Фильтр по профессиям в запросе к hh.** `headhunter.php` больше не просит
   всю Москву: в запросе перечислены шесть ролей из справочника
   `api.hh.ru/professional_roles` — упаковщик-комплектовщик (131), кладовщик
   (52), грузчик (31), разнорабочий (102), повар-пекарь-кондитер (94), мойщик
   посуды и помощник повара (184). Список узкий намеренно: ровно то, что
   `ing_work_type` раскладывает в свои четыре вида работ. Официант, курьер и
   продавец сюда не вошли — их `work_type` вышел бы пустым, и отсев выбросил бы
   их следующим шагом.
2. **Профессия приходит от hh, а не угадывается.** Адаптер читает
   `professional_roles` из ответа и кладёт готовый `work_type`. Догадка по
   названию осталась запасным путём: пропади поле — вакансию разберёт
   `ing_work_type`, а не выбросит отсев.
3. **Отсев на приёме.** `ing_normalize` возвращает `null`, если вид работ
   определить не удалось. Вакансия не пишется вовсе: показывать чужую вакансию,
   которую мы не смогли отнести ни к одному своему виду работ, незачем.
4. **Классификатор расширен** под названия из запрошенных ролей — пекарь,
   кондитер, мойщик посуды, упаковщик, маркировщик, приёмщик, разнорабочий.
   Без этого вакансия, пришедшая по нашему же фильтру, не узнавалась бы.

Отсев не молчит: каждый `null` уже считался как `skipped`, а он попадает и в
статус источника («получено X, пропущено Y»), и в поле `last_skipped`, которое
читает дашборд.

Проверка — `tests/feed_relevance_test.php`, на настоящей production-нормализации
через `INGEST_LIBRARY_ONLY`. Мутации проверены обе: убрать отсев — падают
проверки на бухгалтера и Java-разработчика; убрать фильтр ролей — падает
проверка запроса к hh.

Оговорка: живьём фильтр отсюда не проверить — hh отдаёт 403 на поиск вакансий с
датацентровых адресов (справочники при этом доступны). На московском сервере
адаптер работает.

**Важно про остальные источники.** `work_type` не отдаёт ни один адаптер —
ни `trudvsem`, ни `superjob`, ни `career_feed`; все полагались на догадку по
названию. Значит отсев подействует и на них, и это задумано: правило «в ленте
только свои виды работ» не должно зависеть от того, кто вакансию принёс.

Ожидаемый эффект: лента схлопнется в разы по количеству и станет релевантной.
Это то, что владельцу надо решить отдельно — терпимо ли падение объёма.

### Разведка проведена 15.09: 30 крупнейших российских компаний

Прошли карьерные сайты тридцати компаний из списка cofinder (те, у кого его
собственный парсер в состоянии `ok`). По каждому смотрели разметку, типовые
JSON-эндпоинты и код ответа.

**Разметки `JobPosting` нет ни у одной. Ноль из тридцати.** Это закрывает
вопрос: коннектор, умеющий только разметку, на этом рынке бесполезен.

Живой JSON нашёлся у трёх, и вот что с ними на самом деле:

| Компания | Эндпоинт | Что вышло |
|---|---|---|
| **Yadro** | `careers.yadro.com/api/vacancies` | **221 вакансия, работает** — адрес вакансии собирается как `/vacancy/{slug}`, проверено кодом 200 |
| 2ГИС | `job.2gis.ru/api/v1/vacancies/` | каталог отдаёт, но **адрес вакансии не разгадан**: ни `/vacancy/{id}`, ни `/vacancies/{id}` не отвечают, детальный API уводит редиректом. Без ссылки на первоисточник вакансию не берём — отложено |
| Lesta Games | `join.lesta.team/api/vacancies` | отдаёт только категории, самих вакансий по этому пути нет |

Итог честный: из тридцати компаний **один готовый источник**. Перебором восьми
типовых путей больше и не найти — настоящие эндпоинты видны только в сетевых
запросах страницы, и находить их придётся по одному.

Это и есть мера задачи. Не «подключим 111 компаний», а «каждая компания — это
отдельная разведка», ровно как у cofinder с его 160 парсерами.

### Шаг 2. Разведка по своей нише, а не по списку cofinder

Взять 10–15 работодателей, нанимающих линейный персонал в Москве (Ozon,
Wildberries, Самокат, X5, Магнит, Лента, ВкусВилл, Додо, Яндекс Лавка,
СберМаркет), и по каждому ответить на три вопроса:

- отдаёт ли сайт машиночитаемые данные (разметка, JSON, открытый API);
- что говорит `robots.txt` (у проверенных ни один не запрещает вакансии);
- есть ли там вообще линейные смены, а не только офис.

Результат разведки — таблица «можно / нельзя / чем брать». Без неё писать
коннектор преждевременно.

### Шаг 3. Коннектор под то, что нашлось

Если у работодателя есть JSON или API — подключать как источник, по образцу
`career.php`. Если только HTML — писать разбор под конкретный сайт, зная цену:
у cofinder на этом пути ломается каждый седьмой парсер.

Для клиентов Хантфлоу коннектор дешёвый и уже понятный (адреса и структура
выяснены), но охват по нашей нише пока не подтверждён.

### Шаг 4. Отказ от агрегаторов — последним

SuperJob (`superjob.php`) отключать только
тогда, когда карьерные источники дают достаточный поток смен. Порядок «сначала
отключим, потом наполним» оставит ленту пустой.

hh.ru стоит держать дольше остальных — не как витрину, а как наполнитель, пока
свои и карьерные вакансии не наберут объём.

### Чего делать не нужно

- **Копировать список компаний cofinder.** Проверено: наших вакансий там нет.
- **Строить всё на ATS-платформах.** Крупные работодатели ими не пользуются.
- **Отключать агрегаторы до наполнения.** Пустая лента хуже засорённой.

## 7. Что осталось выяснить

- Сколько смен реально дадут карьерные сайты нашей ниши — ответит шаг 2.
- Согласен ли владелец на падение объёма ленты ради релевантности — шаг 1.
- Даёт ли `has_hr` у cofinder прямой контакт рекрутёра: поле есть, но фильтр по
  нему не работает, а что внутри — не проверяли.

## Приложение: 111 российских компаний cofinder

Отсортированы по числу вакансий. `СЛОМАН` — состояние парсера у самого
cofinder, не у нас.

| Компания | Вакансий | Отрасль | Парсер | Карьерный сайт |
|---|---|---|---|---|
| Сбер | 1756 | Финтех / Банкинг | ok | https://rabota.sber.ru/search |
| МТС | 1563 | Телеком / Экосистема | ok | https://job.mts.ru/vacancies |
| Росатом | 1123 | Энергетика / Госкорпорация | ok | https://rosatom-career.ru/vacancies |
| Яндекс | 1095 | Технологии / Интернет | ok | https://yandex.ru/jobs/vacancies |
| Ozon | 866 | E-commerce | ok | https://career.ozon.ru/vacancy |
| Альфа-Банк | 829 | Финтех / Банкинг | СЛОМАН | https://job.alfabank.ru/vacancies |
| ВТБ | 722 | Финтех / Банкинг | ok | https://rabota-vtb.ru/career |
| Т-Банк | 548 | Финтех / Банкинг | ok | https://www.tbank.ru/career/vacancies/it |
| VK | 256 | Технологии / Интернет | ok | https://team.vk.company/vacancy |
| Золотое Яблоко | 238 | Ритейл / Бьюти | ok | https://job.goldapple.ru/search/all |
| Ростелеком | 210 | Телеком | ok | https://job.rt.ru/search |
| Yadro | 206 | Технологии / Hardware | ok | https://careers.yadro.com/vacancies |
| МегаФон | 146 | Телеком | ok | https://job.megafon.ru/vacancy/all/any |
| Avito | 132 | E-commerce / Classifieds | ok | https://career.avito.com/vacancies/ |
| Cloud.ru | 132 | Технологии / Облака | СЛОМАН | https://cloud.ru/career/vacancies |
| Wildberries | 112 | E-commerce | ok | https://career.rwb.ru/vacancies |
| Билайн | 107 | Телеком | ok | https://job.beeline.ru/vacancies |
| Aston | 99 | Технологии / IT-аутсорс | ok | https://career.astondevs.ru/vacancy |
| 2ГИС | 86 | Технологии / Геосервисы | ok | https://job.2gis.ru |
| КРОК | 74 | Технологии / Интеграция | ok | https://careers.croc.ru/vacancies |
| Lesta Games | 72 | Game development / Entertainment technology | ok | https://join.lesta.team |
| Lamoda | 70 | E-commerce / Мода | ok | https://job.lamoda.ru/vacancies |
| GRASS | 70 | Производство / FMCG | ok | https://job.thgrass.com/vacancies |
| IT_One | 69 | Технологии / IT-аутсорс | ok | https://www.it-one.ru/vacancies |
| Selecty | 68 | Технологии / HR-tech | ok | https://selecty.ru/it-vacancy |
| Тензор | 68 | Технологии / SaaS | ok | https://tensor.ru/vacancies |
| Kaspersky | 62 | Технологии / Кибербезопасность | ok | https://careers.kaspersky.ru/vacancies |
| Группа Астра | 61 | Технологии / ОС и инфраструктура | ok | https://astra.ru/about/career/vacancies |
| Контур | 52 | Технологии / SaaS | ok | https://kontur.ru/career/vacancies |
| IBS | 45 | Технологии / Консалтинг | ok | https://ibs.ru/career/jobs |
| 1С | 44 | Технологии / ERP | ok | https://1c.ru/rus/firm1c/vacan/search |
| Navio | 42 | Технологии / Автотех | ok | https://navio.auto/vacancies |
| Deloitte | 37 | Консалтинг / Аудит | ok | https://careers.delret.ru |
| Солар | 37 | Технологии / Кибербезопасность | ok | https://team.rt-solar.ru/vacancies |
| Точка Банк | 35 | Финтех | СЛОМАН | https://hr.tochka.com/vacancies |
| Evercode Lab | 32 | Технологии / Разработка | ok | https://evercodelab.ru/vacancies |
| СИБУР | 30 | Промышленность / Нефтегазохимия и цифровые продукты | СЛОМАН | https://career.sibur.ru/vacancies |
| ГНИВЦ | 30 | Технологии / Госсектор | ok | https://career.gnivc.ru/vacancies |
| Orion soft | 28 | Технологии / Инфраструктура | ok | https://career.orionsoft.ru/vacancy |
| Axenix | 27 | Консалтинг / IT | ok | https://axenix.pro/career |
| Селектел | 27 | Технологии / Облака | ok | https://selectel.ru/careers/all |
| Centicore Group | 27 | Технологии / Разработка | ok | https://centicore.ru/vacancies |
| Техвилл | 25 | IT services / Product development | ok | https://techvill.ru/vacancies |
| Авиасейлс | 24 | Технологии / Тревел | ok | https://www.aviasales.ru/about/vacancies |
| X5 Tech | 24 | E-commerce / Retail | ok | https://x5.tech/vacancy |
| Циан | 23 | Технологии / Недвижимость | ok | https://krasnodar.cian.ru/vacancies |
| Positive Technologies | 23 | Технологии / Кибербезопасность | СЛОМАН | https://ptsecurity.com/about/vacancy |
| Bell Integrator | 22 | Технологии / Интеграция | ok | https://bellintegrator.ru/information/vacancies |
| Иви | 20 | Медиа / Стриминг | ok | https://corp.ivi.ru/vacancies |
| КОРУС Консалтинг | 20 | Технологии / Консалтинг | ok | https://career.korusconsulting.ru/vacancies |
| Лоция | 20 | Технологии / IT-аутсорс | СЛОМАН | https://loodsen.ru/vacancies |
| SimbirSoft | 19 | Технологии / IT-аутсорс | ok | https://www.simbirsoft.com/vacancies/ |
| Инфосистемы Джет | 18 | Технологии / Интеграция | ok | https://jet.su/career/vacancies |
| Mish | 17 | Технологии / Дизайн | ok | https://mish.design/ru/vacancies |
| Самокат | 16 | E-commerce / Доставка | ok | https://vacancy.samokat.ru/vacancies-business |
| ДатаРу | 16 | Технологии / Hardware | ok | https://dataru.ru/career/vacancy |
| НСПК | 13 | Финтех / Платежные технологии | СЛОМАН | https://www.nspk.ru/vacancies |
| Протей | 13 | Телеком / VoIP | ok | https://protei.ru/vacancies |
| Koronatech | 13 | Финтех | ok | https://koronatech.ru/vacancy/ |
| kokos group | 12 | Технологии / Маркетинг | ok | https://career.kokocgroup.ru/vacancies |
| BSL | 12 | Технологии / IT-аутсорсинг | ok | https://bsl.dev/vacancies.html |
| Arenadata | 12 | Технологии / Big Data | СЛОМАН | https://career.arenadata.tech |
| PrideInBrains | 12 | Технологии / Разработка | ok | https://prideinbrains.com/%D0%92%D0%B0%D0%BA%D0%B0%D0%BD%D1%81%D0%B8%D0%B8/ |
| Inline Group | 11 | Технологии / IT-консалтинг | ok | https://inlinegroup.ru/about/jobs |
| iFellow | 11 | Технологии / IT-аутсорсинг | ok | https://ifellow.ru/vacancy |
| Spotware Systems | 10 | Fintech / Trading technology | ok | https://spotware.applytojob.com/apply |
| Globus IT | 10 | Технологии / IT-аутсорс | ok | https://globus-ltd.ru/vacancies |
| Галактика | 10 | Технологии / ERP | ok | https://galaktika.ru/career |
| SearchInform | 9 | Технологии / Информационная безопасность | ok | https://searchinform.ru/company/jobs |
| Гараж 8 | 8 | Технологии / Разработка | СЛОМАН | https://garage-eight.com/vacancies |
| Спортмастер | 8 | Ритейл / Спорт | ok | https://job.sportmaster.ru/vacancies/ |
| Coral | 8 | Технологии / Wellness | ok | https://coral.tech/it-main |
| Наумен | 8 | Технологии / Enterprise | ok | https://www.naumen.ru/career/vacancies |
| Островок | 7 | Технологии / Тревел | ok | https://career.ostrovok.ru/vacancy |
| M2 Tech | 7 | Технологии / PropTech | ok | https://tech.m2.ru |
| Адвантум | 7 | Технологии / Логистика | ok | https://advantum.ru/jobs |
| amoCRM | 6 | Технологии / SaaS | ok | https://www.amocrm.ru/jobs |
| Performance Lab | 6 | Технологии / QA | ok | https://www.performance-lab.ru/vacancy |
| Softline | 6 | Технологии / Дистрибуция | ok | https://softline.ru/about/vacancies |
| Cloud Networks | 6 | Технологии / Кибербезопасность | ok | https://cloudnetworks.ru/vacancies |
| Юзтех | 6 | Технологии / IT-аутсорс | СЛОМАН | https://usetech.ru/career |
| ITG | 6 | Технологии / Облачные сервисы | СЛОМАН | https://itglobal.com/ru-ru/company/vacancies |
| Траектория технологий | 5 | Технологии / Инфраструктура | ok | https://trctech.ru/karera |
| Twinby | 4 | Технологии / Цифровые двойники | ok | https://twinby.ru/career |
| iSpring | 4 | Технологии / EdTech | ok | https://www.ispring.ru/company/jobs/vacancies |
| Agima | 4 | Технологии / Digital-агентство | ok | https://www.agima.ru/careers |
| Индасофт | 4 | Технологии / Промышленная автоматизация | СЛОМАН | https://indusoft.ru/about/vacancies |
| Reksoft | 4 | Технологии / Разработка | ok | https://career.reksoft.com/vacancies |
| Baum | 4 | Технологии / IT-разработка | ok | https://baum.ru/job |
| Аурига | 4 | Технологии / IT-аутсорс | ok | https://hr.auriga.ru |
| Just AI | 3 | Технологии / AI | ok | https://careers.just-ai.com/vakansii |
| С-Терра | 3 | Технологии / Кибербезопасность | ok | https://www.s-terra.ru/company/rabota-u-nas |
| Rush Agency | 3 | Технологии / Маркетинг | ok | https://www.rush-agency.ru/jobs |
| Crosstech | 3 | Технологии / IT | ok | https://crosstech.ru/vacancies |
| Linx | 3 | Технологии / Облачные сервисы | ok | https://linx.ru/about/career |
| Контакт | 3 | Технологии / IT-аутсорс | СЛОМАН | https://itcontact.ru/vacancies |
| Дельта Компьютерс | 3 | Технологии / Hardware | ok | https://deltacomputers.ru/vacancies |
| RedLab | 2 | Технологии / Разработка | ok | https://redlab.dev/career |
| TerraLink | 2 | Технологии / IT-консалтинг | ok | https://terralink.ru/career/vacancies |
| Спортс | 2 | Медиа / Спорт | СЛОМАН | https://www.sports.ru/football/blogs/3271770.html |
| Notamedia | 2 | Технологии / Digital-агентство | ok | https://nota.media/career |
| BINOM | 2 | Технологии / Разработка | ok | https://binom.systems/vakansii |
| Directum | 2 | Технологии / Документооборот | ok | https://career.directum.ru/vacancy?from=main_button_vacancy |
| Rubius | 2 | Технологии / IT-разработка | ok | https://rubius.com/ru/company/career |
| PIX Robotics | 2 | Технологии / RPA | ok | https://pix.ru/company/vacancy |
| РДВ Технолоджи | 2 | Технологии / Hardware | ok | https://rdwcomp.ru/kompaniya/vakansii |
| Macroscop | 1 | Технологии / Видеоаналитика | ok | https://pro.macroscop.com/vacancies |
| Stepik | 1 | EdTech | ok | https://welcome.stepik.org/ru/careers |
| Пачка | 1 | Технологии / SaaS | ok | https://pachca.com/career |
| Effective Technologies | 1 | Технологии / IT-разработка | СЛОМАН | https://career.effective-group.ru |
| Хоулмонт | 1 | Технологии / Enterprise | ok | https://job.haulmont.ru/career |

---

# Почему конкурент смог, а мы нет: разбор пяти способов

Дописано 15.09.2026 после того, как стало ясно: разведка по типовым путям даёт
один рабочий источник из тридцати. Вопрос владельца был прямой — cofinder читает
140 сайтов, почему не можем мы. Ниже проверенный ответ и пять способов с
цифрами, а не с рассуждениями.

## Короткий ответ

**Конкурент рендерит страницы браузером.** Доказательство лежит в его же
ответе: в поле `parse_status` у Альфа-Банка — Selenium stacktrace, а у трёх
других сайтов ошибки проверки сертификата, каких у простого HTTP-клиента не
бывает.

Мы же пытались читать статикой то, чего в статике нет. Вакансии на этих сайтах
появляются только после выполнения JavaScript.

## Замер: что читается без браузера

Проверены все тридцать карьерных сайтов крупнейших российских компаний.

| Способ | Работает |
|---|---|
| Разметка `schema.org/JobPosting` | **0 из 30** |
| Перебор типовых путей к API | 3 найдено, из них рабочий **1** (Yadro, 221 вакансия) |
| Данные внутри HTML (`__NEXT_DATA__`, `__NUXT__`) | **2 из 30** — ВТБ (20 вакансий), VK (62) |

Итого без браузера доступны **три сайта из тридцати, это 10 %**. Остальные 90 %
отдают пустую оболочку, которую заполняет скрипт.

## Пять способов, по одному

### 1. Разметка schema.org

Ставится ради Google Jobs, читается тривиально, ломаться нечему.
**Не работает: ноль из тридцати.** Проверены и страницы списка, и страницы
отдельных вакансий. Способ остаётся в коде для тех, у кого разметка появится,
но строить на нём нельзя.

### 2. Перебор известных путей к API

Дёшево, быстро, не требует ничего нового.
**Даёт 3 %.** Нашлись `careers.yadro.com/api/vacancies`,
`job.2gis.ru/api/v1/vacancies/`, `join.lesta.team/api/vacancies`; рабочим
оказался один — у 2ГИС не разгадывается адрес вакансии, у Lesta по этому пути
лежат только категории. Настоящие эндпоинты угадать нельзя: они видны лишь в
сетевых запросах страницы.

### 3. Данные, встроенные в HTML

Next.js и Nuxt кладут состояние страницы прямо в разметку, и оно содержит
список вакансий целиком. Читается обычным `curl`, ломается редко.
**Даёт 7 %:** ВТБ и VK. У остальных блок присутствует, но пуст — данные
догружаются отдельно (у Сбера, например, `__NEXT_DATA__` весит 161 байт).

### 4. Рендерить каждую страницу браузером (путь cofinder)

Работает почти везде: браузер выполняет скрипты и видит то же, что человек.
Именно так конкурент читает 140 сайтов.

Цена известна по его же данным: **15 сломанных парсеров из 111**. Каждый сайт —
свои селекторы, редизайн ломает разбор молча. Плюс отдельная инфраструктура:
браузер в проде это память, время и обновления. Наш сервер — PHP и curl, для
этого способа его придётся менять.

### 5. Браузер один раз для разведки, дальше прямое чтение JSON

Браузер открывает карьерный сайт и слушает сетевые запросы. Вакансии всегда
приходят откуда-то — этот адрес и записывается в настройку источника. Дальше
прод читает найденный JSON обычным curl, без браузера.

То же самое делает человек, открывая DevTools, только автоматически и один раз
на компанию.

## Выбор: пятый

Он единственный совмещает охват четвёртого со стоимостью третьего.

- **Охват.** Ищется настоящий эндпоинт, а не угаданный, поэтому работает и там,
  где перебор бессилен, — то есть на тех самых 90 %.
- **Прод не меняется.** Браузер не участвует в обходе: в бою остаётся тот же
  PHP + curl и уже написанный разбор JSON (`cf_json_items`). Источник — строка
  настройки, а не новый код.
- **Ломается реже.** API живёт дольше вёрстки: у cofinder ломаются именно
  парсеры HTML.
- **Разовая работа.** Разведка нужна один раз на компанию; появится новая —
  один прогон.

Четвёртый способ остаётся запасным для сайтов, где данные не приходят
отдельным запросом (например, встроены в поток рендера). Такие записываем как
«не берём» и идём дальше — это правило уже стоит в задании Codex.

## Чего это будет стоить и где подвох

Разведку браузером **нельзя провести из этой сессии**: Chromium в контейнере
ходит только через агентский прокси, а тот требует CONNECT и подменяет
сертификат. Обойти это можно лишь отключением проверки TLS, чего делать нельзя.
Значит прогон разведки — либо на машине владельца, либо на московском сервере,
где браузер выходит в сеть напрямую.

Сам механизм чтения уже готов и проверен на живых данных: `cf_json_items`
разобрал 221 вакансию Yadro, включая поля-списки вроде города.

## Как запустить разведку

Скрипт `scripts/career-discover.mjs` делает пятый способ руками одного запуска.

```bash
node scripts/career-discover.mjs                    # свой список career-sites.tsv
node scripts/career-discover.mjs sites.tsv          # другой список
node scripts/career-discover.mjs https://job.x.ru/  # один сайт
```

Настройки: `DISCOVER_LIMIT` (сколько компаний, 0 — все), `DISCOVER_CONCURRENCY`
(сколько сайтов одновременно, по умолчанию 5), `DISCOVER_SETTLE_MS` (предел
ожидания догрузки), `DISCOVER_OUT` (куда писать результат).

### По каким сайтам ходим

`scripts/career-sites.tsv` — 163 российские компании. Формат строки:
`Название<TAB>адрес`. Имя берётся из файла, а не из хоста: сверять отчёт со
списком придётся глазами, и «Пятёрочка» читается, а `rabota5ka.ru` нет.

Список собран из двух: 62 компании от владельца проекта (ритейл, склад, доставка
и общепит для «Подработки», крупные компании для «Работы») и 111 российских из
каталога cofinder. Пересеклись они всего на десяти хостах.

**Повтором считается одинаковый адрес, а не сайт.** Одинаковых адресов нет ни
одного — это проверяется тестом. А вот разные разделы одного сайта нужны все: у
Самоката корень и `/vacancies-business` ведут к разным наборам вакансий, и взять
один значит потерять другой. Поэтому у шести компаний по две строки, раздел
дописан к названию (`Самокат · /vacancies-business`), чтобы в отчёте они не
сливались. Задвоиться в ленте им не даст отпечаток «компания + должность +
адрес» (`ing_dedupe_key`), по которому приложение прячет двойников.

Сначала я склеил эти шесть по хосту, оставив «более глубокий» адрес, — и это
была ошибка: у Самоката путь `/vacancies-business` сужает выдачу до офиса, то
есть склейка молча выбросила бы все остальные виды работы. Правило «одна строка
на компанию» звучит опрятно, но противоречит задаче брать всё.

### Сужающий фильтр в найденном адресе

Разведка приходит на конкретный раздел, и сайт зовёт своё API уже с фильтром
этого раздела. Записать такой адрес в настройку значит навсегда забрать только
кусок: у X5 это `?business_units=10` — одно подразделение из всех. Поэтому такие
адреса помечаются в отчёте полем `filter_note`, а не принимаются молча.

Разбивка на страницы и язык фильтром не считаются, пустые поля формы тоже:
`?limit=15` у Yadro, `?language=ru` у Авиасейлса и `?search=&minExperience=` у
Lamoda ничего не сужают. Имена вида `pagination[limit]` разбираются и по
внутренней части — так их пишет Strapi, — а `filter[city]` внутренней частью как
раз себя и выдаёт.

Иностранные компании каталога (49: Кипр, Армения, США) не взяты — решение
владельца. Каталог запрошен целиком: `limit=500` отдаёт все 160, параметр
`offset` он игнорирует, дополнительных страниц нет.

Все 62 домена проверены на резолвинг 15.09.2026 — мёртвых нет. Восемь при этом
не открылись обычным `curl` из облачной сессии: кириллический домен, подмена
сертификата агентским прокси и анти-бот, рвущий соединение без браузера. Это
ровно то, ради чего разведка ходит браузером, — списывать такие сайты со счёта
по коду ответа `curl` нельзя.

Что он делает с каждым сайтом: открывает страницу браузером, слушает её сетевые
запросы, находит ответ со списком вакансий, угадывает соответствие полей и
**проверяет адрес вакансии** — подставляет значение в `/vacancy/{id}` и смотрит,
отвечает ли страница. Если ссылка не подтвердилась, так и пишет: без пути к
первоисточнику источник включать нельзя, коннектор такую вакансию отбросит.

На выходе — готовый кусок `connector_config`, который вписывается в источник
`career` в панели. Разбор этого формата уже работает: `cf_json_items` в
`php-proxy/career_feed.php`.

**Запускать не из облачной сессии.** Chromium в контейнере ходит только через
агентский прокси, тот требует CONNECT и подменяет сертификат, а обойти это можно
лишь отключением проверки TLS — чего делать нельзя. Значит запуск на машине
владельца или на московском сервере, где браузер выходит в сеть напрямую.

Чистые функции разбора вынесены в `scripts/career-discover-lib.mjs` и покрыты
`tests/career-discover.test.mjs` — на образцах живых ответов Yadro, VK, Хантфлоу
и 2ГИС. Там же закрыт неочевидный случай: справочники и фильтры в ответе идут
раньше вакансий и выглядят так же (`[{id, name}]`), поэтому берётся самый
длинный список, а не первый попавшийся.

## Прогон по 111 компаниям: что он показал (15.09.2026)

Первый живой прогон разведки. Список — каталог cofinder (наш собственный список
тогда ещё не был слит), поэтому компании почти сплошь айтишные. Итог по статусам:
82 «нет данных», 14 «нет адреса вакансии», 8 ошибок, 7 «готов».

**Из семи «готовых» настоящими вакансиями оказались три.** Остальные четыре —
справочники, и это не случайность, а поломка в нашем коде.

| Компания | Что выбралось | Что это на самом деле |
|---|---|---|
| Ростелеком | 655 записей `{id, name}` | справочник городов |
| МТС | 19 записей с `vacancyCount` | дерево категорий |
| Wildberries | 27 записей с `vacancies_count` | список направлений |
| Золотое Яблоко | 6 записей со ссылками | меню шапки сайта |

Причина: список вакансий выбирался **по длине**, а справочник почти всегда
длиннее. У Ростелекома 655 городов против 382 вакансий — справочник выигрывал
всегда. Исправлено: выбор идёт по признакам вакансии (деньги, место, обязанности,
график, работодатель), а название не считается признаком — оно есть и у города.
Проверено на всех 111 записях прогона: настоящие вакансии набирают 2-6 признаков,
все справочники — 0-1, разделение полное.

Вторая поломка — проверка адреса вакансии. Она смотрела на код ответа, а
одностраничные сайты отдают свою оболочку по любому пути: `job.rt.ru` вернул
26 КБ и на выдуманный `/vacancy/jobtoo-probe-404`. От этого шло и ложное «готов»,
и ложное «нет адреса» у Сбера и Lamoda, где страница вакансии есть. Теперь адрес
проверяется браузером: страница считается настоящей, только если на ней виден
заголовок именно этой вакансии, а на выдуманном адресе его нет.

### Источники, найденные этим прогоном

Проверены вручную запросом — отвечают и отдают вакансии:

| Компания | Адрес данных | Сколько |
|---|---|---|
| Сбер | `rabota.sber.ru/public/app-candidate-public-api-gateway/api/v1/publications` | 50+ |
| Ростелеком | `job.rt.ru/backend/api/vacancies` | 382 |
| МТС | `job.mts.ru/api/v2/catalog/v1/vacancies` | есть «Продавец» |
| Wildberries | `career.rwb.ru/hr-crm-api/api/v2/pub/vacancies?limit=100` | 97 |
| Lamoda | `job.lamoda.ru/api/hr/vacancies/compact` | 10+ |
| Yadro | `careers.yadro.com/api/v1/vacancies/` | 221 |
| Авиасейлс | `vacancies-app.aviasales.ru/api/vacancies?language=ru` | 25 |
| Островок | в HTML, `props.pageProps.vacancies` | 7 |
| Twinby | в HTML | 2 |

Три последних адреса разведка нашла сама, остальные — соседние к найденным:
скрипт видел правильный хост и префикс API, но брал не тот вызов. Важное для
«Подработки»: у Ростелекома в списке «Бригадир монтажников», у МТС «Продавец», у
Wildberries «Повар» — линейный персонал на карьерных сайтах есть, вопреки тому,
что показывал каталог cofinder.

### Про сертификаты

Четыре сайта упали с `ERR_CERT_AUTHORITY_INVALID`: Альфа-Банк, Точка, НСПК,
Positive Technologies. Это не поломка разведки. Российские банки и связанные с
государством организации перешли на сертификаты Минцифры после отзыва западных, а
в Chromium этого корня нет. Чтобы их читать, корневой сертификат Минцифры нужно
добавить в доверенные — решение за владельцем, и это не отключение проверки TLS,
а добавление ещё одного удостоверяющего центра.

## «Работа в России» удалена (15.09.2026)

Миграция 072 этот источник выключала и объясняла, почему не удаляет: строка
хранит историю обходов, а удаление «оборвало бы ссылки у jm_ext_vacancies». Оба
довода оказались неверны — выяснилось при подготовке удаления.

Ссылок по внешнему ключу нет вовсе: `source_id` в `jm_ext_vacancies` объявлен
как `text not null` без `references` (миграция 031), `ext_vacancy_id` в
`jm_partner_applications` — тоже просто текст (045).

Хуже второе. **Выключение не держалось.** `infra/local-web-deploy.sh` при каждом
выкате делал upsert источника с `enabled = true`, то есть следующий же деплой
включал «Работу в России» обратно. Строка из скрипта убрана вместе с источником.
Урок общий: строка, включающая источник на каждом выкате, сильнее любой миграции,
которая его выключает.

Убрано: миграция `073` (вакансии, затем источник — порядок важен, иначе записи
остались бы сиротами навсегда), адаптер `php-proxy/trudvsem.php`, systemd-таймер
полного импорта `jt-trudvsem-import` вместе с `infra/trudvsem-import-loop.sh`,
проверка `tests/trudvsem_infra_test.py` и её вызов в CI. Отклики и их история
(`jm_partner_applications`) НЕ тронуты: внешнего ключа там нет, а человек,
откликнувшийся через нас, имеет право на свою историю.

Что осталось нетронутым намеренно: пользовательское соглашение
(`constants/legal.ts`) по-прежнему упоминает портал в пунктах 2.6, 3.4 и в
политике обработки данных. Это юридический документ с версией, правка которой
может потребовать повторного согласия, — решение за владельцем.

## Девять источников включены (15.09.2026)

Раздел «Работа» был пуст, и виновата была не настройка. `career.php` умел ровно
один вид источника — разметку `JobPosting` на странице. Разбор JSON
(`cf_json_items`) лежал рядом в `career_feed.php`, но в точку входа подключён не
был: найденный разведкой адрес API вписывать было **некуда**.

Подключено, добавлено листание порций, вписаны девять адресов (миграция `074`).
Каждый проверен живым запросом: карта полей разобрала настоящий ответ, вторая
порция отдала другие вакансии, а не те же самые.

| Компания | Сколько у конкурента | Как листается |
|---|---|---|
| Сбер | 1795 | порции по 100, `take`/`skip` |
| МТС | 2553 | страницами по 25, см. оговорку |
| МегаФон | 1013 | страницами по 10 |
| Ростелеком | 382 | страницами по 20 |
| Yadro | 221 | порции по 100 |
| Lamoda | — | порции по 100 |
| Wildberries | 97 | порции по 50 |
| Авиасейлс | 25 | одним куском |
| 2ГИС | 15 | одним куском |

**Оговорка про МТС.** Их API тасует выдачу: два запроса подряд к одной и той же
`page=1` дают разные вакансии — замерено, а не предположено. Полного перебора
это не даёт. Вреда тоже нет: ingest кладёт вакансии с ключом
`(source_id, external_id)`, повторы схлопываются, покрытие накапливается от
обхода к обходу.

**Две ошибки, найденные проверкой, а не догадкой.**

Первая: листание считало **принятые** вакансии, а не сырые записи ответа. У
Wildberries сервер отдаёт 50, после нашего отсева остаётся 31 — и обход решал,
что порция неполная, и бросал работу на первой полусотне из 97. Стало 75
вакансий вместо 31.

Вторая: `rawurlencode` на всём значении превращал `/` в `%2F`, и ссылка Lamoda
`/vacancy/moskva/frontend-vue-developer--1946` ломалась — её slug состоит из
двух сегментов пути. Теперь кодируется каждый сегмент отдельно; подставить чужой
адрес по-прежнему нельзя, `?`, `:` и `#` уходят экранированными.

**Изъян в старой проверке безопасности.** Сторож «непубличный ответ
выбрасывается до разбора» сравнивал позицию разбора с местом, где IP
*читается*, а не где *проверяется*. Разбор, поставленный между чтением и
проверкой, такую версию проходил — мутация оставалась зелёной. Исправлено,
мутация теперь валит проверку.

## Почему 82 сайта из 111 дали «нет данных»

Главная цифра первого прогона. Браузер был, страница открывалась — а списка
вакансий не находилось. Разобрал причины по коду разведки.

**Ранний выход срабатывал на любом списке, а не на вакансиях.** Ожидание
обрывалось, как только в перехваченных ответах появлялось что-то похожее на
список. Справочники приходят раньше вакансий и выглядят так же — то есть
ожидание кончалось ровно перед тем, ради чего оно и было. Моя ошибка: оценку
признаков вакансии я добавил в выбор, но не в условие выхода. Исправлено.

**Страница не прокручивалась.** На карьерных сайтах список сплошь и рядом
подгружается при прокрутке. Стоящая на месте страница не запросит его никогда,
сколько ни жди. Теперь разведка крутит страницу во время ожидания, а если
списка так и нет — уходит в самый низ и ждёт ещё.

Обе правки бьют ровно в те 82 сайта. Насколько именно — скажет прогон; обещать
цифру до замера не буду.

## Битые ссылки: Сбер вёл на 404 (16.09.2026)

Владелец открыл вакансию Сбера в приложении и увидел страницу «мы искали вдоль
и поперёк, но не нашли страницу по вашему запросу». Проверил — так и есть:
`rabota.sber.ru/vacancy/{publicationId}` отдаёт HTTP 404.

Это нарушение нашего же правила, записанного в `cf_json_items`: без пути к
первоисточнику вакансию не берём, иначе человек едет туда, где его не ждут.
Правило было, а проверки адреса при заведении источника — не было.

После этого проверил **все девять** одинаково: открываю ссылку на настоящую
вакансию и ссылку той же формы с выдуманным хвостом. Если ответы не отличаются,
адрес ничего не доказывает.

| Источник | Что вышло |
|---|---|
| Yadro, МТС, Авиасейлс, МегаФон | открывается, на странице виден заголовок именно этой вакансии |
| Ростелеком | заголовок рисует скрипт, но выдуманный адрес от настоящего отличает |
| Lamoda | был 404: верный путь `/vacancies/{slug}`, а не `/vacancy/{slug}` — исправлено |
| Wildberries | отдаёт одну оболочку на любой путь; проверен иначе (см. ниже) |
| **Сбер** | **404, замены нет — убран** |
| **2ГИС** | **404 по всем формам, slug отсутствует — убран** |

У Сбера перебрал `/vacancies/`, `/publication/`, `/search/vacancy/` — везде 404.
Параметр `?publicationId=` страница игнорирует: ответ байт в байт равен голому
`/search`. Поля со ссылкой в их ответе нет вовсе.

Wildberries проверен через их же API: `/pub/vacancies/34863` возвращает
вакансию, `/pub/vacancies/99999999` — 404. Номер настоящий, значит у страницы
есть маршрут, даже если оболочка одинаковая.

### Две ошибки разбора, найденные заодно

**Wildberries: заголовком бралась категория.** В ленту шло «Повар» вместо
«пекарь-тандырщик» — у всех вакансий направления одно и то же слово. Настоящее
название в `name`, категория в `direction_role_title`. Заодно вакансий стало 50
вместо 31: часть отсеивалась как «без заголовка».

**МТС: терялся город.** Он лежит в `region` как `{id, documentId, title}`, а
разбор искал только `name` и `@value`. Добавлен `title`.

Название компании теперь задаётся постоянной строкой (`company_const`): в
ответах его чаще всего нет, а в карточке «Wildberries» читается, «Карьерные
страницы» — нет.

## Третий вид источника: вакансии обычными ссылками (16.09.2026)

Замер по 111 карьерным сайтам, ради которого всё и затевалось:

| Что лежит на странице | Компаний |
|---|---|
| вакансии обычными ссылками `<a href="/vacancy/...">Должность</a>` | 24 |
| состояние в разметке (`__NEXT_DATA__`, `__NUXT__`) | 8 |
| ничего — рисует скрипт, нужен браузер | 65 |
| страница не открылась | 14 |

Разметку `JobPosting`, под которую изначально писался коннектор, держат **двое**
из 111. Поэтому добавлен третий вид — `mode: html_links`: берём ссылки, у
которых в адресе есть заданный кусок (`/vacancy/`), а текст ссылки считаем
должностью.

### Чего это стоило по качеству

Наивная версия дала 47 компаний, но в ленту полезли «Все города» (СИБУР),
«Кандидатам» (Контур), «Подробнее» (Протей), «согласие на обработку
персональных данных» (Lesta), а заголовки склеивались без пробелов:
«Бизнес-ассистентДзенМосква» у VK, «ClickhouseFlinkJavaKafkapython
Java-разработчик» у Иви. Такая лента хуже пустой.

Два правила это чинят. **Ссылка должна вести на конкретную вакансию**: после
`/vacancy/` обязан быть хвост, иначе это ссылка на раздел. **Заголовок берётся
из вложенного `h1..h6` или элемента с `title`/`name` в классе**, и только если
их нет — из всего текста, но с пробелом на каждой границе элемента.

### Проверка ссылок — та же, что после Сбера

Открываю ссылку на настоящую вакансию и ссылку той же формы с выдуманным
хвостом. Засчитано, только если настоящая страница показывает заголовок именно
этой вакансии, а выдуманная — нет, отвечает ошибкой или заметно отличается
длиной.

Строгость стоила двух третей улова: ссылки нашлись у 44 компаний,
**подтвердились у 19**. Остальные 25 отдают одну страницу на любой адрес, и
доказать, что ссылка ведёт к вакансии, нечем.

Отдельно стоит записать: первая версия проверки принимала всё подряд. Контроль
на заведомо выдуманном адресе Яндекса показал «приняла» — заголовок нашёлся на
странице-заглушке, потому что там выведен список вакансий. Без этого контроля я
завёл бы 44 источника вместо 19 и снова привёл бы людей на пустые страницы.

Итого источников стало 25: 7 по JSON и 18 по ссылкам.

## Проверка на проде нашла то, чего не нашли тесты (16.09.2026)

Выкатил 25 источников и дёрнул коннектор на живом сервере — не «должно
работать», а посмотреть, что он реально отдаёт. Три поломки, две из них не
поймал бы ни один тест на выдуманных данных.

**Яндекс убран.** Все 68 его «вакансий» оказались ссылками вида
`/jobs/vacancies?profession=backend` — это фильтры каталога, а заголовком шло
«Разработка», «Аналитика». Настоящих ссылок на вакансии в разметке нет вовсе:
там только навигация и иконки.

Строгая проверка адреса его пропустила — и вот почему это важно. Она проверяла,
что ссылка **открывается** и показывает заголовок. Фильтр каталога открывается
и заголовок показывает. Проверка отвечала на вопрос «работает ли ссылка», а
нужен был вопрос «вакансия ли за ней». Теперь код отсекает такие ссылки сам:
если хвост после `/vacancy/` сам является словом-разделом (`vacancies`, `jobs`,
`search`, `all`), это не вакансия.

**IBS отдавал 301.** Адрес исправлен на `https://ibs.ru/career/jobs/` со
слэшем: без него сервер редиректит на `http://ibs.ru/career/jobs/` — по http, а
мы ходим только по https и за редиректом не идём. Это защита, а не недосмотр:
редирект увёл бы на адрес, который проверку не проходил.

**Название компании перебивалось полем из ответа.** В карточке Ростелекома
стояло «Технический блок» — это направление, а не работодатель. Постоянное
название теперь ставится ПОСЛЕ разбора полей. У источников по ссылкам название
не подставлялось вовсе: код читал `company`, а настройка писала `company_const`.

Источников осталось 24.

## Прогон разведки по нашему списку: Пятёрочка и Перекрёсток (16.09.2026)

170 страниц, из них 12 «готов», 10 «нет адреса вакансии», 138 «нет данных»,
10 ошибок. Прокрутка и починенный ранний выход помогли, но не переломили:
большинство карьерных сайтов подгружает вакансии только после нажатия на
фильтр, а разведка кнопки не жмёт.

Зато две находки стоят всего остального.

| Источник | Вакансий | Что там |
|---|---|---|
| Пятёрочка | **33 905** | «Продавец-кассир», 52 000 ₽, с адресом магазина |
| Перекрёсток | **3 998** | «Оператор склада» |

Для сравнения: весь каталог конкурента cofinder — 12 059 вакансий по 111
компаниям. Одна Пятёрочка втрое больше, и это линейный персонал, которого в
разделе «Подработка» не было вовсе.

Такой адрес перебором типовых путей не найти: `/api/vacancy/hire-request` у
Пятёрочки, тот же backend через `/gw/` у Перекрёстка. Ровно ради этого разведка
и писалась.

**Пометка про фильтр окупилась сразу.** Разведка нашла адрес с городом внутри:
`radius=30000, cityId=692434fa…` — и пометила его полем `filter_note`. С
фильтром отдавалось 10 вакансий вместо 33 905. В настройке фильтра нет.

**Адрес вакансии проверен.** У Перекрёстка прямо: `/vacancies/{id}` отдаёт 200 с
названием должности, выдуманный id — 404. У Пятёрочки страница рисуется
скриптом и оболочка одинаковая, поэтому проверено через их же API:
`/api/vacancy/hire-request/{id}` возвращает вакансию, `/000000000000000000000000`
— 404. Соседние пути `/vacancies/` и `/vakansii/` дают 404, значит сайт их
различает.

### Что из того же прогона не взято и почему

| Источник | Причина |
|---|---|
| METRO, Золотое Яблоко, Т-Банк | их API отвечает 405/404 на GET, нужен POST — коннектор ходит только GET |
| Селектел, Globus IT, VK | список читается, но адрес вакансии подобрать не удалось: все формы дают 404 |
| Сбер | снова без адреса вакансии |

## Пробел, который я закрыл последним: свои 62 сайта (16.09.2026)

Разбор ссылок из HTML я прогонял только по 111 компаниям каталога cofinder, а
по 62 сайтам из списка владельца — **ни разу**. Там-то линейный персонал и
живёт.

| Источник | Вакансий | Кто |
|---|---|---|
| ВкусВилл | 14 | Продавец, Автокурьер, Велокурьер, Сборщик интернет-заказов, работа студентам от 4 часов |
| CDEK | 19 | Курьер на легковом автомобиле, Кладовщик, менеджеры |

У CDEK путь указан точнее — `/vacancies/item/`, а не `/vacancies/`: их ссылки
вида `/vacancies/item/13145`, и по короткому пути в улов попадали бы разделы.

### Чего этот прогон не дал, и это важнее самой находки

Яндекс снова «подтвердился» проверкой: 51 ссылка, все открываются, заголовки
на месте. А за ними — «Как попасть в Яндекс», «Атмосфера», «Бенефиты». Это
навигация по сайту.

Значит проверка «ссылка открывается и показывает заголовок» **принципиально не
отличает** вакансию от страницы о компании: и то и другое существует и содержит
свой заголовок. Отличить может только человек, посмотрев на список должностей.

Отсюда правило, которое теперь записано: **прежде чем завести источник по
ссылкам, смотрю глазами первые несколько заголовков**. Так отсеян Яндекс и
HTML-вариант МегаФона, у которого половина ссылок — разделы каталога вида
`/vacancy/all/...`.

### Про добычу адресов из JS-бандлов

Попробовал достать адреса API прямо из скриптов сайта: у одностраничного
приложения путь к своему API записан в коде, даже если список грузится после
нажатия фильтра. Прошёл 148 сайтов, вытащил бандлы, поискал пути со словами
vacancy/job/career.

Результат скромный: кандидаты нашлись у 7 сайтов из 148. Причина в том, что
пути чаще собираются из кусков в рантайме, а не лежат строкой. Способ рабочий,
но не тот рычаг, на который я рассчитывал. Записываю, чтобы не пробовать
второй раз.

## Все 62 компании владельца: где мы на самом деле (16.09.2026)

Владелец попросил довести до всех своих 62 компаний. Посчитал точно, а не на
глаз: работало **7**, не хватало **55**.

Добавлено этим заходом ещё три: Норникель (40 — «Бурильщик шпуров, рудник
Скалистый», «Бетонщик 5 разряда, Курейская ГЭС, вахта»), Лемана ПРО (10 — с
зарплатой прямо в заголовке) и Мария-Ра (14 — «Администратор», «Сборщик
товара»). Итого из его списка работает **10**.

### Правило проверки адреса пришлось уточнить

Прежнее требовало, чтобы на странице вакансии был виден её заголовок. Но у
Леманы и Марии-Ра страница рисуется скриптом, заголовка в разметке нет — а
выдуманный адрес той же формы отвечает 404. Значит сайт настоящий адрес от
выдуманного **отличает**, и ссылка ведёт куда надо. У Норникеля сошлось и то и
другое.

### Почему остальные 52 не берутся

| Препятствие | Компаний | Кто |
|---|---|---|
| страница пустая, вакансии рисует скрипт | ~30 | Магнит, ДИКСИ, Монетка, Детский мир, РИВ ГОШ, Улыбка радуги, Петрович, Hoff, Befree |
| сайт не пускает без браузера (401/403/451) | 11 | Спортмастер, Деловые Линии, DNS, HENDERSON, kari, Балтика, ROSTIC'S, DPD, Черкизово, Аэрофлот, Почта России |
| соединение рвётся или домен не отвечает | 9 | Лента, Глобус, ПЭК, РЖД, Подружка, Вкусно — и точка, Шоколадница, SPAR, Ярче! |
| API требует POST | 3 | METRO, Золотое Яблоко, Т-Банк |
| ссылки есть, но за ними не вакансии | 3 | СИБУР (города), РУСАЛ (кадровый резерв), Северсталь (адрес не доказать) |

Честный вывод: **разбором ссылок и перебором путей эти 52 не взять**. Нужны две
разные работы.

1. **POST-режим коннектора** — откроет METRO, Золотое Яблоко, Т-Банк. Из них
   METRO это линейный персонал. Работа небольшая и полностью в нашей власти.
2. **Разведка, которая жмёт кнопки.** Тридцать сайтов отдают вакансии только
   после нажатия на фильтр или кнопку «показать вакансии». Сейчас разведка
   открывает страницу, прокручивает и ждёт — этого мало. Нужно научить её
   кликать по элементам со словами «вакансии», «показать», «найти».

Остальные — 401/403 и обрывы соединения — упираются в анти-бот, и это отдельный
разговор: тут честнее договариваться с работодателем, чем обходить защиту.

## Два рычага к остальным компаниям (16.09.2026)

### POST-режим коннектора: METRO, 292 вакансии

Их API на GET отвечает 405, поэтому источник висел в «не взято» с самого
начала. Коннектор теперь умеет POST: метод и тело задаются в настройке
источника рядом с адресом (`method`, `body`).

Про безопасность, потому что POST — новая возможность у точки входа, открытой
наружу. Тело берётся **только** из настройки, которую пишет администратор; из
запроса в него не попадает ничего. Адрес проходит ту же проверку: публичный
HTTPS, без служебных сетей, без переходов по редиректу, с закреплённым
DNS-адресом. POST идёт тем же curl-вызовом и под теми же сторожами. Обе мутации
(POST всегда включён; тело берётся из запроса) валят проверки.

Адрес вакансии проверен строго: `/vacancy/21580` отдаёт страницу с названием
должности, `/vacancy/jobtoo-probe-404` — ту же оболочку **без** него. Листание
через `line`/`linesPerPage`, вторая порция даёт другие номера.

Золотое Яблоко и Т-Банк тем же способом не открылись: у первого поле `filters`
ждёт список особого вида, у второго запрос принимается, но возвращает ноль
вакансий без правильных фильтров. Подобрать вслепую не вышло, и гадать дальше
я не стал.

### Разведка научилась жать кнопки

Главная причина «нет данных» у тридцати сайтов: вакансии появляются **только**
после нажатия «Показать вакансии», «Все вакансии» или выбора города. Открытая и
прокрученная страница у них пуста, сколько ни жди.

Теперь после прокрутки разведка нажимает — осторожно и по правилам:

- только по видимым кнопкам и ссылкам-кнопкам;
- только если надпись обещает список вакансий (`looksClickable`, вынесена в
  библиотеку и покрыта тестами: «Показать вакансии» да, «Отправить резюме» нет);
- текст длиннее сорока знаков не кнопка, а абзац — не трогаем;
- не больше четырёх нажатий на сайт, и останавливаемся, как только список
  появился;
- ничего не отправляем и не заполняем: нажатие на кнопку каталога не меняет
  чужих данных.

Мутации «нажимать на всё подряд» и «снять предел длины» валят тесты.

## Прогон без нажатий: моя ошибка в порядке работ (16.09.2026)

Владелец запустил разведку сразу после того, как я дописал нажатия, — но код
нажатий лежал в непролитых коммитах, а workflow берёт скрипт из `main`.
Прогон повторил прошлый результат, и это целиком моя недоработка: надо было
сказать «сначала сольём, потом запускайте».

Проверяется одной строкой, и теперь я проверяю её перед тем, как звать на
прогон:

```
git show origin/main:scripts/career-discover.mjs | grep -c looksClickable
```

### Что прогон всё же дал: третий способ чтения

В отчёте нашлись сайты, у которых вакансии лежат в состоянии страницы, а не
приходят отдельным запросом. Добавлен режим `embedded`: Next.js кладёт данные в
`<script id="__NEXT_DATA__">`, Nuxt — в `window.__NUXT__`; для нас это тот же
JSON, только внутри HTML, и разбирает его тот же `cf_json_items`.

Взят **VK**, 25 вакансий. Адрес проверен строго: `/vacancy/52666` отдаёт 200 с
названием «Бизнес-ассистент», `/vacancy/jobtoo-probe-404` — 404.

### Два источника, которые читаются, но не взяты

**ВТБ**, 20 вакансий. Их карьерная страница — витрина hh: ссылка на вакансию
ведёт на `hh.ru/vacancy/136824761`, своей страницы вакансии у сайта нет.
Источник рабочий, но человек уедет к агрегатору, от которых владелец решил
уйти. Решение за ним, поэтому спрашиваю, а не добавляю молча.

**Кофемания**, 18 вакансий (Хостес, Бармен-Официант, Специалист по сборке
заказов — как раз общепит). Отдельной страницы вакансии нет вовсе: в данных
лежит `anchor` вида «hostess», а якоря на странице рисует скрипт. Все угаданные
пути отвечают 404, проверить нечем.

## Почему новых вакансий не было в ленте (16.09.2026)

Владелец спросил «и где эти наши вакансии новые?». Я пошёл смотреть не отчёты, а
прод — и нашёл две вещи, из-за которых половина работы не доезжала до людей.

### Одна страница держала все тридцать три

Обход раздела «Работа» стоял намертво. Устройство такое: `career.php` отдаёт по
одному работодателю за запрос и говорит `next_url` на следующего, а `ingest.php`
листает. И вот что делает приёмник, получив ошибку:

- прекращает заход **целиком**, а не по этому адресу;
- оставляет в контрольной точке тот адрес, на котором споткнулся;
- стирает контрольную точку **только после полного успешного круга**.

Значит один недоступный работодатель встаёт поперёк всех остальных навсегда:
следующий заход упирается в тот же адрес, и так по кругу. На проде обход умирал
на самом первом адресе — Пятёрочке, — и до остальных тридцати двух не доходил
вовсе. В ленте лежало то, что успело записаться раньше.

Проверено прямым обходом прода по одному адресу, а не по логам: из тридцати
двух адресов отвечают ЧЕТЫРНАДЦАТЬ, и первые четырнадцать позиций списка — те
самые, которые я добавлял последними, — почти сплошь ошибки. (В первой редакции
этой записи стояло «восемнадцать» — я ошибся в счёте, пересчитал по сохранённым
ответам.)

**Починено.** Упавший адрес — теперь не ошибка обхода, а пропуск: переходим к
следующей компании (`cf_next_step`). Порции упавшего не дочитываем: не ответил
адрес — не ответит и его вторая страница.

Второе следствие пришлось закрыть отдельно. Раз круг теперь доходит до конца,
приёмник счёл бы его полным и **погасил** вакансии тех, кого не увидел: один
403 у работодателя стирал бы его вакансии из ленты. Поэтому ответ с пропуском
помечается `partial`, и по этой пометке гашение на таком круге не делается.
В статусе источника это видно как «ок, не весь».

Семь мутаций проверены поимённо: вернуть обрыв на упавшем адресе, дочитывать
порции упавшего, гасить после неполного круга, потерять пометку на контрольной
точке, вернуть `cf_fail` на недоступной странице, снять второй рубеж по
непубличному адресу, снять проверку разрешения адреса. Все семь валят проверки.

### Три адреса ломались на редиректе

ВкусВилл, Норникель и Лемана ПРО отвечали 301/307. За редиректом мы не ходим
намеренно — он увёл бы на адрес, который проверку не проходил. Адреса исправлены
на конечные (миграция `084`).

Про Леману честно: её редирект **сужает** выдачу до Москвы и области. Это выбор
самого сайта, без региона он не отдаёт ничего. Взято с сужением и записано
здесь, чтобы через месяц не гадать, почему у Леманы только московские смены.

### Чего я НЕ могу починить кодом, и это надо знать

Восемь работодателей отвечают нашему серверу 403, 404, 405 и 477 — Пятёрочка,
Перекрёсток, Ростелеком, Wildberries, МТС, Lamoda, Yadro, Aviasales, CDEK. Те же
самые адреса из другого места отвечают 200. Разница — адрес, с которого пришли:
это защита от ботов, которая закрывается по репутации IP.

Тут важно признать свою ошибку в методе. Я проверял эти источники из разведки
(GitHub Actions) и из сессии — то есть **не с того сервера, который потом по ним
ходит**. «Проверено вызовом» в прошлых записях означало не то, что я имел в виду.
Правило на будущее: источник считается рабочим, когда по нему сходил прод.

Скорее всего, блокировку мы сами и заработали: застрявший обход долбил один и
тот же адрес каждый заход. Пропуск вместо обрыва убирает и эту причину — теперь
каждый адрес трогается не чаще раза за круг.

Обходить защиту не предлагаю и не буду: честный путь тут — договариваться с
работодателем о доступе. Это решение владельца, не моё.

### В карточках стояло направление вместо работодателя

«Технический блок» вместо Ростелекома, «IT и разработка продуктов» вместо Yadro,
«Розничная сеть» вместо Lamoda. Настройка была исправлена давно
(`company_const`), и код берёт постоянное название после разбора полей — но эти
строки записались раньше и с тех пор не переписывались, потому что обход до них
не доходил. Миграция `084` чинит данные, не дожидаясь круга.

### Обратная сторона пощады, и чем она закрыта

Пометка `partial` гасить не даёт. Но пока хоть один работодатель отвечает 403,
круг ВСЕГДА неполный — значит не гасится ничего, и заполненная вакансия висит в
ленте вечно. Человек открывает её и получает 404: ровно то, за что владелец
справедливо ругал Сбера.

Поэтому предел по возрасту: вакансию, которой не видно неделю, гасим и на
неполном круге. Неделя — это семь заходов подряд, за которые её не отдал ни
сайт, ни кэш. Свежие не страдают, у них отметка сегодняшняя.

На полном круге граница прежняя — начало круга.

## Две поломки в ленте, найденные владельцем (16.09.2026)

Владелец показал два экрана: в фильтре «Показать 4», нажимаешь — пусто; и
карточка МТС с надписью «Источник не прислал описания», хотя на сайте текст
есть. Обе настоящие, обе посчитаны по базе.

### Отпечаток без места склеивал разные вакансии

`ing_dedupe_key` сводит одинаковые предложения от разных партнёров, иначе лента
выглядит спамом. Собирается из компании, должности, метро, адреса, даты и
начала смены. У постоянной вакансии даты и времени нет, а METRO не присылает НИ
адреса, НИ метро — и отпечаток вырождается в «компания + должность».

Счёт по проду: у METRO 294 строки, уникальных отпечатков 100. Двадцать три
разных «Продавца-кассира» в разных магазинах были одной карточкой. Всего по
карьерному источнику терялось 240 вакансий из 1583.

Отсюда и «Показать 4» при пустом списке: счётчик спрашивает сервер сейчас, а
список показывает то, что телефон скачал раньше, и на вырожденных отпечатках
эта разница вылезла наружу.

Починено: без адреса и метро различаем постоянную вакансию по её собственному
адресу. Для смен правило не включается — у них место есть всегда, иначе смену
не показать ни на карте, ни в ленте, и сведение партнёров не страдает.

### Описания не было у 725 вакансий из 1583

Причина простая: в списке описания почти нигде нет. У страниц со ссылками его
не может быть в принципе, а API отдают короткую карточку — у МТС поле
`description` в ответе есть, но пустое у всех двадцати пяти вакансий страницы.
Мапить было нечего.

Значит описание надо брать со страницы самой вакансии. Сборщик теперь так и
делает: если источник описания не прислал, открывает страницу вакансии и
забирает текст. Сторожа те же, что у остального обхода: публичный HTTPS, без
служебных сетей, без переходов по редиректу, с закреплённым DNS-адресом и
повторной проверкой адреса, к которому фактически пришли. Бюджет общий с
обходом: упёрлись — дочитаем на следующем заходе, ingest.php нудят раз в минуту.

Порядок разбора: разметка `JobPosting`, потом `meta description`/`og:description`
(их пишут под выдачу поисковика, то есть человеку), и лишь потом самый длинный
кусок текста.

Про последний путь отдельно, потому что он чуть не уехал в ленту мусором.
Первый замер по 23 работодателям принёс у BSL политику конфиденциальности, а у
kokos group — форму отклика. Поэтому выбрасываем меню, подвал и формы, берём
только смысловые контейнеры и отсеиваем текст про обработку данных и про
отклик. Пустое описание честнее чужой политики конфиденциальности.

Итог того же замера после правок: 20 работодателей из 23 отдают настоящий
текст. Три не отдают — и все три правильно: у BSL на странице только политика,
у kokos только форма, у Wildberries страница пустая (её рисует скрипт).

### Поправка к числу выше

В первой редакции этой записи и в коммите стояло «описания не было у 858 из
1583». Это ровно наоборот: 858 вакансий описание ИМЕЛИ (Ростелеком 361, METRO
277, Yadro 220), а не имели его 725. Я сложил столбец и подписал его не тем
словом. Пересчитано запросом, исправлено здесь и в комментариях кода; в уже
слитом коммите остаётся как есть, поэтому поправка записана отдельно.

Суть правки от этого не меняется: описание живёт на странице вакансии, и
сборщик теперь туда ходит.
