# Переезд на российскую инфраструктуру (Timeweb Cloud)

План переноса JobToo с зарубежной облачной инфраструктуры на сервер в России.

Составлен 14.08.2026. Обновляется по ходу дела — отмечайте сделанное.

---

## Зачем

Ст. 18.5 152-ФЗ: база с персональными данными россиян должна физически
находиться в РФ. Сейчас ФИО, телефоны, метро, переписка и фото 420 человек
лежат в Supabase за пределами страны.

---

## Что именно переезжает

Из кода видно, что от Supabase используются **четыре** сервиса, а не только база:

| Сервис | Где используется | Куда переезжает |
|---|---|---|
| PostgreSQL | вся база `jm_*` | контейнер `postgres` |
| PostgREST (`/rest/v1`) | `php-proxy/db.php`, дашборд | контейнер `postgrest` |
| Realtime (`postgres_changes`) | 12 подписок в `contexts/AppContext.tsx` | контейнер `realtime` |
| Storage (бакет `avatars`) | аватары, фото и аудио в чате | контейнер `storage-api` |

Supabase **Auth не используется** — вход свой, bcrypt в `php-proxy/db.php`.
Это упрощает переезд: контейнер `gotrue` не нужен.

Поэтому ставим **self-hosted Supabase** — тот же API, что и облачный.
В коде меняются только адрес и ключи, логика не трогается.

`php-proxy` на Reg.ru остаётся где есть — он уже в России.

---

## Как я буду настраивать, если у меня нет SSH

Проверено: из моей среды открыт HTTPS, но **порт 22 закрыт**. Поэтому:

1. **Создание сервера** — через API Timeweb (`https://api.timeweb.cloud/api/v1`).
2. **Первичная настройка** — через `cloud-init`: скрипт, который сервер
   выполняет сам при первом запуске. Ставит Docker, тянет конфигурацию,
   поднимает контейнеры, выпускает TLS-сертификат. Руками не вводится ничего.
3. **Дальнейшее управление** — через git: сервер по расписанию подтягивает
   этот репозиторий и применяет изменения. Я вношу правку коммитом, сервер
   её забирает. Никакого SSH, и всё видно в истории.
4. **Аварийный доступ** — веб-консоль в личном кабинете Timeweb, на случай
   если что-то сломается до того, как поднимется сеть. Это уже вы.

---

## Этап 0. Что делаете вы (≈20 минут)

Это единственное, что требуется от вас руками.

- [ ] Зарегистрироваться на `timeweb.cloud`, пополнить баланс (~3000 ₽ на старт)
- [ ] Личный кабинет → **API и Terraform** → создать токен
- [ ] Передать токен мне

Токен можно отозвать в любой момент той же кнопкой — после настройки он мне
больше не нужен.

---

## Этап 1. Сервер

Делаю по API, участие не требуется.

- [ ] Создать сервер: **Ubuntu 24.04, 4 vCPU / 8 ГБ RAM / 80 ГБ NVMe**
- [ ] Swap 4 ГБ (сборка Next.js и Realtime любят память)
- [ ] Файрвол: наружу только 80 и 443; 5432 и 22 закрыты
- [ ] `cloud-init`: docker, docker compose, git, ufw, fail2ban, certbot, cron
- [ ] Ключ SSH сгенерировать, приватную часть передать вам для аварийного входа

**Почему 8 ГБ, а не 4:** одновременно работают Postgres, Realtime, Storage,
PostgREST, nginx и сборка дашборда. На 4 ГБ это встанет в момент сборки.

---

## Этап 2. Supabase на своём сервере

- [ ] `docker-compose.yml`: postgres, postgrest, realtime, storage-api, imgproxy, kong
- [ ] **Не поднимаем**: gotrue (свой вход), edge-runtime (см. этап 6), studio
      (не нужен, есть свой дашборд)
- [ ] Сгенерировать JWT-секрет и ключи `anon` / `service_role`
- [ ] Postgres: `wal_level=logical` и слот репликации — без этого Realtime не работает
- [ ] Накатить все 23 миграции из `supabase/migrations/`
- [ ] nginx + Let's Encrypt на поддомен `db.jobtoo.ru`
- [ ] A-запись `db.jobtoo.ru` → IP сервера (в панели Reg.ru или по их API)

---

## Этап 3. Перенос данных

Данные тянет сам новый сервер — у него, в отличие от меня, открыты все порты.

- [ ] Взять в панели Supabase строку подключения (Settings → Database)
- [ ] `pg_dump` схемы и данных всех таблиц `jm_*` → восстановить на новом сервере
- [ ] Перекачать файлы бакета `avatars` (аватары, фото и аудио чатов)
- [ ] Сверить количество строк по каждой таблице: было / стало
- [ ] Проверить последовательности и индексы

**Сверка обязательна.** Молча потерянная таблица обнаружится через неделю,
когда человек не увидит свою переписку.

---

## Этап 4. Переключение

Самый рискованный шаг. Делать в окно с малой нагрузкой (ночь по Москве).

- [ ] Поменять `SB_URL` и ключи: `php-proxy/db.php:25`, `admin.php:16`, `tg.php:28`
- [ ] Поменять в `lib/supabase.ts`, `dashboard/lib/supabase.ts`, `serverSupabase.ts`
- [ ] Повторить дамп непосредственно перед переключением (данные набежали)
- [ ] Выложить php-proxy, проверить: вход, лента, чат, отклик, загрузка фото
- [ ] Проверить Realtime: сообщение в чате должно долетать без обновления
- [x] EAS Update для приложения — уходит сам на каждый коммит в main
- [x] Приложение уведено с облака 15.08: адрес и ключ `anon` переведены на
      свой сервер. До этого ключ не был задан вовсе, и живые обновления с
      загрузкой файлов не работали ни там, ни там — выяснилось при проверке
      самой сборки, а не по жалобам.
- [x] Живые обновления перестроены: вместо подписки на таблицы, которой
      нужны права на чтение, сервер шлёт сигнал без данных. Ключ из
      приложения не открывает ничего.
- [x] Загрузка файлов переведена через прокси — по той же причине.
- [x] Рассылка из дашборда переписана с облачной функции на PHP.

**Откат:** старый Supabase не выключаем **две недели**. Если что-то пошло не
так — возвращаем прежний `SB_URL` одним коммитом.

> **15.08, вечером: облачный проект остановлен.** Не удалён — приостановлен,
> чтобы было куда заглянуть, если через неделю обнаружится непереехавшая
> мелочь. Откатиться на него уже нельзя: там данные позавчерашние, а все
> новые отклики, сообщения и регистрации идут сюда.
>
> Перед остановкой пришлось убрать три живые зависимости, каждая из которых
> сломалась бы молча: миграции всё это время применялись **в облако** (в тот
> день трижды), выкладка облачных функций осталась от переписанного
> `push-notify`, а нативные сборки брали адрес облака из `eas.json`.
> Ни одна из трёх не всплыла бы до первой правки схемы или первой установки
> приложения.

---

## Этап 4б. Переезд домена jobtoo.ru

Пока имя `jobtoo.ru` указывает на Reg.ru, каждый запрос приложения делает
лишний прыжок: телефон → хостинг → Москва. Прокси уже переехал и проверен,
осталось перевести само имя.

### Порядок, и почему именно такой

Сертификат нельзя выпустить на имя, которое указывает не на нас: Let's
Encrypt приходит проверять по адресу из DNS. А поменять запись раньше
сертификата — значит на несколько минут оставить без https и приложение,
и вебхук бота.

Первая попытка была выпустить заранее: пусть хостинг перенаправляет
проверку на московский адрес. **Не работает.** Reg.ru обслуживает
`/.well-known/acme-challenge/` не из папки сайта — файл, положенный туда
по FTP, отдаётся как 404. Правило не достаёт до него ни из корневого
`.htaccess` (правила перезаписи в подкаталоги не наследуются), ни из
самого каталога. Проверено маячком, а не предположением.

Поэтому иначе: сервер выпускает сертификат сам, как только запись
поменяли, — и успевает раньше людей. Спрашивает он **ответственные за
домен серверы напрямую**, а не обычным способом: TTL записи час, и
обычный преобразователь ещё час отдавал бы прежний адрес. У ответственных
же новое значение появляется в ту же секунду, как его сохранили, а до
людей доходит в течение часа. Этого зазора хватает, чтобы сертификат
встал на место до первого посетителя.

- [x] Сайт приезжает на сервер выпуском с меткой `web` — FTP и ssh туда нет
- [x] Секреты (токен бота, пароль дашборда) приезжают вместе с выкладкой
- [x] Сервер каждую минуту спрашивает ответственные серверы домена и, увидев
      свой адрес, немедленно выпускает сертификат
- [x] Вписать в настройки репозитория то, что лежит на странице за паролем
      панели (`JT_DEPLOY_TOKEN`, `SB_SERVICE_KEY`)
- [ ] Дождаться доставки токена бота и пароля дашборда, проверить по отчёту
- [x] Поменять записи в Reg.ru — 15.08, около 15:27
- [x] Сертификат выпущен в 15:33:25, сайт проверен по тому, чего на хостинге
      нет вовсе: `/rest/v1/`, `/storage/v1/`, `/realtime/v1/api/`, `/api/admin.php`

**Перерыв составил около шести минут** — между сменой записей и выпуском
сертификата. Задело тех, чей преобразователь успел переключиться в это окно;
у остальных TTL держал прежний адрес, и они ничего не заметили.

Его можно было избежать, и стоит записать как: сертификат надо было
выпустить заранее. Не вышло по причине, выясненной здесь же выше — Reg.ru
не отдаёт каталог проверки Let's Encrypt из папки сайта. Оставшийся способ
— проверка через запись в DNS — требовал бы двух заходов в панель вместо
одного, и я выбрал более короткий путь, недооценив длину окна.

**Вебхук бота на момент переезда шёл через внешний relay.** Это было
проверено запросом `getWebhookInfo`. Исходящие сообщения бота — рассылки
и посты в группу — шли из `db.php` на московском сервере. Главным
препятствием оказался сетевой путь: `api.telegram.org` доступен с сервера
только по IPv6, а IPv4-адрес недоступен.

### Что менять в DNS

| Запись | Было | Стало |
|---|---|---|
| `A jobtoo.ru` | 31.31.196.219 | 147.45.184.99 |
| `A www.jobtoo.ru` | 31.31.196.219 | 147.45.184.99 |
| `AAAA jobtoo.ru` | 2a00:f940:2:2:1:1:0:174 | 2a03:6f00:a::1:ba1f |

**Почту не трогаем.** Она живёт на записях `MX` (`mx1/mx2.hosting.reg.ru`),
а они отдельные от `A`. Отправку подтверждает `TXT`-запись SPF, где адрес
хостинга и его почтовые серверы перечислены явно, — она тоже остаётся.

`AAAA` менять обязательно. Сперва я советовал её удалить, считая, что у
московского сервера IPv6 нет. Это неверно: адрес есть, и именно по IPv6
сервер ходит в Telegram — IPv4 туда закрыт. Поэтому запись не убираем, а
переводим, а nginx слушает оба стека. Оставить её указывающей на хостинг
нельзя: у кого IPv6 работает — в мобильных сетях это половина — браузер
пошёл бы по ней на старое место, и получилось бы «у меня по-старому, а у
соседа новое».

**Откат:** вернуть прежние адреса. Выкладка на Reg.ru продолжает работать,
там лежит рабочая копия сайта и прокси, — возврат записи всё чинит.

---

## Этап 5. Дашборд на российском сервере

**Завершено.** Панель работает на `admin.jobtoo.ru` и разворачивается
автоматически из standalone-сборки.

- [x] `output: 'standalone'` — переносимая серверная сборка
- [x] GitHub Actions публикует выпуск `dashboard`
- [x] Сервер забирает выпуск и разворачивает его через blue-green
- [x] Рассылки переписаны с облачной функции на PHP
- [x] Ключи Web Push создаются и хранятся только на российском сервере

---

## Этап 6. Push-уведомления

- [x] Переписана на PHP: рассылку делает `php-proxy/admin.php`. Движок
      функций на сервере не поднимали вовсе, а пока переписано не было,
      кнопка рассылки в дашборде отвечала 404 — и никто бы не узнал почему
- [x] Файл Deno-функции удалён: он вводил в заблуждение и вдобавок
      проверялся tsconfig вместе с приложением

---

## Этап 7. Эксплуатация и меры защиты

Это то, что требует приказ ФСТЭК №21 — и просто здравый смысл.

- [x] Ежедневный `pg_dump` в 04:30, 14 дней хранения, проверка целостности архива
- [x] Проверка восстановления — еженедельно, в отдельную базу. Копия, которую
      не разворачивали, копией не является
- [x] Хранение журналов: 500 МБ и 14 дней для системного, предел для докерного
- [x] Наблюдение: `status.json` обновляется раз в минуту и проверяет службы
      изнутри машины, а не по их собственному мнению о себе
- [x] Автообновление сертификатов + перезагрузка шлюза после продления
      (без неё nginx продолжал бы отдавать просроченный из памяти)
- [x] Storage переведён с суперпользователя на владельца своей схемы
- [x] Realtime и Studio — тоже на свои роли, не суперпользователем.
      У Realtime право на репликацию и чтение, у Studio только чтение.
      Пересадка с автооткатом: не поднялось — вернулся суперпользователь
- [x] Второй адрес хранения копий: объектное хранилище Timeweb. База
      ежедневно, файлы по воскресеньям — иначе одни и те же фотографии
      лежали бы в четырнадцати экземплярах
- [x] Медиа переписки — в закрытом бакете, ссылки подписанные и живут час.
      До этого фото и голосовые из чатов лежали в публичном: ссылка
      открывалась кем угодно и никогда не протухала
- [ ] Проверка, что файл переписки просит участник этой переписки. Сейчас
      нечем: у прокси нет способа узнать, кто его зовёт — приложение
      предъявляет общий секрет из сборки, а не токен человека. Нужны
      сессионные токены, это отдельная работа
- [ ] **Шифрование дисков — не сделано и не может быть сделано отсюда.**
      Диск шифруется при создании машины, на работающей это означает
      пересоздание сервера и перенос данных заново. Делать это через сутки
      после переезда — менять реальный риск простоя на бумажный. Правильный
      момент: когда понадобится машина мощнее, и переезд будет нужен всё равно
- [ ] Ротировать `GH_READ_TOKEN` — он показывался на экране во время работы
      с консолью
- [ ] Убрать `APP_SECRET_PREV`. Убирается очисткой значения, без выкладки:
      `array_filter` в db.php сам выбрасывает пустое. Раз в сутки в отчёт
      уходит число запросов со старым ключом — ноль несколько дней подряд
      и есть ответ, что пора

---

## Этап 8. Юридическое (не блокирует переезд)

Переезд сервера закрывает главное требование, но не всё.

- [x] **Expo Push (`exp.host`, США)** — поток инвентаризирован: передаются
      push-токен, служебный payload и текст сервисного уведомления; для новых
      сообщений имя отправителя и текст обычного чата минимизируются.
- [x] **Telegram (`api.telegram.org`)** — поток инвентаризирован отдельно:
      после добровольной привязки туда могут уходить chat ID, полный текст
      уведомления, имя отправителя и предпросмотр сообщения; карточка кандидата
      работодателю содержит имя/фамилию, возраст, метро, рейтинг и телефон.
- [x] **Nominatim** больше не основной геокодер — используется Яндекс (РФ).
- [x] Основное уведомление РКН об обработке ПДн направлено
      (№ 02-11-33294/77).
- [ ] Отдельное уведомление о намерении осуществлять трансграничную передачу
      по ст. 12 ещё нужно направить. № 02-11-34185/77 — прежнее обращение по
      вопросу, а не номер такого уведомления.
- [x] Документы приложения 19.09.2026 повторно сверены с трансграничным кодом:
      отдельно раскрыты push/web-push, Telegram, страны, категории субъектов,
      правовое основание и фактические категории передаваемых данных.
- [x] Удаление аккаунта действительно удаляет: своё стирает, чужое
      обезличивает. До этого кнопка не делала вообще ничего — запрос
      отклонялся по правам, а ответ никто не читал
- [x] Согласие записывается, у документов появились редакции. Кто
      регистрировался до 25 июня 2026, помечен как не дававший согласия:
      экрана с документами тогда не было
- [ ] Чаты между пользователями — возможен статус организатора распространения
      информации (ст. 10.1 149-ФЗ). Вопрос к юристу

---

## Деньги

| Статья | В месяц |
|---|---|
| Сервер 4/8/80 | ~1500–2500 ₽ |
| Объектное хранилище (бэкапы, файлы) | ~100–300 ₽ |
| **Итого** | **~2000–3000 ₽** |

По сравнению с прежней зарубежной инфраструктурой — сопоставимо или дешевле,
при том что данные оказываются в России.

---

## Чего опасаться

1. **Realtime в self-hosted капризен.** Требует `wal_level=logical` и живого
   слота репликации. Если слот отвалится — чаты тихо перестанут обновляться
   живьём, при этом всё остальное будет работать. Нужен отдельный healthcheck
   именно на Realtime.
2. **Память.** 8 ГБ хватает, но без запаса. Обязателен swap.
3. **Файлы Storage.** Их легко забыть при переносе — база переедет, а аватары
   останутся ссылками на мёртвый домен.
4. **Приложение у людей на телефонах.** Адрес зашит в собранной версии.
   Нужен EAS Update, и какое-то время часть людей будет ходить на старый адрес —
   поэтому старый Supabase и не выключаем сразу.
